// A nested project's files are invisible to the enclosing module
// (§V1.6): if the loader descended here, this file would fail to parse
// and the snippet would be rejected.
this is not volt at all {{{
