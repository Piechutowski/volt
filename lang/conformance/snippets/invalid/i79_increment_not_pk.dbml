// spec: §6.3 increment extension — increment applies only to the
// single-column primary key; elsewhere it would be silently ignored
Table users {
  id integer [pk]
  counter integer [not null, increment]
}
