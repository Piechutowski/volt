-- Code generated by nao gen sqlite from hostile.dbml; DO NOT EDIT.

PRAGMA foreign_keys = ON;

CREATE TABLE users (
  id INTEGER PRIMARY KEY NOT NULL
);

-- Same base name as public users; schema prefix disambiguates
CREATE TABLE core_users (
  id INTEGER PRIMARY KEY NOT NULL,
  api_key TEXT NOT NULL,
  homepage_url TEXT,
  http_status INTEGER,
  -- Quoted name with a space
  "full name" TEXT NOT NULL,
  precision_value REAL,
  balance TEXT,
  kind TEXT NOT NULL CHECK (kind IN ('read write', 'admin')),
  -- Column named like a Go keyword
  type TEXT,
  func INTEGER
);

CREATE TABLE "2fa_codes" (
  code TEXT NOT NULL,
  user_id INTEGER NOT NULL,
  expires_at TIMESTAMP NOT NULL,
  PRIMARY KEY (user_id, code),
  FOREIGN KEY (user_id) REFERENCES users (id)
);
