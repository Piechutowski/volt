-- Code generated by nao gen sqlite from seed_records.dbml; DO NOT EDIT.

PRAGMA foreign_keys = ON;

CREATE TABLE currencies (
  code TEXT PRIMARY KEY NOT NULL,
  name TEXT NOT NULL
);

-- Seeded exchange rates
CREATE TABLE rates (
  id INTEGER PRIMARY KEY,
  currency_code TEXT NOT NULL,
  rate TEXT NOT NULL,
  fetched_at TIMESTAMP NOT NULL,
  active INTEGER NOT NULL,
  comment TEXT,
  FOREIGN KEY (currency_code) REFERENCES currencies (code)
);

INSERT INTO currencies (code, name) VALUES
  ('EUR', 'Euro'),
  ('PLN', 'Polish złoty');

INSERT INTO rates (id, currency_code, rate, fetched_at, active, comment) VALUES
  (1, 'EUR', '4.32', '2024-01-15 10:30:00', 1, NULL),
  (2, 'PLN', '1.0', datetime('now'), 0, NULL);
