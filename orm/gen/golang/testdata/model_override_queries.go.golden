// Code generated by nao gen from model_override.edbml; DO NOT EDIT.
//
// CRUD methods over the models in nao_models.go. Identity arguments
// are positional, data arguments ride in params structs, and the SQL
// binds named placeholders — see docs/decisions.md D15-D17.

package models

import (
	"context"
	"database/sql"
	"github.com/Piechutowski/volt/orm/rt"
	"strings"
)

// Queries bundles the generated CRUD over one database handle: a
// *sql.DB, a *sql.Tx, or anything else satisfying rt.DBTX.
type Queries struct {
	db    rt.DBTX
	cache *rt.StmtCache
}

// New returns Queries running on db.
func New(db rt.DBTX) *Queries { return &Queries{db: db} }

// WithCache returns a copy of q whose dynamic queries (Query, Count,
// Exists, DeleteWhere, UpdateWhere) run through cache: identical option
// shapes render identical SQL, prepared once (D31). Bind a cache to a
// *sql.DB, never a transaction.
func (q *Queries) WithCache(cache *rt.StmtCache) *Queries {
	return &Queries{db: q.db, cache: cache}
}

// WithTx returns a copy of q running on tx. The statement cache does not
// follow: its statements are prepared on the outer handle (D31).
func (q *Queries) WithTx(tx *sql.Tx) *Queries { return &Queries{db: tx} }

// Tx runs fn inside a transaction: the Queries handed to fn joins it, a
// nil error commits, anything else rolls back. q must be backed by a
// handle that can begin a transaction (a *sql.DB).
func (q *Queries) Tx(ctx context.Context, fn func(*Queries) error) error {
	return rt.Tx(ctx, q.db, func(tx *sql.Tx) error { return fn(q.WithTx(tx)) })
}

// rowScanner is the shared surface of *sql.Row and *sql.Rows.
type rowScanner interface {
	Scan(dest ...any) error
}

// menuScan reads one menus row in generated column order.
func menuScan(r rowScanner) (Menu, error) {
	var v Menu
	err := r.Scan(&v.ID, &v.Title, &v.Price)
	return v, err
}

const menuGetSQL = `SELECT "id", "title", "price" FROM "menus" WHERE "id" = :id`

// MenuGet fetches the menus row with the given primary key, or rt.ErrNotFound.
func (q *Queries) MenuGet(ctx context.Context, id int32) (Menu, error) {
	return menuScan(q.db.QueryRowContext(ctx, menuGetSQL, sql.Named("id", id)))
}

const menuGetManySQL = `SELECT "id", "title", "price" FROM "menus" WHERE "id" IN`

// MenuGetMany fetches the menus rows whose primary key is in ids, in
// database order. Missing keys are not an error; they are just absent.
func (q *Queries) MenuGetMany(ctx context.Context, ids []int32) ([]Menu, error) {
	if len(ids) == 0 {
		return nil, nil
	}
	args := make([]any, len(ids))
	for i, k := range ids {
		args[i] = k
	}
	query := menuGetManySQL + " (?" + strings.Repeat(", ?", len(ids)-1) + ")"
	rows, err := q.db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Menu
	for rows.Next() {
		v, err := menuScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const menuListSQL = `SELECT "id", "title", "price" FROM "menus"`

// MenuList returns every menus row.
func (q *Queries) MenuList(ctx context.Context) ([]Menu, error) {
	rows, err := q.db.QueryContext(ctx, menuListSQL)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Menu
	for rows.Next() {
		v, err := menuScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const menuCreateSQL = `INSERT INTO "menus" ("title") VALUES (:title) RETURNING "id", "title", "price"`

// MenuCreateParams are the caller-supplied columns of MenuCreate. The
// auto-increment key and defaulted columns are the database's job (D16).
type MenuCreateParams struct {
	Title string `db:"title" json:"title"`
}

// MenuCreate inserts a menus row and returns the row the database wrote,
// generated keys and defaults filled in.
func (q *Queries) MenuCreate(ctx context.Context, arg MenuCreateParams) (Menu, error) {
	return menuScan(q.db.QueryRowContext(ctx, menuCreateSQL,
		sql.Named("title", arg.Title),
	))
}

const menuUpdateSQL = `UPDATE "menus" SET "title" = :title, "price" = :price WHERE "id" = :id RETURNING "id", "title", "price"`

// MenuUpdateParams are the data columns of MenuUpdate: every column
// outside the primary key.
type MenuUpdateParams struct {
	Title string `db:"title" json:"title"`
	Price string `db:"price" json:"price"`
}

// MenuUpdate rewrites every non-key column of the identified menus row
// and returns the result, or rt.ErrNotFound when no row matches.
func (q *Queries) MenuUpdate(ctx context.Context, id int32, arg MenuUpdateParams) (Menu, error) {
	return menuScan(q.db.QueryRowContext(ctx, menuUpdateSQL,
		sql.Named("id", id),
		sql.Named("title", arg.Title),
		sql.Named("price", arg.Price),
	))
}

const menuDeleteSQL = `DELETE FROM "menus" WHERE "id" = :id`

// MenuDelete removes the identified menus row; rt.ErrNotFound reports
// that nothing matched.
func (q *Queries) MenuDelete(ctx context.Context, id int32) error {
	res, err := q.db.ExecContext(ctx, menuDeleteSQL, sql.Named("id", id))
	if err != nil {
		return err
	}
	n, err := res.RowsAffected()
	if err != nil {
		return err
	}
	if n == 0 {
		return rt.ErrNotFound
	}
	return nil
}

// orderStatusScan reads one order_status row in generated column order.
func orderStatusScan(r rowScanner) (OrderStatus, error) {
	var v OrderStatus
	err := r.Scan(&v.Code, &v.Label)
	return v, err
}

const orderStatusGetSQL = `SELECT "code", "label" FROM "order_status" WHERE "code" = :code`

// OrderStatusGet fetches the order_status row with the given primary key, or rt.ErrNotFound.
func (q *Queries) OrderStatusGet(ctx context.Context, code string) (OrderStatus, error) {
	return orderStatusScan(q.db.QueryRowContext(ctx, orderStatusGetSQL, sql.Named("code", code)))
}

const orderStatusGetManySQL = `SELECT "code", "label" FROM "order_status" WHERE "code" IN`

// OrderStatusGetMany fetches the order_status rows whose primary key is in codes, in
// database order. Missing keys are not an error; they are just absent.
func (q *Queries) OrderStatusGetMany(ctx context.Context, codes []string) ([]OrderStatus, error) {
	if len(codes) == 0 {
		return nil, nil
	}
	args := make([]any, len(codes))
	for i, k := range codes {
		args[i] = k
	}
	query := orderStatusGetManySQL + " (?" + strings.Repeat(", ?", len(codes)-1) + ")"
	rows, err := q.db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []OrderStatus
	for rows.Next() {
		v, err := orderStatusScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const orderStatusGetByLabelSQL = `SELECT "code", "label" FROM "order_status" WHERE "label" = :label`

// OrderStatusGetByLabel fetches the order_status row with the given label (a unique column),
// or rt.ErrNotFound.
func (q *Queries) OrderStatusGetByLabel(ctx context.Context, label string) (OrderStatus, error) {
	return orderStatusScan(q.db.QueryRowContext(ctx, orderStatusGetByLabelSQL, sql.Named("label", label)))
}

const orderStatusListSQL = `SELECT "code", "label" FROM "order_status"`

// OrderStatusList returns every order_status row.
func (q *Queries) OrderStatusList(ctx context.Context) ([]OrderStatus, error) {
	rows, err := q.db.QueryContext(ctx, orderStatusListSQL)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []OrderStatus
	for rows.Next() {
		v, err := orderStatusScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const orderStatusCreateSQL = `INSERT INTO "order_status" ("code", "label") VALUES (:code, :label) RETURNING "code", "label"`

// OrderStatusCreateParams are the caller-supplied columns of OrderStatusCreate. The
// auto-increment key and defaulted columns are the database's job (D16).
type OrderStatusCreateParams struct {
	Code  string `db:"code" json:"code"`
	Label string `db:"label" json:"label"`
}

// OrderStatusCreate inserts a order_status row and returns the row the database wrote,
// generated keys and defaults filled in.
func (q *Queries) OrderStatusCreate(ctx context.Context, arg OrderStatusCreateParams) (OrderStatus, error) {
	return orderStatusScan(q.db.QueryRowContext(ctx, orderStatusCreateSQL,
		sql.Named("code", arg.Code),
		sql.Named("label", arg.Label),
	))
}

const orderStatusUpdateSQL = `UPDATE "order_status" SET "label" = :label WHERE "code" = :code RETURNING "code", "label"`

// OrderStatusUpdateParams are the data columns of OrderStatusUpdate: every column
// outside the primary key.
type OrderStatusUpdateParams struct {
	Label string `db:"label" json:"label"`
}

// OrderStatusUpdate rewrites every non-key column of the identified order_status row
// and returns the result, or rt.ErrNotFound when no row matches.
func (q *Queries) OrderStatusUpdate(ctx context.Context, code string, arg OrderStatusUpdateParams) (OrderStatus, error) {
	return orderStatusScan(q.db.QueryRowContext(ctx, orderStatusUpdateSQL,
		sql.Named("code", code),
		sql.Named("label", arg.Label),
	))
}

const orderStatusDeleteSQL = `DELETE FROM "order_status" WHERE "code" = :code`

// OrderStatusDelete removes the identified order_status row; rt.ErrNotFound reports
// that nothing matched.
func (q *Queries) OrderStatusDelete(ctx context.Context, code string) error {
	res, err := q.db.ExecContext(ctx, orderStatusDeleteSQL, sql.Named("code", code))
	if err != nil {
		return err
	}
	n, err := res.RowsAffected()
	if err != nil {
		return err
	}
	if n == 0 {
		return rt.ErrNotFound
	}
	return nil
}
