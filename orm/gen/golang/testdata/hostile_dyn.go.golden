// Code generated by nao gen from hostile.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/orm/rt"
	"time"
)

// userTableSQL and userColumnsSQL are the fixed shape of User's
// dynamic statements; the interpreter renders everything else (D28).
const (
	userTableSQL   = `"users"`
	userColumnsSQL = `"id"`
)

// Typed column handles of User (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter User queries; mixing models is a compile
// error.
var (
	UserID = rt.Column[User, int64]{Name: "id"}
)

// UserLimit caps how many rows UserQuery returns (D30).
func UserLimit(n int) rt.Opt[User] { return rt.Limit[User](n) }

// UserOffset skips n rows; keyset pagination (UserAfter) scales better (D34).
func UserOffset(n int) rt.Opt[User] { return rt.Offset[User](n) }

// UserDistinct deduplicates the rows UserQuery returns.
func UserDistinct() rt.Opt[User] { return rt.Distinct[User]() }

// UserOrderBy sorts UserQuery's rows by Asc/Desc terms built on the
// User column handles.
func UserOrderBy(terms ...rt.Order[User]) rt.Opt[User] { return rt.OrderBy(terms...) }

// UserAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per UserOrderBy term, in the same order.
func UserAfter(key ...any) rt.Opt[User] { return rt.After[User](key...) }

// UserSet collects the typed assignments of a UserUpdateWhere, built
// with Set/SetNull on the User column handles.
func UserSet(assigns ...rt.Assign[User]) []rt.Assign[User] { return assigns }

// UserQuery returns the users rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) UserQuery(ctx context.Context, opts ...rt.Opt[User]) ([]User, error) {
	query, args, err := rt.SelectRender(userTableSQL, userColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []User
	for rows.Next() {
		v, err := userScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// UserCount reports how many users rows match every given predicate;
// none counts the whole table.
func (q *Queries) UserCount(ctx context.Context, preds ...rt.Pred[User]) (int64, error) {
	query, args, err := rt.CountRender(userTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// UserExists reports whether any users row matches every given predicate.
func (q *Queries) UserExists(ctx context.Context, preds ...rt.Pred[User]) (bool, error) {
	query, args, err := rt.ExistsRender(userTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// UserDeleteWhere removes every users row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) UserDeleteWhere(ctx context.Context, preds ...rt.Pred[User]) (int64, error) {
	query, args, err := rt.DeleteRender(userTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// UserUpdateWhere applies the typed assignments (UserSet) to every users
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) UserUpdateWhere(ctx context.Context, set []rt.Assign[User], preds ...rt.Pred[User]) (int64, error) {
	query, args, err := rt.UpdateRender(userTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// coreUserTableSQL and coreUserColumnsSQL are the fixed shape of CoreUser's
// dynamic statements; the interpreter renders everything else (D28).
const (
	coreUserTableSQL   = `"core_users"`
	coreUserColumnsSQL = `"id", "api_key", "homepage_url", "http_status", "full name", "precision_value", "balance", "kind", "type", "func"`
)

// Typed column handles of CoreUser (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter CoreUser queries; mixing models is a compile
// error.
var (
	CoreUserID             = rt.Column[CoreUser, uint64]{Name: "id"}
	CoreUserAPIKey         = rt.Column[CoreUser, string]{Name: "api_key"}
	CoreUserHomepageURL    = rt.NullColumn[CoreUser, string]{Column: rt.Column[CoreUser, string]{Name: "homepage_url"}}
	CoreUserHTTPStatus     = rt.NullColumn[CoreUser, int32]{Column: rt.Column[CoreUser, int32]{Name: "http_status"}}
	CoreUserFullName       = rt.Column[CoreUser, string]{Name: "full name"}
	CoreUserPrecisionValue = rt.NullColumn[CoreUser, float64]{Column: rt.Column[CoreUser, float64]{Name: "precision_value"}}
	CoreUserBalance        = rt.NullColumn[CoreUser, string]{Column: rt.Column[CoreUser, string]{Name: "balance"}}
	CoreUserKind           = rt.Column[CoreUser, ECoreAuditKind]{Name: "kind"}
	CoreUserType           = rt.NullColumn[CoreUser, string]{Column: rt.Column[CoreUser, string]{Name: "type"}}
	CoreUserFunc           = rt.NullColumn[CoreUser, int32]{Column: rt.Column[CoreUser, int32]{Name: "func"}}
)

// CoreUserLimit caps how many rows CoreUserQuery returns (D30).
func CoreUserLimit(n int) rt.Opt[CoreUser] { return rt.Limit[CoreUser](n) }

// CoreUserOffset skips n rows; keyset pagination (CoreUserAfter) scales better (D34).
func CoreUserOffset(n int) rt.Opt[CoreUser] { return rt.Offset[CoreUser](n) }

// CoreUserDistinct deduplicates the rows CoreUserQuery returns.
func CoreUserDistinct() rt.Opt[CoreUser] { return rt.Distinct[CoreUser]() }

// CoreUserOrderBy sorts CoreUserQuery's rows by Asc/Desc terms built on the
// CoreUser column handles.
func CoreUserOrderBy(terms ...rt.Order[CoreUser]) rt.Opt[CoreUser] { return rt.OrderBy(terms...) }

// CoreUserAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per CoreUserOrderBy term, in the same order.
func CoreUserAfter(key ...any) rt.Opt[CoreUser] { return rt.After[CoreUser](key...) }

// CoreUserSet collects the typed assignments of a CoreUserUpdateWhere, built
// with Set/SetNull on the CoreUser column handles.
func CoreUserSet(assigns ...rt.Assign[CoreUser]) []rt.Assign[CoreUser] { return assigns }

// CoreUserQuery returns the core.users rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) CoreUserQuery(ctx context.Context, opts ...rt.Opt[CoreUser]) ([]CoreUser, error) {
	query, args, err := rt.SelectRender(coreUserTableSQL, coreUserColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []CoreUser
	for rows.Next() {
		v, err := coreUserScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// CoreUserCount reports how many core.users rows match every given predicate;
// none counts the whole table.
func (q *Queries) CoreUserCount(ctx context.Context, preds ...rt.Pred[CoreUser]) (int64, error) {
	query, args, err := rt.CountRender(coreUserTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// CoreUserExists reports whether any core.users row matches every given predicate.
func (q *Queries) CoreUserExists(ctx context.Context, preds ...rt.Pred[CoreUser]) (bool, error) {
	query, args, err := rt.ExistsRender(coreUserTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// CoreUserDeleteWhere removes every core.users row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) CoreUserDeleteWhere(ctx context.Context, preds ...rt.Pred[CoreUser]) (int64, error) {
	query, args, err := rt.DeleteRender(coreUserTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// CoreUserUpdateWhere applies the typed assignments (CoreUserSet) to every core.users
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) CoreUserUpdateWhere(ctx context.Context, set []rt.Assign[CoreUser], preds ...rt.Pred[CoreUser]) (int64, error) {
	query, args, err := rt.UpdateRender(coreUserTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// x2faCodeTableSQL and x2faCodeColumnsSQL are the fixed shape of X2faCode's
// dynamic statements; the interpreter renders everything else (D28).
const (
	x2faCodeTableSQL   = `"2fa_codes"`
	x2faCodeColumnsSQL = `"code", "user_id", "expires_at"`
)

// Typed column handles of X2faCode (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter X2faCode queries; mixing models is a compile
// error.
var (
	X2faCodeCode      = rt.Column[X2faCode, string]{Name: "code"}
	X2faCodeUserID    = rt.Column[X2faCode, int64]{Name: "user_id"}
	X2faCodeExpiresAt = rt.Column[X2faCode, time.Time]{Name: "expires_at"}
)

// X2faCodeLimit caps how many rows X2faCodeQuery returns (D30).
func X2faCodeLimit(n int) rt.Opt[X2faCode] { return rt.Limit[X2faCode](n) }

// X2faCodeOffset skips n rows; keyset pagination (X2faCodeAfter) scales better (D34).
func X2faCodeOffset(n int) rt.Opt[X2faCode] { return rt.Offset[X2faCode](n) }

// X2faCodeDistinct deduplicates the rows X2faCodeQuery returns.
func X2faCodeDistinct() rt.Opt[X2faCode] { return rt.Distinct[X2faCode]() }

// X2faCodeOrderBy sorts X2faCodeQuery's rows by Asc/Desc terms built on the
// X2faCode column handles.
func X2faCodeOrderBy(terms ...rt.Order[X2faCode]) rt.Opt[X2faCode] { return rt.OrderBy(terms...) }

// X2faCodeAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per X2faCodeOrderBy term, in the same order.
func X2faCodeAfter(key ...any) rt.Opt[X2faCode] { return rt.After[X2faCode](key...) }

// X2faCodeSet collects the typed assignments of a X2faCodeUpdateWhere, built
// with Set/SetNull on the X2faCode column handles.
func X2faCodeSet(assigns ...rt.Assign[X2faCode]) []rt.Assign[X2faCode] { return assigns }

// X2faCodeQuery returns the 2fa_codes rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) X2faCodeQuery(ctx context.Context, opts ...rt.Opt[X2faCode]) ([]X2faCode, error) {
	query, args, err := rt.SelectRender(x2faCodeTableSQL, x2faCodeColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []X2faCode
	for rows.Next() {
		v, err := x2faCodeScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// X2faCodeCount reports how many 2fa_codes rows match every given predicate;
// none counts the whole table.
func (q *Queries) X2faCodeCount(ctx context.Context, preds ...rt.Pred[X2faCode]) (int64, error) {
	query, args, err := rt.CountRender(x2faCodeTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// X2faCodeExists reports whether any 2fa_codes row matches every given predicate.
func (q *Queries) X2faCodeExists(ctx context.Context, preds ...rt.Pred[X2faCode]) (bool, error) {
	query, args, err := rt.ExistsRender(x2faCodeTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// X2faCodeDeleteWhere removes every 2fa_codes row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) X2faCodeDeleteWhere(ctx context.Context, preds ...rt.Pred[X2faCode]) (int64, error) {
	query, args, err := rt.DeleteRender(x2faCodeTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// X2faCodeUpdateWhere applies the typed assignments (X2faCodeSet) to every 2fa_codes
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) X2faCodeUpdateWhere(ctx context.Context, set []rt.Assign[X2faCode], preds ...rt.Pred[X2faCode]) (int64, error) {
	query, args, err := rt.UpdateRender(x2faCodeTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
