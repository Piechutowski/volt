// Code generated by nao gen from closure_table.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/orm/rt"
)

// categoryTableSQL and categoryColumnsSQL are the fixed shape of Category's
// dynamic statements; the interpreter renders everything else (D28).
const (
	categoryTableSQL   = `"categories"`
	categoryColumnsSQL = `"id", "name"`
)

// Typed column handles of Category (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Category queries; mixing models is a compile
// error.
var (
	CategoryID   = rt.Column[Category, int32]{Name: "id"}
	CategoryName = rt.Column[Category, string]{Name: "name"}
)

// CategoryLimit caps how many rows CategoryQuery returns (D30).
func CategoryLimit(n int) rt.Opt[Category] { return rt.Limit[Category](n) }

// CategoryOffset skips n rows; keyset pagination (CategoryAfter) scales better (D34).
func CategoryOffset(n int) rt.Opt[Category] { return rt.Offset[Category](n) }

// CategoryDistinct deduplicates the rows CategoryQuery returns.
func CategoryDistinct() rt.Opt[Category] { return rt.Distinct[Category]() }

// CategoryOrderBy sorts CategoryQuery's rows by Asc/Desc terms built on the
// Category column handles.
func CategoryOrderBy(terms ...rt.Order[Category]) rt.Opt[Category] { return rt.OrderBy(terms...) }

// CategoryAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per CategoryOrderBy term, in the same order.
func CategoryAfter(key ...any) rt.Opt[Category] { return rt.After[Category](key...) }

// CategorySet collects the typed assignments of a CategoryUpdateWhere, built
// with Set/SetNull on the Category column handles.
func CategorySet(assigns ...rt.Assign[Category]) []rt.Assign[Category] { return assigns }

// CategoryQuery returns the categories rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) CategoryQuery(ctx context.Context, opts ...rt.Opt[Category]) ([]Category, error) {
	query, args, err := rt.SelectRender(categoryTableSQL, categoryColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Category
	for rows.Next() {
		v, err := categoryScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// CategoryCount reports how many categories rows match every given predicate;
// none counts the whole table.
func (q *Queries) CategoryCount(ctx context.Context, preds ...rt.Pred[Category]) (int64, error) {
	query, args, err := rt.CountRender(categoryTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// CategoryExists reports whether any categories row matches every given predicate.
func (q *Queries) CategoryExists(ctx context.Context, preds ...rt.Pred[Category]) (bool, error) {
	query, args, err := rt.ExistsRender(categoryTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// CategoryDeleteWhere removes every categories row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) CategoryDeleteWhere(ctx context.Context, preds ...rt.Pred[Category]) (int64, error) {
	query, args, err := rt.DeleteRender(categoryTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// CategoryUpdateWhere applies the typed assignments (CategorySet) to every categories
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) CategoryUpdateWhere(ctx context.Context, set []rt.Assign[Category], preds ...rt.Pred[Category]) (int64, error) {
	query, args, err := rt.UpdateRender(categoryTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// categoryPathTableSQL and categoryPathColumnsSQL are the fixed shape of CategoryPath's
// dynamic statements; the interpreter renders everything else (D28).
const (
	categoryPathTableSQL   = `"category_paths"`
	categoryPathColumnsSQL = `"ancestor_id", "descendant_id", "depth"`
)

// Typed column handles of CategoryPath (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter CategoryPath queries; mixing models is a compile
// error.
var (
	CategoryPathAncestorID   = rt.Column[CategoryPath, int32]{Name: "ancestor_id"}
	CategoryPathDescendantID = rt.Column[CategoryPath, int32]{Name: "descendant_id"}
	CategoryPathDepth        = rt.Column[CategoryPath, int32]{Name: "depth"}
)

// CategoryPathLimit caps how many rows CategoryPathQuery returns (D30).
func CategoryPathLimit(n int) rt.Opt[CategoryPath] { return rt.Limit[CategoryPath](n) }

// CategoryPathOffset skips n rows; keyset pagination (CategoryPathAfter) scales better (D34).
func CategoryPathOffset(n int) rt.Opt[CategoryPath] { return rt.Offset[CategoryPath](n) }

// CategoryPathDistinct deduplicates the rows CategoryPathQuery returns.
func CategoryPathDistinct() rt.Opt[CategoryPath] { return rt.Distinct[CategoryPath]() }

// CategoryPathOrderBy sorts CategoryPathQuery's rows by Asc/Desc terms built on the
// CategoryPath column handles.
func CategoryPathOrderBy(terms ...rt.Order[CategoryPath]) rt.Opt[CategoryPath] {
	return rt.OrderBy(terms...)
}

// CategoryPathAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per CategoryPathOrderBy term, in the same order.
func CategoryPathAfter(key ...any) rt.Opt[CategoryPath] { return rt.After[CategoryPath](key...) }

// CategoryPathSet collects the typed assignments of a CategoryPathUpdateWhere, built
// with Set/SetNull on the CategoryPath column handles.
func CategoryPathSet(assigns ...rt.Assign[CategoryPath]) []rt.Assign[CategoryPath] { return assigns }

// CategoryPathQuery returns the category_paths rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) CategoryPathQuery(ctx context.Context, opts ...rt.Opt[CategoryPath]) ([]CategoryPath, error) {
	query, args, err := rt.SelectRender(categoryPathTableSQL, categoryPathColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []CategoryPath
	for rows.Next() {
		v, err := categoryPathScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// CategoryPathCount reports how many category_paths rows match every given predicate;
// none counts the whole table.
func (q *Queries) CategoryPathCount(ctx context.Context, preds ...rt.Pred[CategoryPath]) (int64, error) {
	query, args, err := rt.CountRender(categoryPathTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// CategoryPathExists reports whether any category_paths row matches every given predicate.
func (q *Queries) CategoryPathExists(ctx context.Context, preds ...rt.Pred[CategoryPath]) (bool, error) {
	query, args, err := rt.ExistsRender(categoryPathTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// CategoryPathDeleteWhere removes every category_paths row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) CategoryPathDeleteWhere(ctx context.Context, preds ...rt.Pred[CategoryPath]) (int64, error) {
	query, args, err := rt.DeleteRender(categoryPathTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// CategoryPathUpdateWhere applies the typed assignments (CategoryPathSet) to every category_paths
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) CategoryPathUpdateWhere(ctx context.Context, set []rt.Assign[CategoryPath], preds ...rt.Pred[CategoryPath]) (int64, error) {
	query, args, err := rt.UpdateRender(categoryPathTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
