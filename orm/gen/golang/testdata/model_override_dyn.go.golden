// Code generated by nao gen from model_override.edbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/orm/rt"
)

// menuTableSQL and menuColumnsSQL are the fixed shape of Menu's
// dynamic statements; the interpreter renders everything else (D28).
const (
	menuTableSQL   = `"menus"`
	menuColumnsSQL = `"id", "title", "price"`
)

// Typed column handles of Menu (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Menu queries; mixing models is a compile
// error.
var (
	MenuID    = rt.Column[Menu, int32]{Name: "id"}
	MenuTitle = rt.Column[Menu, string]{Name: "title"}
	MenuPrice = rt.Column[Menu, string]{Name: "price"}
)

// MenuLimit caps how many rows MenuQuery returns (D30).
func MenuLimit(n int) rt.Opt[Menu] { return rt.Limit[Menu](n) }

// MenuOffset skips n rows; keyset pagination (MenuAfter) scales better (D34).
func MenuOffset(n int) rt.Opt[Menu] { return rt.Offset[Menu](n) }

// MenuDistinct deduplicates the rows MenuQuery returns.
func MenuDistinct() rt.Opt[Menu] { return rt.Distinct[Menu]() }

// MenuOrderBy sorts MenuQuery's rows by Asc/Desc terms built on the
// Menu column handles.
func MenuOrderBy(terms ...rt.Order[Menu]) rt.Opt[Menu] { return rt.OrderBy(terms...) }

// MenuAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per MenuOrderBy term, in the same order.
func MenuAfter(key ...any) rt.Opt[Menu] { return rt.After[Menu](key...) }

// MenuSet collects the typed assignments of a MenuUpdateWhere, built
// with Set/SetNull on the Menu column handles.
func MenuSet(assigns ...rt.Assign[Menu]) []rt.Assign[Menu] { return assigns }

// MenuQuery returns the menus rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) MenuQuery(ctx context.Context, opts ...rt.Opt[Menu]) ([]Menu, error) {
	query, args, err := rt.SelectRender(menuTableSQL, menuColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Menu
	for rows.Next() {
		v, err := menuScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// MenuCount reports how many menus rows match every given predicate;
// none counts the whole table.
func (q *Queries) MenuCount(ctx context.Context, preds ...rt.Pred[Menu]) (int64, error) {
	query, args, err := rt.CountRender(menuTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// MenuExists reports whether any menus row matches every given predicate.
func (q *Queries) MenuExists(ctx context.Context, preds ...rt.Pred[Menu]) (bool, error) {
	query, args, err := rt.ExistsRender(menuTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// MenuDeleteWhere removes every menus row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) MenuDeleteWhere(ctx context.Context, preds ...rt.Pred[Menu]) (int64, error) {
	query, args, err := rt.DeleteRender(menuTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// MenuUpdateWhere applies the typed assignments (MenuSet) to every menus
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) MenuUpdateWhere(ctx context.Context, set []rt.Assign[Menu], preds ...rt.Pred[Menu]) (int64, error) {
	query, args, err := rt.UpdateRender(menuTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// orderStatusTableSQL and orderStatusColumnsSQL are the fixed shape of OrderStatus's
// dynamic statements; the interpreter renders everything else (D28).
const (
	orderStatusTableSQL   = `"order_status"`
	orderStatusColumnsSQL = `"code", "label"`
)

// Typed column handles of OrderStatus (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter OrderStatus queries; mixing models is a compile
// error.
var (
	OrderStatusCode  = rt.Column[OrderStatus, string]{Name: "code"}
	OrderStatusLabel = rt.Column[OrderStatus, string]{Name: "label"}
)

// OrderStatusLimit caps how many rows OrderStatusQuery returns (D30).
func OrderStatusLimit(n int) rt.Opt[OrderStatus] { return rt.Limit[OrderStatus](n) }

// OrderStatusOffset skips n rows; keyset pagination (OrderStatusAfter) scales better (D34).
func OrderStatusOffset(n int) rt.Opt[OrderStatus] { return rt.Offset[OrderStatus](n) }

// OrderStatusDistinct deduplicates the rows OrderStatusQuery returns.
func OrderStatusDistinct() rt.Opt[OrderStatus] { return rt.Distinct[OrderStatus]() }

// OrderStatusOrderBy sorts OrderStatusQuery's rows by Asc/Desc terms built on the
// OrderStatus column handles.
func OrderStatusOrderBy(terms ...rt.Order[OrderStatus]) rt.Opt[OrderStatus] {
	return rt.OrderBy(terms...)
}

// OrderStatusAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per OrderStatusOrderBy term, in the same order.
func OrderStatusAfter(key ...any) rt.Opt[OrderStatus] { return rt.After[OrderStatus](key...) }

// OrderStatusSet collects the typed assignments of a OrderStatusUpdateWhere, built
// with Set/SetNull on the OrderStatus column handles.
func OrderStatusSet(assigns ...rt.Assign[OrderStatus]) []rt.Assign[OrderStatus] { return assigns }

// OrderStatusQuery returns the order_status rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) OrderStatusQuery(ctx context.Context, opts ...rt.Opt[OrderStatus]) ([]OrderStatus, error) {
	query, args, err := rt.SelectRender(orderStatusTableSQL, orderStatusColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []OrderStatus
	for rows.Next() {
		v, err := orderStatusScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// OrderStatusCount reports how many order_status rows match every given predicate;
// none counts the whole table.
func (q *Queries) OrderStatusCount(ctx context.Context, preds ...rt.Pred[OrderStatus]) (int64, error) {
	query, args, err := rt.CountRender(orderStatusTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// OrderStatusExists reports whether any order_status row matches every given predicate.
func (q *Queries) OrderStatusExists(ctx context.Context, preds ...rt.Pred[OrderStatus]) (bool, error) {
	query, args, err := rt.ExistsRender(orderStatusTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// OrderStatusDeleteWhere removes every order_status row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) OrderStatusDeleteWhere(ctx context.Context, preds ...rt.Pred[OrderStatus]) (int64, error) {
	query, args, err := rt.DeleteRender(orderStatusTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// OrderStatusUpdateWhere applies the typed assignments (OrderStatusSet) to every order_status
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) OrderStatusUpdateWhere(ctx context.Context, set []rt.Assign[OrderStatus], preds ...rt.Pred[OrderStatus]) (int64, error) {
	query, args, err := rt.UpdateRender(orderStatusTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
