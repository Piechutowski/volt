// spec: §1.4 — keywords are case-insensitive
TABLE users {
  id INT [PK, NOT NULL]
}
enum status { active }
table posts { user_id int [Ref: > users.id] }
table likes { user_id int }
REF r: users.id < likes.user_id
