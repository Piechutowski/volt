-- Code generated by nao gen sqlite from polymorphic.dbml; DO NOT EDIT.

PRAGMA foreign_keys = ON;

-- Polymorphic: (commentable_type, commentable_id) addresses a row
-- in whichever table the type names. No FK can enforce this.
CREATE TABLE comments (
  id INTEGER PRIMARY KEY,
  commentable_type TEXT NOT NULL CHECK (commentable_type IN ('post', 'photo', 'video')),
  -- ID within the table selected by commentable_type
  commentable_id INTEGER NOT NULL,
  body TEXT NOT NULL
);
CREATE INDEX idx_comments_1 ON comments (commentable_type, commentable_id);
