-- Code generated by nao gen sqlite from closure_table.dbml; DO NOT EDIT.

PRAGMA foreign_keys = ON;

CREATE TABLE categories (
  id INTEGER PRIMARY KEY NOT NULL,
  name TEXT NOT NULL
);

-- Closure table over categories
CREATE TABLE category_paths (
  ancestor_id INTEGER NOT NULL,
  descendant_id INTEGER NOT NULL,
  -- 0 for the self-pair
  depth INTEGER NOT NULL,
  PRIMARY KEY (ancestor_id, descendant_id),
  FOREIGN KEY (ancestor_id) REFERENCES categories (id),
  FOREIGN KEY (descendant_id) REFERENCES categories (id)
);
CREATE INDEX idx_category_paths_1 ON category_paths (descendant_id);
