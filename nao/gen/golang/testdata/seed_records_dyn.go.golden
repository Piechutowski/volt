// Code generated by nao gen from seed_records.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/nao/rt"
	"time"
)

// currencyTableSQL and currencyColumnsSQL are the fixed shape of Currency's
// dynamic statements; the interpreter renders everything else (D28).
const (
	currencyTableSQL   = `"currencies"`
	currencyColumnsSQL = `"code", "name"`
)

// Typed column handles of Currency (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Currency queries; mixing models is a compile
// error.
var (
	CurrencyCode = rt.Column[Currency, string]{Name: "code"}
	CurrencyName = rt.Column[Currency, string]{Name: "name"}
)

// CurrencyLimit caps how many rows CurrencyQuery returns (D30).
func CurrencyLimit(n int) rt.Opt[Currency] { return rt.Limit[Currency](n) }

// CurrencyOffset skips n rows; keyset pagination (CurrencyAfter) scales better (D34).
func CurrencyOffset(n int) rt.Opt[Currency] { return rt.Offset[Currency](n) }

// CurrencyDistinct deduplicates the rows CurrencyQuery returns.
func CurrencyDistinct() rt.Opt[Currency] { return rt.Distinct[Currency]() }

// CurrencyOrderBy sorts CurrencyQuery's rows by Asc/Desc terms built on the
// Currency column handles.
func CurrencyOrderBy(terms ...rt.Order[Currency]) rt.Opt[Currency] { return rt.OrderBy(terms...) }

// CurrencyAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per CurrencyOrderBy term, in the same order.
func CurrencyAfter(key ...any) rt.Opt[Currency] { return rt.After[Currency](key...) }

// CurrencySet collects the typed assignments of a CurrencyUpdateWhere, built
// with Set/SetNull on the Currency column handles.
func CurrencySet(assigns ...rt.Assign[Currency]) []rt.Assign[Currency] { return assigns }

// CurrencyQuery returns the currencies rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) CurrencyQuery(ctx context.Context, opts ...rt.Opt[Currency]) ([]Currency, error) {
	query, args, err := rt.SelectRender(currencyTableSQL, currencyColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	out := []Currency{}
	for rows.Next() {
		v, err := currencyScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// CurrencyCount reports how many currencies rows match every given predicate;
// none counts the whole table.
func (q *Queries) CurrencyCount(ctx context.Context, preds ...rt.Pred[Currency]) (int64, error) {
	query, args, err := rt.CountRender(currencyTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// CurrencyExists reports whether any currencies row matches every given predicate.
func (q *Queries) CurrencyExists(ctx context.Context, preds ...rt.Pred[Currency]) (bool, error) {
	query, args, err := rt.ExistsRender(currencyTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// CurrencyDeleteWhere removes every currencies row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) CurrencyDeleteWhere(ctx context.Context, preds ...rt.Pred[Currency]) (int64, error) {
	query, args, err := rt.DeleteRender(currencyTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// CurrencyUpdateWhere applies the typed assignments (CurrencySet) to every currencies
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) CurrencyUpdateWhere(ctx context.Context, set []rt.Assign[Currency], preds ...rt.Pred[Currency]) (int64, error) {
	query, args, err := rt.UpdateRender(currencyTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// rateTableSQL and rateColumnsSQL are the fixed shape of Rate's
// dynamic statements; the interpreter renders everything else (D28).
const (
	rateTableSQL   = `"rates"`
	rateColumnsSQL = `"id", "currency_code", "rate", "fetched_at", "active", "comment"`
)

// Typed column handles of Rate (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Rate queries; mixing models is a compile
// error.
var (
	RateID           = rt.Column[Rate, int32]{Name: "id"}
	RateCurrencyCode = rt.Column[Rate, string]{Name: "currency_code"}
	RateRate         = rt.Column[Rate, string]{Name: "rate"}
	RateFetchedAt    = rt.Column[Rate, time.Time]{Name: "fetched_at"}
	RateActive       = rt.Column[Rate, bool]{Name: "active"}
	RateComment      = rt.NullColumn[Rate, string]{Column: rt.Column[Rate, string]{Name: "comment"}}
)

// RateLimit caps how many rows RateQuery returns (D30).
func RateLimit(n int) rt.Opt[Rate] { return rt.Limit[Rate](n) }

// RateOffset skips n rows; keyset pagination (RateAfter) scales better (D34).
func RateOffset(n int) rt.Opt[Rate] { return rt.Offset[Rate](n) }

// RateDistinct deduplicates the rows RateQuery returns.
func RateDistinct() rt.Opt[Rate] { return rt.Distinct[Rate]() }

// RateOrderBy sorts RateQuery's rows by Asc/Desc terms built on the
// Rate column handles.
func RateOrderBy(terms ...rt.Order[Rate]) rt.Opt[Rate] { return rt.OrderBy(terms...) }

// RateAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per RateOrderBy term, in the same order.
func RateAfter(key ...any) rt.Opt[Rate] { return rt.After[Rate](key...) }

// RateSet collects the typed assignments of a RateUpdateWhere, built
// with Set/SetNull on the Rate column handles.
func RateSet(assigns ...rt.Assign[Rate]) []rt.Assign[Rate] { return assigns }

// RateQuery returns the rates rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) RateQuery(ctx context.Context, opts ...rt.Opt[Rate]) ([]Rate, error) {
	query, args, err := rt.SelectRender(rateTableSQL, rateColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	out := []Rate{}
	for rows.Next() {
		v, err := rateScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// RateCount reports how many rates rows match every given predicate;
// none counts the whole table.
func (q *Queries) RateCount(ctx context.Context, preds ...rt.Pred[Rate]) (int64, error) {
	query, args, err := rt.CountRender(rateTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// RateExists reports whether any rates row matches every given predicate.
func (q *Queries) RateExists(ctx context.Context, preds ...rt.Pred[Rate]) (bool, error) {
	query, args, err := rt.ExistsRender(rateTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// RateDeleteWhere removes every rates row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) RateDeleteWhere(ctx context.Context, preds ...rt.Pred[Rate]) (int64, error) {
	query, args, err := rt.DeleteRender(rateTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// RateUpdateWhere applies the typed assignments (RateSet) to every rates
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) RateUpdateWhere(ctx context.Context, set []rt.Assign[Rate], preds ...rt.Pred[Rate]) (int64, error) {
	query, args, err := rt.UpdateRender(rateTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
