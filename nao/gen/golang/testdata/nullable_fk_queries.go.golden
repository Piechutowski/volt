// Code generated by nao gen from nullable_fk.dbml; DO NOT EDIT.
//
// CRUD methods over the models in nao_models.go. Identity arguments
// are positional, data arguments ride in params structs, and the SQL
// binds named placeholders — see docs/decisions.md D15-D17.

package models

import (
	"context"
	"database/sql"
	"encoding/json"
	"github.com/Piechutowski/volt/nao/rt"
	"strings"
	"time"
)

// Queries bundles the generated CRUD over one database handle: a
// *sql.DB, a *sql.Tx, or anything else satisfying rt.DBTX.
type Queries struct {
	db    rt.DBTX
	cache *rt.StmtCache
}

// New returns Queries running on db.
func New(db rt.DBTX) *Queries { return &Queries{db: db} }

// WithCache returns a copy of q whose dynamic queries (Query, Count,
// Exists, DeleteWhere, UpdateWhere) run through cache: identical option
// shapes render identical SQL, prepared once (D31). Bind a cache to a
// *sql.DB, never a transaction.
func (q *Queries) WithCache(cache *rt.StmtCache) *Queries {
	return &Queries{db: q.db, cache: cache}
}

// WithTx returns a copy of q running on tx. The statement cache does not
// follow: its statements are prepared on the outer handle (D31).
func (q *Queries) WithTx(tx *sql.Tx) *Queries { return &Queries{db: tx} }

// Tx runs fn inside a transaction: the Queries handed to fn joins it, a
// nil error commits, anything else rolls back. q must be backed by a
// handle that can begin a transaction (a *sql.DB).
func (q *Queries) Tx(ctx context.Context, fn func(*Queries) error) error {
	return rt.Tx(ctx, q.db, func(tx *sql.Tx) error { return fn(q.WithTx(tx)) })
}

// rowScanner is the shared surface of *sql.Row and *sql.Rows.
type rowScanner interface {
	Scan(dest ...any) error
}

// userScan reads one users row in generated column order.
func userScan(r rowScanner) (User, error) {
	var v User
	err := r.Scan(&v.ID)
	return v, err
}

const userGetSQL = `SELECT "id" FROM "users" WHERE "id" = :id`

// UserGet fetches the users row with the given primary key, or rt.ErrNotFound.
func (q *Queries) UserGet(ctx context.Context, id int64) (User, error) {
	return userScan(q.db.QueryRowContext(ctx, userGetSQL, sql.Named("id", id)))
}

const userGetManySQL = `SELECT "id" FROM "users" WHERE "id" IN`

// UserGetMany fetches the users rows whose primary key is in ids, in
// database order. Missing keys are not an error; they are just absent.
func (q *Queries) UserGetMany(ctx context.Context, ids []int64) ([]User, error) {
	if len(ids) == 0 {
		return nil, nil
	}
	args := make([]any, len(ids))
	for i, k := range ids {
		args[i] = k
	}
	query := userGetManySQL + " (?" + strings.Repeat(", ?", len(ids)-1) + ")"
	rows, err := q.db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []User
	for rows.Next() {
		v, err := userScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const userListSQL = `SELECT "id" FROM "users"`

// UserList returns every users row.
func (q *Queries) UserList(ctx context.Context) ([]User, error) {
	rows, err := q.db.QueryContext(ctx, userListSQL)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []User
	for rows.Next() {
		v, err := userScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const userCreateSQL = `INSERT INTO "users" ("id") VALUES (:id) RETURNING "id"`

// UserCreateParams are the caller-supplied columns of UserCreate. The
// auto-increment key and defaulted columns are the database's job (D16).
type UserCreateParams struct {
	ID int64 `db:"id" json:"id"`
}

// UserCreate inserts a users row and returns the row the database wrote,
// generated keys and defaults filled in.
func (q *Queries) UserCreate(ctx context.Context, arg UserCreateParams) (User, error) {
	return userScan(q.db.QueryRowContext(ctx, userCreateSQL,
		sql.Named("id", arg.ID),
	))
}

const userDeleteSQL = `DELETE FROM "users" WHERE "id" = :id`

// UserDelete removes the identified users row; rt.ErrNotFound reports
// that nothing matched.
func (q *Queries) UserDelete(ctx context.Context, id int64) error {
	res, err := q.db.ExecContext(ctx, userDeleteSQL, sql.Named("id", id))
	if err != nil {
		return err
	}
	n, err := res.RowsAffected()
	if err != nil {
		return err
	}
	if n == 0 {
		return rt.ErrNotFound
	}
	return nil
}

// postScan reads one posts row in generated column order.
func postScan(r rowScanner) (Post, error) {
	var v Post
	err := r.Scan(&v.ID, &v.AuthorID, &v.EditorID, &v.ReviewedAt, &v.Meta, &v.Raw)
	return v, err
}

const postGetSQL = `SELECT "id", "author_id", "editor_id", "reviewed_at", "meta", "raw" FROM "posts" WHERE "id" = :id`

// PostGet fetches the posts row with the given primary key, or rt.ErrNotFound.
func (q *Queries) PostGet(ctx context.Context, id int64) (Post, error) {
	return postScan(q.db.QueryRowContext(ctx, postGetSQL, sql.Named("id", id)))
}

const postGetManySQL = `SELECT "id", "author_id", "editor_id", "reviewed_at", "meta", "raw" FROM "posts" WHERE "id" IN`

// PostGetMany fetches the posts rows whose primary key is in ids, in
// database order. Missing keys are not an error; they are just absent.
func (q *Queries) PostGetMany(ctx context.Context, ids []int64) ([]Post, error) {
	if len(ids) == 0 {
		return nil, nil
	}
	args := make([]any, len(ids))
	for i, k := range ids {
		args[i] = k
	}
	query := postGetManySQL + " (?" + strings.Repeat(", ?", len(ids)-1) + ")"
	rows, err := q.db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Post
	for rows.Next() {
		v, err := postScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const postListSQL = `SELECT "id", "author_id", "editor_id", "reviewed_at", "meta", "raw" FROM "posts"`

// PostList returns every posts row.
func (q *Queries) PostList(ctx context.Context) ([]Post, error) {
	rows, err := q.db.QueryContext(ctx, postListSQL)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Post
	for rows.Next() {
		v, err := postScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

const postCreateSQL = `INSERT INTO "posts" ("id", "author_id", "editor_id", "reviewed_at", "meta", "raw") VALUES (:id, :author_id, :editor_id, :reviewed_at, :meta, :raw) RETURNING "id", "author_id", "editor_id", "reviewed_at", "meta", "raw"`

// PostCreateParams are the caller-supplied columns of PostCreate. The
// auto-increment key and defaulted columns are the database's job (D16).
type PostCreateParams struct {
	ID       int64 `db:"id" json:"id"`
	AuthorID int64 `db:"author_id" json:"author_id"`
	// NULL until an editor is assigned
	EditorID   rt.Null[int64]     `db:"editor_id" json:"editor_id"`
	ReviewedAt rt.Null[time.Time] `db:"reviewed_at" json:"reviewed_at"`
	// nil slice expresses NULL; no pointer needed
	Meta json.RawMessage `db:"meta" json:"meta"`
	Raw  []byte          `db:"raw" json:"raw"`
}

// PostCreate inserts a posts row and returns the row the database wrote,
// generated keys and defaults filled in.
func (q *Queries) PostCreate(ctx context.Context, arg PostCreateParams) (Post, error) {
	return postScan(q.db.QueryRowContext(ctx, postCreateSQL,
		sql.Named("id", arg.ID),
		sql.Named("author_id", arg.AuthorID),
		sql.Named("editor_id", arg.EditorID),
		sql.Named("reviewed_at", arg.ReviewedAt),
		sql.Named("meta", arg.Meta),
		sql.Named("raw", arg.Raw),
	))
}

const postUpdateSQL = `UPDATE "posts" SET "author_id" = :author_id, "editor_id" = :editor_id, "reviewed_at" = :reviewed_at, "meta" = :meta, "raw" = :raw WHERE "id" = :id RETURNING "id", "author_id", "editor_id", "reviewed_at", "meta", "raw"`

// PostUpdateParams are the data columns of PostUpdate: every column
// outside the primary key.
type PostUpdateParams struct {
	AuthorID int64 `db:"author_id" json:"author_id"`
	// NULL until an editor is assigned
	EditorID   rt.Null[int64]     `db:"editor_id" json:"editor_id"`
	ReviewedAt rt.Null[time.Time] `db:"reviewed_at" json:"reviewed_at"`
	// nil slice expresses NULL; no pointer needed
	Meta json.RawMessage `db:"meta" json:"meta"`
	Raw  []byte          `db:"raw" json:"raw"`
}

// PostUpdate rewrites every non-key column of the identified posts row
// and returns the result, or rt.ErrNotFound when no row matches.
func (q *Queries) PostUpdate(ctx context.Context, id int64, arg PostUpdateParams) (Post, error) {
	return postScan(q.db.QueryRowContext(ctx, postUpdateSQL,
		sql.Named("id", id),
		sql.Named("author_id", arg.AuthorID),
		sql.Named("editor_id", arg.EditorID),
		sql.Named("reviewed_at", arg.ReviewedAt),
		sql.Named("meta", arg.Meta),
		sql.Named("raw", arg.Raw),
	))
}

const postDeleteSQL = `DELETE FROM "posts" WHERE "id" = :id`

// PostDelete removes the identified posts row; rt.ErrNotFound reports
// that nothing matched.
func (q *Queries) PostDelete(ctx context.Context, id int64) error {
	res, err := q.db.ExecContext(ctx, postDeleteSQL, sql.Named("id", id))
	if err != nil {
		return err
	}
	n, err := res.RowsAffected()
	if err != nil {
		return err
	}
	if n == 0 {
		return rt.ErrNotFound
	}
	return nil
}
