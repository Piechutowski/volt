// Code generated by nao gen from partials.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/nao/rt"
	"time"
)

// invoiceTableSQL and invoiceColumnsSQL are the fixed shape of Invoice's
// dynamic statements; the interpreter renders everything else (D28).
const (
	invoiceTableSQL   = `"invoices"`
	invoiceColumnsSQL = `"id", "total", "created_at", "updated_at", "deleted_at"`
)

// Typed column handles of Invoice (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Invoice queries; mixing models is a compile
// error.
var (
	InvoiceID        = rt.Column[Invoice, int64]{Name: "id"}
	InvoiceTotal     = rt.Column[Invoice, string]{Name: "total"}
	InvoiceCreatedAt = rt.Column[Invoice, time.Time]{Name: "created_at"}
	InvoiceUpdatedAt = rt.NullColumn[Invoice, time.Time]{Column: rt.Column[Invoice, time.Time]{Name: "updated_at"}}
	InvoiceDeletedAt = rt.NullColumn[Invoice, time.Time]{Column: rt.Column[Invoice, time.Time]{Name: "deleted_at"}}
)

// InvoiceLimit caps how many rows InvoiceQuery returns (D30).
func InvoiceLimit(n int) rt.Opt[Invoice] { return rt.Limit[Invoice](n) }

// InvoiceOffset skips n rows; keyset pagination (InvoiceAfter) scales better (D34).
func InvoiceOffset(n int) rt.Opt[Invoice] { return rt.Offset[Invoice](n) }

// InvoiceDistinct deduplicates the rows InvoiceQuery returns.
func InvoiceDistinct() rt.Opt[Invoice] { return rt.Distinct[Invoice]() }

// InvoiceOrderBy sorts InvoiceQuery's rows by Asc/Desc terms built on the
// Invoice column handles.
func InvoiceOrderBy(terms ...rt.Order[Invoice]) rt.Opt[Invoice] { return rt.OrderBy(terms...) }

// InvoiceAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per InvoiceOrderBy term, in the same order.
func InvoiceAfter(key ...any) rt.Opt[Invoice] { return rt.After[Invoice](key...) }

// InvoiceSet collects the typed assignments of a InvoiceUpdateWhere, built
// with Set/SetNull on the Invoice column handles.
func InvoiceSet(assigns ...rt.Assign[Invoice]) []rt.Assign[Invoice] { return assigns }

// InvoiceQuery returns the invoices rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) InvoiceQuery(ctx context.Context, opts ...rt.Opt[Invoice]) ([]Invoice, error) {
	query, args, err := rt.SelectRender(invoiceTableSQL, invoiceColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	out := []Invoice{}
	for rows.Next() {
		v, err := invoiceScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// InvoiceCount reports how many invoices rows match every given predicate;
// none counts the whole table.
func (q *Queries) InvoiceCount(ctx context.Context, preds ...rt.Pred[Invoice]) (int64, error) {
	query, args, err := rt.CountRender(invoiceTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// InvoiceExists reports whether any invoices row matches every given predicate.
func (q *Queries) InvoiceExists(ctx context.Context, preds ...rt.Pred[Invoice]) (bool, error) {
	query, args, err := rt.ExistsRender(invoiceTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// InvoiceDeleteWhere removes every invoices row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) InvoiceDeleteWhere(ctx context.Context, preds ...rt.Pred[Invoice]) (int64, error) {
	query, args, err := rt.DeleteRender(invoiceTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// InvoiceUpdateWhere applies the typed assignments (InvoiceSet) to every invoices
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) InvoiceUpdateWhere(ctx context.Context, set []rt.Assign[Invoice], preds ...rt.Pred[Invoice]) (int64, error) {
	query, args, err := rt.UpdateRender(invoiceTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
