// Code generated by nao gen from polymorphic.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/nao/rt"
)

// commentTableSQL and commentColumnsSQL are the fixed shape of Comment's
// dynamic statements; the interpreter renders everything else (D28).
const (
	commentTableSQL   = `"comments"`
	commentColumnsSQL = `"id", "commentable_type", "commentable_id", "body"`
)

// Typed column handles of Comment (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Comment queries; mixing models is a compile
// error.
var (
	CommentID              = rt.Column[Comment, int64]{Name: "id"}
	CommentCommentableType = rt.Column[Comment, ECommentableType]{Name: "commentable_type"}
	CommentCommentableID   = rt.Column[Comment, int64]{Name: "commentable_id"}
	CommentBody            = rt.Column[Comment, string]{Name: "body"}
)

// CommentLimit caps how many rows CommentQuery returns (D30).
func CommentLimit(n int) rt.Opt[Comment] { return rt.Limit[Comment](n) }

// CommentOffset skips n rows; keyset pagination (CommentAfter) scales better (D34).
func CommentOffset(n int) rt.Opt[Comment] { return rt.Offset[Comment](n) }

// CommentDistinct deduplicates the rows CommentQuery returns.
func CommentDistinct() rt.Opt[Comment] { return rt.Distinct[Comment]() }

// CommentOrderBy sorts CommentQuery's rows by Asc/Desc terms built on the
// Comment column handles.
func CommentOrderBy(terms ...rt.Order[Comment]) rt.Opt[Comment] { return rt.OrderBy(terms...) }

// CommentAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per CommentOrderBy term, in the same order.
func CommentAfter(key ...any) rt.Opt[Comment] { return rt.After[Comment](key...) }

// CommentSet collects the typed assignments of a CommentUpdateWhere, built
// with Set/SetNull on the Comment column handles.
func CommentSet(assigns ...rt.Assign[Comment]) []rt.Assign[Comment] { return assigns }

// CommentQuery returns the comments rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) CommentQuery(ctx context.Context, opts ...rt.Opt[Comment]) ([]Comment, error) {
	query, args, err := rt.SelectRender(commentTableSQL, commentColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Comment
	for rows.Next() {
		v, err := commentScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// CommentCount reports how many comments rows match every given predicate;
// none counts the whole table.
func (q *Queries) CommentCount(ctx context.Context, preds ...rt.Pred[Comment]) (int64, error) {
	query, args, err := rt.CountRender(commentTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// CommentExists reports whether any comments row matches every given predicate.
func (q *Queries) CommentExists(ctx context.Context, preds ...rt.Pred[Comment]) (bool, error) {
	query, args, err := rt.ExistsRender(commentTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// CommentDeleteWhere removes every comments row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) CommentDeleteWhere(ctx context.Context, preds ...rt.Pred[Comment]) (int64, error) {
	query, args, err := rt.DeleteRender(commentTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// CommentUpdateWhere applies the typed assignments (CommentSet) to every comments
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) CommentUpdateWhere(ctx context.Context, set []rt.Assign[Comment], preds ...rt.Pred[Comment]) (int64, error) {
	query, args, err := rt.UpdateRender(commentTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
