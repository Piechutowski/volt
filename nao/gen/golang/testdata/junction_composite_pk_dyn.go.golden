// Code generated by nao gen from junction_composite_pk.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/nao/rt"
	"time"
)

// userTableSQL and userColumnsSQL are the fixed shape of User's
// dynamic statements; the interpreter renders everything else (D28).
const (
	userTableSQL   = `"users"`
	userColumnsSQL = `"id"`
)

// Typed column handles of User (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter User queries; mixing models is a compile
// error.
var (
	UserID = rt.Column[User, int64]{Name: "id"}
)

// UserLimit caps how many rows UserQuery returns (D30).
func UserLimit(n int) rt.Opt[User] { return rt.Limit[User](n) }

// UserOffset skips n rows; keyset pagination (UserAfter) scales better (D34).
func UserOffset(n int) rt.Opt[User] { return rt.Offset[User](n) }

// UserDistinct deduplicates the rows UserQuery returns.
func UserDistinct() rt.Opt[User] { return rt.Distinct[User]() }

// UserOrderBy sorts UserQuery's rows by Asc/Desc terms built on the
// User column handles.
func UserOrderBy(terms ...rt.Order[User]) rt.Opt[User] { return rt.OrderBy(terms...) }

// UserAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per UserOrderBy term, in the same order.
func UserAfter(key ...any) rt.Opt[User] { return rt.After[User](key...) }

// UserSet collects the typed assignments of a UserUpdateWhere, built
// with Set/SetNull on the User column handles.
func UserSet(assigns ...rt.Assign[User]) []rt.Assign[User] { return assigns }

// UserQuery returns the users rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) UserQuery(ctx context.Context, opts ...rt.Opt[User]) ([]User, error) {
	query, args, err := rt.SelectRender(userTableSQL, userColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []User
	for rows.Next() {
		v, err := userScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// UserCount reports how many users rows match every given predicate;
// none counts the whole table.
func (q *Queries) UserCount(ctx context.Context, preds ...rt.Pred[User]) (int64, error) {
	query, args, err := rt.CountRender(userTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// UserExists reports whether any users row matches every given predicate.
func (q *Queries) UserExists(ctx context.Context, preds ...rt.Pred[User]) (bool, error) {
	query, args, err := rt.ExistsRender(userTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// UserDeleteWhere removes every users row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) UserDeleteWhere(ctx context.Context, preds ...rt.Pred[User]) (int64, error) {
	query, args, err := rt.DeleteRender(userTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// UserUpdateWhere applies the typed assignments (UserSet) to every users
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) UserUpdateWhere(ctx context.Context, set []rt.Assign[User], preds ...rt.Pred[User]) (int64, error) {
	query, args, err := rt.UpdateRender(userTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// roleTableSQL and roleColumnsSQL are the fixed shape of Role's
// dynamic statements; the interpreter renders everything else (D28).
const (
	roleTableSQL   = `"roles"`
	roleColumnsSQL = `"id", "name"`
)

// Typed column handles of Role (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Role queries; mixing models is a compile
// error.
var (
	RoleID   = rt.Column[Role, int32]{Name: "id"}
	RoleName = rt.Column[Role, string]{Name: "name"}
)

// RoleLimit caps how many rows RoleQuery returns (D30).
func RoleLimit(n int) rt.Opt[Role] { return rt.Limit[Role](n) }

// RoleOffset skips n rows; keyset pagination (RoleAfter) scales better (D34).
func RoleOffset(n int) rt.Opt[Role] { return rt.Offset[Role](n) }

// RoleDistinct deduplicates the rows RoleQuery returns.
func RoleDistinct() rt.Opt[Role] { return rt.Distinct[Role]() }

// RoleOrderBy sorts RoleQuery's rows by Asc/Desc terms built on the
// Role column handles.
func RoleOrderBy(terms ...rt.Order[Role]) rt.Opt[Role] { return rt.OrderBy(terms...) }

// RoleAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per RoleOrderBy term, in the same order.
func RoleAfter(key ...any) rt.Opt[Role] { return rt.After[Role](key...) }

// RoleSet collects the typed assignments of a RoleUpdateWhere, built
// with Set/SetNull on the Role column handles.
func RoleSet(assigns ...rt.Assign[Role]) []rt.Assign[Role] { return assigns }

// RoleQuery returns the roles rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) RoleQuery(ctx context.Context, opts ...rt.Opt[Role]) ([]Role, error) {
	query, args, err := rt.SelectRender(roleTableSQL, roleColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Role
	for rows.Next() {
		v, err := roleScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// RoleCount reports how many roles rows match every given predicate;
// none counts the whole table.
func (q *Queries) RoleCount(ctx context.Context, preds ...rt.Pred[Role]) (int64, error) {
	query, args, err := rt.CountRender(roleTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// RoleExists reports whether any roles row matches every given predicate.
func (q *Queries) RoleExists(ctx context.Context, preds ...rt.Pred[Role]) (bool, error) {
	query, args, err := rt.ExistsRender(roleTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// RoleDeleteWhere removes every roles row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) RoleDeleteWhere(ctx context.Context, preds ...rt.Pred[Role]) (int64, error) {
	query, args, err := rt.DeleteRender(roleTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// RoleUpdateWhere applies the typed assignments (RoleSet) to every roles
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) RoleUpdateWhere(ctx context.Context, set []rt.Assign[Role], preds ...rt.Pred[Role]) (int64, error) {
	query, args, err := rt.UpdateRender(roleTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// userRoleTableSQL and userRoleColumnsSQL are the fixed shape of UserRole's
// dynamic statements; the interpreter renders everything else (D28).
const (
	userRoleTableSQL   = `"user_roles"`
	userRoleColumnsSQL = `"user_id", "role_id", "granted_at"`
)

// Typed column handles of UserRole (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter UserRole queries; mixing models is a compile
// error.
var (
	UserRoleUserID    = rt.Column[UserRole, int64]{Name: "user_id"}
	UserRoleRoleID    = rt.Column[UserRole, int32]{Name: "role_id"}
	UserRoleGrantedAt = rt.Column[UserRole, time.Time]{Name: "granted_at"}
)

// UserRoleLimit caps how many rows UserRoleQuery returns (D30).
func UserRoleLimit(n int) rt.Opt[UserRole] { return rt.Limit[UserRole](n) }

// UserRoleOffset skips n rows; keyset pagination (UserRoleAfter) scales better (D34).
func UserRoleOffset(n int) rt.Opt[UserRole] { return rt.Offset[UserRole](n) }

// UserRoleDistinct deduplicates the rows UserRoleQuery returns.
func UserRoleDistinct() rt.Opt[UserRole] { return rt.Distinct[UserRole]() }

// UserRoleOrderBy sorts UserRoleQuery's rows by Asc/Desc terms built on the
// UserRole column handles.
func UserRoleOrderBy(terms ...rt.Order[UserRole]) rt.Opt[UserRole] { return rt.OrderBy(terms...) }

// UserRoleAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per UserRoleOrderBy term, in the same order.
func UserRoleAfter(key ...any) rt.Opt[UserRole] { return rt.After[UserRole](key...) }

// UserRoleSet collects the typed assignments of a UserRoleUpdateWhere, built
// with Set/SetNull on the UserRole column handles.
func UserRoleSet(assigns ...rt.Assign[UserRole]) []rt.Assign[UserRole] { return assigns }

// UserRoleQuery returns the user_roles rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) UserRoleQuery(ctx context.Context, opts ...rt.Opt[UserRole]) ([]UserRole, error) {
	query, args, err := rt.SelectRender(userRoleTableSQL, userRoleColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []UserRole
	for rows.Next() {
		v, err := userRoleScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// UserRoleCount reports how many user_roles rows match every given predicate;
// none counts the whole table.
func (q *Queries) UserRoleCount(ctx context.Context, preds ...rt.Pred[UserRole]) (int64, error) {
	query, args, err := rt.CountRender(userRoleTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// UserRoleExists reports whether any user_roles row matches every given predicate.
func (q *Queries) UserRoleExists(ctx context.Context, preds ...rt.Pred[UserRole]) (bool, error) {
	query, args, err := rt.ExistsRender(userRoleTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// UserRoleDeleteWhere removes every user_roles row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) UserRoleDeleteWhere(ctx context.Context, preds ...rt.Pred[UserRole]) (int64, error) {
	query, args, err := rt.DeleteRender(userRoleTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// UserRoleUpdateWhere applies the typed assignments (UserRoleSet) to every user_roles
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) UserRoleUpdateWhere(ctx context.Context, set []rt.Assign[UserRole], preds ...rt.Pred[UserRole]) (int64, error) {
	query, args, err := rt.UpdateRender(userRoleTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
