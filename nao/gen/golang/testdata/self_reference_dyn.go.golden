// Code generated by nao gen from self_reference.dbml; DO NOT EDIT.
//
// The dynamic query layer over the models in nao_models.go: typed
// column handles, per-model option wrappers, and predicate-driven
// terminals — see docs/decisions.md D28-D34.

package models

import (
	"context"
	"github.com/Piechutowski/volt/nao/rt"
)

// employeeTableSQL and employeeColumnsSQL are the fixed shape of Employee's
// dynamic statements; the interpreter renders everything else (D28).
const (
	employeeTableSQL   = `"employees"`
	employeeColumnsSQL = `"id", "full_name", "manager_id"`
)

// Typed column handles of Employee (D29): inert predicate, order and
// assignment builders for the dynamic query layer (D28). A predicate
// built here can only enter Employee queries; mixing models is a compile
// error.
var (
	EmployeeID        = rt.Column[Employee, int64]{Name: "id"}
	EmployeeFullName  = rt.Column[Employee, string]{Name: "full_name"}
	EmployeeManagerID = rt.NullColumn[Employee, int64]{Column: rt.Column[Employee, int64]{Name: "manager_id"}}
)

// EmployeeLimit caps how many rows EmployeeQuery returns (D30).
func EmployeeLimit(n int) rt.Opt[Employee] { return rt.Limit[Employee](n) }

// EmployeeOffset skips n rows; keyset pagination (EmployeeAfter) scales better (D34).
func EmployeeOffset(n int) rt.Opt[Employee] { return rt.Offset[Employee](n) }

// EmployeeDistinct deduplicates the rows EmployeeQuery returns.
func EmployeeDistinct() rt.Opt[Employee] { return rt.Distinct[Employee]() }

// EmployeeOrderBy sorts EmployeeQuery's rows by Asc/Desc terms built on the
// Employee column handles.
func EmployeeOrderBy(terms ...rt.Order[Employee]) rt.Opt[Employee] { return rt.OrderBy(terms...) }

// EmployeeAfter resumes strictly after the row with the given key — keyset
// pagination (D34): one value per EmployeeOrderBy term, in the same order.
func EmployeeAfter(key ...any) rt.Opt[Employee] { return rt.After[Employee](key...) }

// EmployeeSet collects the typed assignments of a EmployeeUpdateWhere, built
// with Set/SetNull on the Employee column handles.
func EmployeeSet(assigns ...rt.Assign[Employee]) []rt.Assign[Employee] { return assigns }

// EmployeeQuery returns the employees rows matching every given predicate,
// shaped by the remaining options (order, limit, offset, keyset,
// distinct). No options means every row.
func (q *Queries) EmployeeQuery(ctx context.Context, opts ...rt.Opt[Employee]) ([]Employee, error) {
	query, args, err := rt.SelectRender(employeeTableSQL, employeeColumnsSQL, opts)
	if err != nil {
		return nil, err
	}
	rows, err := rt.StmtQuery(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Employee
	for rows.Next() {
		v, err := employeeScan(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, v)
	}
	return out, rows.Err()
}

// EmployeeCount reports how many employees rows match every given predicate;
// none counts the whole table.
func (q *Queries) EmployeeCount(ctx context.Context, preds ...rt.Pred[Employee]) (int64, error) {
	query, args, err := rt.CountRender(employeeTableSQL, preds)
	if err != nil {
		return 0, err
	}
	var n int64
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &n); err != nil {
		return 0, err
	}
	return n, nil
}

// EmployeeExists reports whether any employees row matches every given predicate.
func (q *Queries) EmployeeExists(ctx context.Context, preds ...rt.Pred[Employee]) (bool, error) {
	query, args, err := rt.ExistsRender(employeeTableSQL, preds)
	if err != nil {
		return false, err
	}
	var found bool
	if err := rt.RowScan(ctx, q.db, q.cache, query, args, &found); err != nil {
		return false, err
	}
	return found, nil
}

// EmployeeDeleteWhere removes every employees row matching the predicates and
// reports how many went. No effective predicate is an error, never a
// full-table delete (D42).
func (q *Queries) EmployeeDeleteWhere(ctx context.Context, preds ...rt.Pred[Employee]) (int64, error) {
	query, args, err := rt.DeleteRender(employeeTableSQL, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}

// EmployeeUpdateWhere applies the typed assignments (EmployeeSet) to every employees
// row matching the predicates and reports how many changed — the
// partial update of the dynamic layer (D32). No effective predicate
// is an error, never a full-table rewrite (D42).
func (q *Queries) EmployeeUpdateWhere(ctx context.Context, set []rt.Assign[Employee], preds ...rt.Pred[Employee]) (int64, error) {
	query, args, err := rt.UpdateRender(employeeTableSQL, set, preds)
	if err != nil {
		return 0, err
	}
	res, err := rt.StmtExec(ctx, q.db, q.cache, query, args...)
	if err != nil {
		return 0, err
	}
	return res.RowsAffected()
}
