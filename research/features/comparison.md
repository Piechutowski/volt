# Cross-framework feature comparison — Go std · Rails · Laravel · Django · Phoenix

This document is the synthesis layer of the Volt framework research. Volt is a planned Go web framework; before designing it, we inventoried four mature full-stack frameworks — **Laravel**, **Ruby on Rails**, **Phoenix**, and **Django** — against a shared skeleton of 21 problem areas (P1–P21, from routing through admin UIs), then inventoried the **Go standard library** against the same skeleton as the baseline Volt starts from. This file compares the answers problem by problem — every matrix now leads with the Go std column, so each row reads as "what Go already gives Volt" against "what the four frameworks add" — then closes with a synthesis: what Go already covers, the table-stakes baseline, each framework's differentiators, the gaps none of them fill, and the design tensions Volt must resolve.

**How to read it.** Each `## PN` section carries a one-line portrait of each stack's approach, a capability matrix, the notable divergences, and Volt-specific notes. Matrix cells follow the format *mechanism · TIER · feature ID(s)*. The first column is the **Go std** baseline — the standard library, the `go` toolchain, and `golang.org/x` before any framework exists — with its own tier scale: **STD** (importable stdlib API under the Go 1 compatibility promise), **TOOL** (language or toolchain feature: goroutines, struct tags, `//go:embed`, `go test -race`), **X** (`golang.org/x/*`, Go-team-maintained but outside the promise), and **NO** (absent from std, x/ and the toolchain — candidate Volt surface). Framework tiers are as recorded in the inventories: **CORE** (in the framework/default app), **OPT** (first-party or contrib, opt-in), **ECO** (blessed ecosystem), **DIY** (documented pattern you build yourself); `—` means that corpus has no answer. Feature IDs (ROUTE-9, ORM-25, AUTH-2, …) refer into the per-stack inventory documents — in the Go std column they refer into [gostd.md](./gostd.md) — which hold the full detail:

- [gostd.md](./gostd.md)
- [laravel.md](./laravel.md)
- [rails.md](./rails.md)
- [phoenix.md](./phoenix.md)
- [django.md](./django.md)

**Corpus provenance.** go1.26.5 (`go doc -all` dumps of the installed toolchain, fetched 2026-07-14), Laravel 13.x, Rails 8.1.3, Phoenix 1.8.9 (+ Ecto 3.14.1, LiveView 1.2.7, Plug 1.20.3), Django 6.1.x — all from official documentation sources; exact commits are recorded in `../corpus/*/MANIFEST`. Framework-specific extra sections beyond P21 (Phoenix's BEAM runtime, Django's GeoDjango/PostgreSQL, Laravel's search/AI/billing) are folded into the nearest section or the closing synthesis where relevant.

## The five philosophies at a glance

| Framework | One-line philosophy | Runtime model | Data-layer stance | Frontend stance | Batteries stance |
|---|---|---|---|---|---|
| **Go std** | Batteries for protocols, not products: if an RFC defines it, std implements it; the moment a product decision is required, std abstains | Goroutine per request; concurrency is a language feature, not a framework feature | `database/sql` only — pool, transactions, type bridge; nothing above the raw SQL string | `html/template` only — contextual auto-escaping, no components, no asset pipeline | Primitives: small implicitly-satisfied interfaces as seams, wire-level codecs, and a toolchain that ships the deploy story |
| **Rails** | Convention over configuration; omakase full stack — the database is the infrastructure | Threaded Rack behind the GVL; Executor-wrapped units of work; documented tuning playbook | Archetypal fat Active Record, schema-reflected, callbacks as architecture | HTML over the wire: Hotwire (Turbo + Stimulus) + importmaps, no Node build | Complete default meal including deployment (Solid trifecta, Kamal); every course can be sent back |
| **Laravel** | A fluent DSL and a config-selected driver for every problem; convention density with escape hatches | PHP process-per-request; Octane resident workers as the opt-in escape | Maximal Active Record (Eloquent) plus a fluent SQL builder | Blade components + first-party Vite plugin; interactivity delegated to Livewire/Inertia | Maximal: first-party packages for the whole second ring (websockets, OAuth2, billing, search, AI), shading into paid products |
| **Django** | Loose coupling + DRY: the model is the single source of truth; explicit over implicit, "no magic" | WSGI/ASGI dual stack; sync and async views both first-class | Declarative Active Record with lazy QuerySets; database-agnostic by charter | Deliberately restricted templates; staticfiles collect/hash only; JS toolchains out of scope | Encyclopedic contrib (admin, auth, GIS, i18n) but abstains from REST, real-time, and workers |
| **Phoenix** | Explicit pipelines of one immutable value; compile time is a feature; the BEAM replaces infrastructure | BEAM process per connection; supervision trees; native clustering | Ecto data mapper: Repo/Schema/Changeset, no lazy loading, database as the integrity layer | Compile-checked HEEx function components; LiveView server-rendered reactive UI | Deliberately thin: no jobs, cache, or storage in core; generators write owned code into your app |

**Go std** is not a framework but the floor the comparison now stands on. Its consistent line: complete at the wire level — a production HTTP/1.1+HTTP/2 server, TLS 1.3, SMTP, MIME, JSON, cookies, multipart — and deliberately empty one level up: an SMTP client but no mailer, a JSON codec but no serializer, cookies but no sessions, SQL but no ORM. Concurrency is the language (goroutine-per-request, `context` cancellation), deployment is the toolchain (one static binary with `//go:embed`-ed assets), and every extension seam is a one-to-four-method interface satisfied implicitly. Reading its column: STD/TOOL/X cells are what Volt inherits for free; NO cells mark where the standard library stops and Volt's potential surface begins.

**Rails** bets that naming conventions can replace wiring: `Book` → `books` → `BooksController` → `app/views/books/`, schema reflected rather than declared. The model object is deliberately fat — validations, callbacks, dirty tracking, encryption live on it — and controllers stay thin. Rails 8's sharpest position is infrastructural: the Solid trifecta (Queue/Cache/Cable) runs jobs, caching, and websockets on the application's own database, removing Redis from the default stack, while a generated Dockerfile, Kamal, and Thruster claim the deploy path all the way to a Linux box. Auth flipped in 8.0 from engine-magic to generated, owned code atop small primitives. What Rails refuses: authorization frameworks, DI containers, API serializers/versioning, presence, and metrics export — instrumentation events are emitted for others to consume.

**Laravel** optimizes for a single coherent product experience. One pattern — a config-selected named driver behind a uniform fluent API, with an `extend()` hook — repeats across cache, queue, mail, storage, auth, broadcasting, and logging, and Laravel 13 extends it with failover drivers for availability. The service container is the universal seam, facades the ergonomic front door, and every I/O subsystem ships a `::fake()` for testing. Where the other three stop at the framework's edge, Laravel keeps shipping first-party packages outward — Reverb, Horizon, Passport, Cashier, Scout, Octane, now an AI/MCP pillar — trading ecosystem pluralism for a docs corpus that reads as one product. Its acknowledged weak flanks: thin i18n, no metrics story, request-centric validation that treats the database as secondary, and a process-per-request runtime it must sell Octane to escape.

**Django** is the oldest philosophy and the most explicitly argued: loose coupling (templates know nothing of requests, the ORM nothing of display), DRY (the model declaration is the single source from which forms, admin, migrations, and serialization derive), and "no magic" (`save()` never validates, fields never infer from names). Its two flagship consequences are the model admin — a production CRUD UI derived entirely from model metadata, unmatched by any peer — and derived migrations (`makemigrations` diffs model state). Contrib depth is encyclopedic (auth, gettext-based i18n with JS catalogs, a full timezone framework, GeoDjango) and API stability is a published promise. But the charter is equally explicit about refusals: database-agnostic core, no NoSQL, no websockets in core, a tasks framework that deliberately ships no worker, no REST framework, and total frontend-toolchain agnosticism.

**Phoenix** makes explicitness and the compiler its product. Everything HTTP is a plug over one immutable conn — reading the endpoint and router *is* reading the request path — and the same pipeline shape repeats in Ecto changesets and LiveView sockets. Routes, `~p` URLs, queries, and HEEx templates are all verified at compile time; unsafe SQL interpolation is a compile *error*. Generators scaffold whole vertical slices into your application and declare the output yours, with v1.8 Scopes threading an authorization context through everything future generators emit. The BEAM replaces a broker fleet: PubSub, CRDT presence, clustering, and supervision ship with zero external services, and LiveView makes server-rendered reactive UI the default paradigm. The trade: Phoenix deliberately ships no job queue, no cache framework, no storage abstraction, no admin — and generated code receives no automatic security patches.

---

## P1 — Routing & HTTP dispatch

- **Go std** — `http.ServeMux` (1.22+): `[METHOD ][HOST]/[PATH]` patterns with `{name}`/`{name...}`/`{$}` wildcards, specificity-based precedence with registration-time conflict panics, automatic 404/405; params are untyped strings, and named routes, reverse URL generation, groups, and introspection are all absent.
- **Rails** — Ruby DSL in `config/routes.rb` centered on RESTful `resources` minting 7 CRUD routes + `_path`/`_url` helpers; everything else layers onto that convention.
- **Laravel** — fluent `Route` facade in convention route files (`routes/web.php`/`api.php`); implicit model binding is the signature convenience; throttling, CORS, signing hang directly off the router.
- **Django** — URLconf: plain Python `urlpatterns` lists matched top-down on path only (never the verb); typed converters, `include()` nesting with namespaces, `reverse()` for generation.
- **Phoenix** — compile-time macro router expanding to one pattern-matched function; verified `~p` path literals fail at build time; pipelines attach plug stacks to scopes.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Verb+path route declaration | `"GET /path"` ServeMux method patterns (GET also matches HEAD) · STD · ROUTE-1/2 | `get/post/…` + `match … via:` · CORE · ROUTE-3 | `Route::get/post/…`, `match`/`any` · CORE · ROUTE-1 | `path()`/`re_path()`; **verb deliberately ignored** by router (dispatch in view) · CORE · ROUTE-1/3/14 | verb macros compiled to one pattern-match · CORE · ROUTE-1 |
| RESTful resource routing | — | `resources` DSL + singular `resource`, member/collection, deep customization (`only/as/param/path_names`) · CORE · ROUTE-1/4/6/18 | `Route::resource/apiResource` 7/5 actions, only/except · CORE · CTRL-4 (P2) | — (per-URL CBVs instead) | `resources` macro 8 actions, `:only/:except` · CORE · ROUTE-2/3 |
| Nested / shallow resources | — | ≤1-level nesting convention, `shallow:` · CORE · ROUTE-5 | nested + `shallow()`, scoped child bindings · CORE · CTRL-5 (P2) | — (prefix nesting via `include()` only · ROUTE-9/10) | nested `resources do … end` · CORE · ROUTE-4 |
| Route parameters | `{name}` wildcards → `PathValue` (string only) · STD · ROUTE-3/6 | `:id` segments, optional `(.:format)` · CORE · ROUTE-9 | `{p}`, optional `{p?}`, injected by name · CORE · ROUTE-4 | `<conv:name>` captures → view kwargs · CORE · ROUTE-3/4 | `:param` → string-keyed params · CORE · ROUTE-12 |
| Param constraints / typed converters | — (strconv/regexp by hand · ROUTE-7) | segment regex + verb constraints · CORE · ROUTE-11 | `where` regex + `whereNumber/Uuid/In…`, global `Route::pattern` · CORE · ROUTE-5 | typed converters `str/int/slug/uuid/path` returning typed values; custom `register_converter` · CORE/DIY · ROUTE-5/6 | — (pattern-match in action heads instead) |
| Wildcard / regex catch-all | `{name...}` rest-of-path + bare-`/` subtree; no regex · STD · ROUTE-4/8 | `*path` glob · CORE · ROUTE-13 | only via Folio `[...ids]` · OPT · ROUTE-22 | `path` converter (crosses `/`); full regex incl. unnamed/nested groups (discouraged) · CORE/OPT · ROUTE-5/4/7/8 | — |
| Grouping, prefixing & namespacing | — (hand-rolled `StripPrefix` sub-muxes · ROUTE-15/16) | `namespace`/`scope` decoupling URL, module, helper name; parametric scopes · CORE · ROUTE-7/19 | route groups sharing middleware/prefix/name/controller · CORE · ROUTE-7 | `include()` nesting + app/instance namespaces (`'ns:name'`) · CORE · ROUTE-9/16 | `scope` path + module namespace, arbitrarily nested · CORE · ROUTE-5 |
| Reusable route fragments | — | routing `concern`s · CORE · ROUTE-8 | — (groups suffice) | `include(pattern_list)` · CORE · ROUTE-9 | pipelines compose (pipelines plug pipelines) · CORE · ROUTE-8 |
| Route file organization | — (plain code; one mux) | one `routes.rb` + `draw(:admin)` split files · CORE · ROUTE-21 | convention files web/api/console/channels; custom files & full override in `bootstrap/app.php` · CORE · ROUTE-2/19 | per-app URLconf modules composed by `include()` · CORE · ROUTE-1/9 | one compiled router module; `forward` for sub-routers · CORE · ROUTE-14 |
| Model binding from route params | — | — (fetch in action; models used generation-side only · ROUTE-16) | implicit Eloquent binding (custom key `{post:slug}`, scoped, soft-deletes, `missing()`), enum binding, explicit `Route::model/bind` · CORE · ROUTE-9/10/11 | — (view-side `get_object_or_404` · CTRL-50 (P2)) | — (generation-side only: `Phoenix.Param` · ROUTE-11) |
| Named routes & URL generation | — (string concat/`url.URL`; only `JoinPath` helps · ROUTE-17/22) | auto `_path`/`_url` helpers, `url_for(@model)`, polymorphic, `direct`/`resolve` remaps, `default_url_options` · CORE · ROUTE-2/16/17, CTRL-21 (P2) | `->name()` + `route()/url()/action()`, `URL::defaults`, fluent `Uri` · CORE · ROUTE-6/21 | `name=` + `reverse()/reverse_lazy()`, namespaced resolution order, `query=`/`fragment=`; `get_script_prefix` · CORE · ROUTE-15/16/17/18/21 | `~p` sigil + `url()`; structs interpolate via `Phoenix.Param` · CORE · ROUTE-9/10/11 |
| Compile/build-time route safety | registration-time conflict panic; specificity, not order · STD · ROUTE-10 | — (runtime; `--unused` detection · ROUTE-22) | — (runtime) | — (runtime `NoReverseMatch` · ROUTE-17) | `~p` warns on unknown paths; duplicate routes warn at compile · CORE · ROUTE-9/6 |
| Redirect & shorthand routes | `http.Redirect` + routable `RedirectHandler` · STD · ROUTE-24 | `redirect()` with `%{param}` interpolation, block form, status · CORE · ROUTE-14 | `Route::redirect/permanentRedirect`, `Route::view` · CORE · ROUTE-3 | `RedirectView`/`TemplateView` in URLconf · CORE · CTRL-92/93 (P2); DB-driven contrib.redirects fallback · OPT · ROUTE-24 | — |
| Subdomain / host routing | exact-host patterns (`example.com/`), no wildcard subdomains · STD · ROUTE-9 | request-based constraints (`subdomain:`), `matches?` objects · CORE · ROUTE-12 | `Route::domain('{account}.…')` with params · CORE · ROUTE-8 | — (per-request URLconf swap can emulate · OPT · ROUTE-2) | — |
| Mounting sub-apps / engines | any `http.Handler` mounts; `StripPrefix` sub-mux idiom · STD · ROUTE-15 | `mount`/`match` any Rack app · CORE · ROUTE-15 | — | `include()` third-party app URLconfs · CORE · ROUTE-9 | `forward` any plug; standalone `Plug.Router` micro-router · CORE · ROUTE-14/18 |
| Fallback / unmatched handling | synthesized 404/405 + reusable `NotFoundHandler` · STD · ROUTE-11 | — (error templates · CTRL-11 (P2)) | `Route::fallback` · CORE · ROUTE-12 | `handler400/403/404/500` hooks · DIY · ROUTE-22; contrib.redirects last-resort · OPT · ROUTE-24 | — (no clause → ErrorHTML/JSON · CTRL-19 (P2)) |
| Rate limiting / throttling | — (x/time/rate · CTRL-32 (P2)) | controller `rate_limit to:, within:` (cache-backed) · CORE · CTRL-15 (P2) | named `RateLimiter::for` + `throttle:` middleware, Redis variant · CORE · ROUTE-13 | — (not inventoried) | — (not inventoried; ecosystem) |
| CORS | — | — (rack-cors, not inventoried) | `HandleCors` + `config/cors.php`, auto preflight · CORE · ROUTE-16 | — (django-cors-headers, not inventoried) | — (cors_plug, not inventoried) |
| Form method spoofing / verb normalization | — (only GET→HEAD auto-match · ROUTE-2) | Rack::MethodOverride in default stack · CORE · CTRL-18 (P2, stack) | `_method` + `@method` · CORE · ROUTE-14 | — (no override mechanism) | `Plug.MethodOverride` + `Plug.Head` · CORE · CTRL-16 (P2) |
| Signed URLs | — | — (in P1; signing lives elsewhere) | `signedRoute`/`temporarySignedRoute` + `signed` middleware · CORE · ROUTE-20 | — (signing framework elsewhere; signed cookies · CTRL-16 (P2)) | — (Phoenix.Token elsewhere) |
| Route-table performance | — (moot: compiled matcher, no cache to build) | — | `route:cache` compiled table · CORE · ROUTE-17 | first-access compile + resolver cache · CORE · ROUTE-23 | compile-time pattern-match; VM-optimized tree in Plug.Router · CORE · ROUTE-1/18 |
| Route introspection | matched `Request.Pattern` only; no table enumeration · STD · ROUTE-19/18 | `bin/rails routes` (`-g`/`-c`/`--unused`); console helpers · CORE · ROUTE-22 | `route:list` CLI; current-route API · CORE · ROUTE-18/15 | programmatic `resolve()`/`ResolverMatch` (no CLI) · CORE · ROUTE-19/20 | `mix phx.routes`; router cheatsheet · CORE · ROUTE-13/19 |
| Routing test assertions | `ServeMux.Handler(r)` match-without-dispatch · STD · ROUTE-20 | `assert_generates/recognizes/routing` · CORE · ROUTE-23 | — | — (documented `@override_settings(ROOT_URLCONF=…)` pattern · ROUTE-22) | — |
| Localized route paths | — | `scope(path_names:)`, Unicode routes · CORE · ROUTE-20 | localized resource verbs · CORE · CTRL-5 (P2) | `gettext_lazy` translated routes · CORE · ROUTE-3 | — |
| Default/extra params to handlers | — | `defaults:`/`defaults do`, `root` · CORE · ROUTE-10 | — (generation-side `URL::defaults` · ROUTE-21) | extra-options dicts (per route or per `include`), Python default args · OPT · ROUTE-11/12/13 | — |
| API versioning | prefix sub-muxes by hand · STD · ROUTE-15 | plain `namespace` scopes · CORE · ROUTE-7 | opt-in `/api` prefix file · CORE · ROUTE-2 | plain `include()` prefixes · CORE · ROUTE-9 | plain nested scopes; explicitly no dedicated machinery · CORE · ROUTE-17 |
| File-based (page) routing | — | — | Folio: Blade files under `pages/` become routes, `[id]`/`[Model]` params · OPT · ROUTE-22 | — | — |
| Stateful / live routes | — | — | — | — | `live`/`live_session` router-mounted LiveViews with shared `on_mount` · CORE · ROUTE-15/16 |
| Health-check endpoint | — | `/up` `Rails::HealthController` in generated apps · CORE · ROUTE-24 | — | — | — |
| Per-request routing swap | — | — | — | middleware sets `request.urlconf` · OPT · ROUTE-2 | — |
| Format-suffix dispatch | — (no Accept negotiation anywhere · CTRL-34 (P2)) | `(.:format)` + `respond_to do \|format\|` · CORE · ROUTE-9/25 | — (Accept-header negotiation instead · CTRL-11 (P2)) | — (Accept-header negotiation · CTRL-18/19 (P2)) | — (`accepts` pipeline plug · ROUTE-7) |

**Notable divergences.** Django is the structural outlier: the router matches path only and never the HTTP verb (verb dispatch happens in the view), and it has no resource-routing concept at all, while Rails makes RESTful `resources` the organizing center and Laravel/Phoenix ship it as a first-class macro. Route-resolution cost is handled three different ways: Phoenix eliminates it at compile time (and uniquely verifies URL literals at build time), Laravel and Django paper over interpreted-language cost with route caches, Rails does neither. Laravel is alone in resolving domain objects inside the router (implicit model binding, enum binding, 404-on-miss); the other three treat that as controller/view business. Cross-cutting dispatch concerns also land in different layers: Laravel hangs rate limiting, CORS, and signed-URL validation directly off the router, Rails puts rate limiting in the controller and CORS/signing nowhere, Phoenix and Django route them to pipelines/middleware or the ecosystem. Finally, mountability differs by substrate: Rails and Phoenix can mount any Rack app/plug because they sit on an ecosystem-wide interface, Django composes at the URLconf level, Laravel has no mounting story.

**Volt notes.**
- Go's `http.ServeMux` (1.22+) already gives verb+`{param}` matching; three of four frameworks say verb-aware routing is table stakes, and Django's verb-agnostic design is the historical outlier, not a model.
- Phoenix's compile-time verified `~p` paths are the only static-safety story among the four and the most natural fit for Go: typed route constants or generated URL helpers could deliver what Laravel/Rails/Django only offer as runtime string lookup (`route('name')`, `reverse()`), which fights Go's type system.
- Route caching (Laravel, Django) is a scripting-language workaround Go gets for free — but every framework ships a route-introspection CLI (`route:list`, `bin/rails routes`, `mix phx.routes`), which a compiled router still needs to provide.
- Laravel's implicit model binding is the biggest DX gap between these frameworks and idiomatic Go; a Go equivalent needs generics/codegen and a defined DB-coupling boundary, since it welds the router to the ORM (Rails and Phoenix deliberately only couple in the URL-generation direction).
- ServeMux's specificity-based precedence with registration-time conflict panics (ROUTE-10) is a safety property none of the four frameworks offers — all four dispatch order-sensitively (top-down/first-match) — so Volt gets shadowing-proof routing for free as long as its routing sugar compiles down to real ServeMux patterns.

## P2 — Request handling: controllers & middleware

- **Go std** — `http.Handler` is the entire contract: controllers, middleware, routers, and proxies are all the same type, middleware is `func(next Handler) Handler` by convention; the server itself is production-grade (timeouts, graceful shutdown, panic isolation, 1.25 CSRF) but there is no middleware framework, no sessions, and no error-page layer.
- **Rails** — `ActionController::Base` subclasses, strong-parameters allowlist at the mass-assignment boundary, `before/after/around_action` callbacks, fully editable Rack stack underneath.
- **Laravel** — plain controller classes with container DI, a middleware onion configured centrally in `bootstrap/app.php`, rich `Request` object, fluent response/redirect builders.
- **Django** — views are plain callables (`HttpRequest → HttpResponse`); middleware onion via the `MIDDLEWARE` setting; optional generic class-based views layered on top; sync and async both first-class.
- **Phoenix** — Plug all the way down: one immutable `%Plug.Conn{}` flows through function/module plugs at endpoint, pipeline, and controller level; actions are plain 2-arity functions.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Handler unit | `Handler` interface / `HandlerFunc`; one type for everything · STD · CTRL-1 | `ActionController::Base` subclass, method per action, implicit params/session · CORE · CTRL-1 | plain classes (no base class), `__invoke` single-action · CORE · CTRL-1 | plain callables (FBV) + optional `View` class w/ verb-name dispatch · CORE · CTRL-42/90 | controllers are plugs; actions `fn(conn, params)`, params pattern-matched · CORE · CTRL-1/6 |
| Slim / API-only base variants | — (already minimal) | `ActionController::Metal`/`::API` à-la-carte modules · CORE · CTRL-23 | — (same classes for both) | — | — (already minimal) |
| DI into handlers | — (closures/struct fields by hand) | — | container resolves ctor + method type-hints alongside route params · CORE · CTRL-6 | — | — (explicit args) |
| Middleware contract | `func(next Handler) Handler` idiom, not an API · STD · CTRL-2 | Rack middleware · CORE · CTRL-17 | `handle($req, Closure $next)` classes · CORE · CTRL-7 | factory `(get_response) → callable`; optional `process_view/exception/template_response` hooks · DIY · CTRL-62/67/68/69 | function plugs + module plugs (`init/1`, `call/2`); everything is a plug · CORE · CTRL-1 |
| Stack configuration & ordering | — (source nesting order only · CTRL-4) | `config.middleware use/insert_before/after/swap/move/delete`; `bin/rails middleware` · CORE · CTRL-17 | central `bootstrap/app.php`: append/prepend/replace, aliases, group editing, explicit priority · CORE · CTRL-8 | ordered `MIDDLEWARE` setting; 15-point ordering doc; `MiddlewareNotUsed` startup opt-out · CORE/OPT · CTRL-63/76/66 | literal code order in endpoint + `pipeline`/`pipe_through` per scope · CORE · CTRL-3, ROUTE-7 (P1) |
| Middleware parameters | — (wrapper-function args · CTRL-2) | constructor args via `use` · CORE · CTRL-17 | `:`-delimited args (`throttle:60,1`) · CORE · CTRL-9 | — (`__init__` takes only `get_response` · CTRL-65) | `init/1` opts, compile-time · CORE · CTRL-1 |
| Short-circuiting | respond without calling `next` · STD · CTRL-2 | Rack contract (respond without calling app) · CORE · CTRL-17 | return response before `$next` (onion contract) · CORE · CTRL-7 | return without calling `get_response` · CORE · CTRL-64 | explicit `halt/1` · CORE · CTRL-2 |
| Per-controller/action hooks | — | `before/after/around_action` + `skip_*`/`prepend_*` · CORE · CTRL-8 | `HasMiddleware`/`#[Middleware]` attr w/ only/except; `#[Authorize]` attr · CORE · CTRL-2/3 | per-view decorators (`require_*`, `condition`, csrf…) + `method_decorator` for CBVs · CORE · CTRL-52–61/103 | controller plugs w/ `when action in […]` guards; plug chains + halt for auth · CORE · CTRL-7/8 |
| Post-response hooks | — | — (Executor lifecycle is the closest · CTRL-19) | terminable middleware `terminate()` (FastCGI) · CORE · CTRL-10 | — | — |
| Default middleware battery | 5 wrappers total (`TimeoutHandler`/`StripPrefix`/`MaxBytesHandler`/`AllowQuerySemicolons`/`CrossOriginProtection`); no curated stack · STD · CTRL-3 | ~20 documented middlewares (HostAuthorization, RequestId, RemoteIp, ETag, ConditionalGet…) · CORE · CTRL-18 | `web`/`api` groups; `TrimStrings`, `ConvertEmptyStringsToNull` · CORE · CTRL-8/16 | `SessionMiddleware`, `CommonMiddleware` + ordering guidance · CORE · CTRL-76/77/78 | endpoint defaults (Static, RequestId, Telemetry, Parsers, MethodOverride, Head, Session) + Plug batteries (BasicAuth, Logger, SSL, CSRF, Static) · CORE · CTRL-4/18 |
| Server-interface standard | `http.Handler` IS the ecosystem-wide standard · STD · CTRL-1 | Rack throughout; mountable Rack apps · CORE · CTRL-17, ROUTE-15 (P1) | PSR-7 bridge (return PSR-7, converted back) · CORE · CTRL-12 | WSGI + ASGI dual stack · CORE · CTRL-79 | Plug IS the standard; swappable Bandit/Cowboy adapters · CORE · CTRL-1/23 |
| Request introspection | `Request` struct (Method/URL/Header/Host/RemoteAddr/TLS) + `BasicAuth`/`UserAgent`/`Referer` · STD · CTRL-6 | `request`/`response` objects; `query_/request_/path_parameters` · CORE · CTRL-9 | `path/is/routeIs/fullUrl/host/method`, headers, `bearerToken`, `ip/ips` · CORE · CTRL-11 | `HttpRequest` attrs: scheme/method/path/META/headers/encoding, streamed body, `get_host` (proxy-aware), `build_absolute_uri` · CORE · CTRL-1–8/13–15/17/20 | one `%Plug.Conn{}` struct unifying req+resp · CORE · CTRL-2 |
| Params access & merging | `ParseForm` merges body+query into multi-value `url.Values`; path params separate · STD · CTRL-9, ROUTE-6 (P1) | one `params` bag (query+body+route), hash/array conventions · CORE · CTRL-2 | `input()` dot-notation + typed getters (`integer/date/enum…`), presence predicates, `merge` · CORE · CTRL-13/14 | deliberately unmerged `GET`/`POST` QueryDicts (multi-value API) · CORE · CTRL-3/21/22 | one merged map, priority path>body>query; raw sources preserved · CORE · CTRL-15 |
| Input allowlisting at the boundary | — | Strong Parameters: `expect` (400 on shape mismatch)/`require`/`permit` · CORE · CTRL-4 | `only/except` selection (validation → P6 FormRequest) · CORE · CTRL-13 | — (forms layer, filed P6) | — (Ecto changeset `cast`, filed P6) |
| Body parsing & limits | urlencoded/multipart only — no auto JSON; `MaxBytesReader` + typed `MaxBytesError` for 413 · STD · CTRL-9/8 | auto JSON parse keyed on Content-Type; `wrap_parameters` · CORE · CTRL-3 | JSON via `json()`/`input()` (parsing implicit) · CORE · CTRL-13 | no auto JSON body parse; raw `body` + swappable multipart parser class · CORE/OPT · CTRL-2/9 | `Plug.Parsers` (urlencoded/multipart/JSON) w/ `:length`/`:read_timeout` DoS limits · CORE · CTRL-14 |
| File uploads | buffered `ParseMultipartForm`/`FormFile` + streaming `MultipartReader` · STD · CTRL-10/11 | — here (Active Storage, filed P13) | `$request->file()`, mime helpers, `store/storeAs` to disks · CORE · CTRL-17 | `FILES` → `UploadedFile` · CORE · CTRL-4 | multipart via `Plug.Parsers` · CORE · CTRL-14 |
| Content negotiation & variants | — (single-value `ParseMediaType` only, no q-values · CTRL-34) | `respond_to` format blocks + `request.variant` per device · CORE · ROUTE-25 (P1)/CTRL-10 | `accepts/prefers/expectsJson` · CORE · CTRL-11 | `accepts()` + RFC 9110 `get_preferred_type()` · CORE · CTRL-18/19 | `accepts` pipeline plug; per-format render/error views · CORE · ROUTE-7 (P1), CTRL-19 |
| Conditional GET / ETag | `ServeContent` honors Range/If-None-Match/If-Modified-Since · STD · CTRL-16 | ETag/ConditionalGet middleware in stack · CORE · CTRL-18 | — (filed P12) | `condition/etag/last_modified` decorators + `conditional_page` · CORE · CTRL-54/55/56 | — |
| Response construction | `ResponseWriter`: implicit 200, C-T sniffing, auto Content-Length, trailers, 1xx · STD · CTRL-15 | mutable `response` headers · CORE · CTRL-9 | `response()` w/ status/headers/queued cookies; strings/arrays auto-convert · CORE · CTRL-18 | `HttpResponse` family, dict-style headers, status subclasses (400/404/…), file-like write API · CORE · CTRL-23–26/29/35/43 | `send_resp/3`, `put_status`, `put_resp_content_type` · CORE · CTRL-10 |
| Response types (JSON/file/template) | `ServeContent` (files) + `Error`/`NotFound`; JSON via `encoding/json` by hand · STD · CTRL-16 | `send_data/send_file` + X-Sendfile offload · CORE · CTRL-13 | view, `json/jsonp`, `download`, `file` · CORE · CTRL-20 | `render()` shortcut, `JsonResponse`, `FileResponse` (`wsgi.file_wrapper`) · CORE · CTRL-37/39/47 | `render/3`, `text/json/html` helpers · CORE · CTRL-9 |
| Redirect helpers | `http.Redirect` (Location + HTML body) · STD · CTRL-16, ROUTE-24 (P1) | (controller `redirect_to` not inventoried; route-level · ROUTE-14 (P1)) | `redirect/back/route/action/away` + flashed input · CORE · CTRL-19 | `redirect()` (model/view-name/URL), 301/302/307/308 matrix, `RedirectView` · CORE · CTRL-32/33/48/49/93 | `redirect/2` splits `to:` (in-app only, open-redirect safe) vs `external:` · CORE · CTRL-11 |
| Streaming & SSE | `Flusher`/`ResponseController.Flush`; SSE is a hand-rolled loop · STD · CTRL-17/14 | `ActionController::Live` + `response.stream` SSE · CORE · CTRL-14 | `stream/streamJson/streamDownload`, SSE `eventStream` + first-party npm client hooks · CORE · CTRL-21 | `StreamingHttpResponse` (async iterator under ASGI); async file streaming via `aiofiles` · OPT/ECO · CTRL-38/40 | conn streams natively via `send_resp`; WebSocket `upgrade` for bare Plug · CORE/OPT · CTRL-2/22 |
| Error → response mapping | — (`http.Error` one-liner; no registry, no debug page · CTRL-29) | `rescue_from` + `public/` error templates, format-aware dev pages · CORE · CTRL-11 | — in P1–P2 (central exception handler filed elsewhere) | `Http404`, default error views, `handler40x` hooks, auto exception→response between layers · CORE/DIY · CTRL-44/45/71, ROUTE-22 (P1) | `action_fallback` normalizes non-conn returns; `ErrorHTML/JSON`; `Plug.Exception` status protocol; `Plug.Debugger`/`ErrorHandler` · CORE · CTRL-13/19/20/21 |
| Flash messages | — (needs sessions · CTRL-23) | `flash.now`/`flash.keep` · CORE · CTRL-6 | session `flash/reflash/keep/now` · CORE · CTRL-23 | contrib.messages: levels, tags, 3 storage backends, CBV mixin · OPT · CTRL-114–117 | `put_flash/clear_flash` + `flash_group` component · CORE · CTRL-12 |
| Old-input repopulation | — | — (form builders rebind model) | `flash*/withInput()/old()` · CORE · CTRL-15 | — (bound forms re-render) | — (changesets re-render) |
| Sessions | — (flagship gap: no store, no lifecycle · CTRL-22) | encrypted `CookieStore` default + Cache/AR/Memcached; lazy; `reset_session` · CORE · CTRL-5 | file/cookie/db/redis/dynamodb/mongo drivers; per-session cache; per-session request serialization via locks; custom drivers · CORE · CTRL-23/24/25/26 | contrib.sessions: 5 backends, async API, `cycle_key`, `clearsessions`, cookie security settings, custom engines · OPT · CTRL-106–113 | cookie-backed `Plug.Session` + `fetch/put/get_session` · CORE · CTRL-17 |
| Cookies (signed/encrypted) | plain r/w incl. SameSite/Partitioned/`ParseCookie`; signing/encryption DIY · STD · CTRL-20/21 | plain/`signed`/`encrypted`/`permanent` + key rotation · CORE · CTRL-7 | queued cookies, automatic encryption · CORE · CTRL-18 | `set_cookie` (samesite etc.) + salted signed cookies w/ expiry · CORE/OPT · CTRL-27/28/16 | — (only the session cookie inventoried) |
| Per-request global state | `r.Context()` + `context.WithValue` (unexported key types) · STD · CTRL-12 | `CurrentAttributes`, auto-reset per request · CORE · CTRL-20 | — in P1–P2 (Context filed elsewhere) | middleware-set request attrs (`user/session/site`) + app hooks · CORE/OPT · CTRL-10/11 | `conn.assigns` · CORE · CTRL-2 |
| Sync/async execution model | — n/a (goroutine per request is the runtime; disconnect cancels ctx · CTRL-13) | threaded Rack; Executor/Reloader wrap every unit of work · CORE · CTRL-19 | — (process-per-request; Octane filed P18) | first-class async views/middleware/CBVs, `sync_to_async`/`async_to_sync` adapters, capability flags, `SynchronousOnlyOperation` guard, cost model · CORE · CTRL-46/72–74/79–89/91 | — n/a (BEAM process per connection is the runtime) |
| Code reload / dev-loop safety | — | Executor/Reloader framework · CORE · CTRL-19 | — | — (runserver autoreload, not inventoried here) | dev endpoint block: live reload, CodeReloader, CheckRepoStatus ("run migrations" page) · CORE · CTRL-5 |
| Generic CRUD handler abstractions | — (no `as_view()` analog · CTRL-5) | — (scaffold generators, P17) | resource controllers (see P1) · CORE · CTRL-4/5 | full generic-CBV suite: Template/Redirect/List/Detail/Form/Create/Update/Delete + 7 date-archive views, mixin lattice, hook methods · CORE · CTRL-92–105 | — (phx.gen, P17) |
| Pagination at the request layer | — | — (ecosystem) | — (ORM paginator, filed P4) | `Paginator` + `AsyncPaginator`, `ListView paginate_by` · CORE · CTRL-118/119/120 | — (ecosystem) |
| HTTP auth helpers | `Request.BasicAuth()` · STD · CTRL-6 | Basic/Digest/Token helpers · CORE · CTRL-12 | — (guards, filed P7) | — (contrib.auth, filed P7) | `Plug.BasicAuth` · CORE · CTRL-18 |
| Browser/client gating | — | `allow_browser versions:` → 406 for old browsers, on by default · CORE · CTRL-16 | — | — | — |
| Sensitive-data log filtering | — | `filter_parameters`/`filter_redirect` · CORE · CTRL-22 | — (filed P20) | `sensitive_variables`/`sensitive_post_parameters` decorators · CORE · CTRL-61 | — (filed P20) |
| Request normalization | path cleanup (`.`/`..`/`//`) + trailing-slash redirects · STD · ROUTE-12/13 (P1) | — | `TrimStrings`, `ConvertEmptyStringsToNull` (disable-able) · CORE · CTRL-16 | `APPEND_SLASH`/`PREPEND_WWW` + `no_append_slash` opt-out · CORE/OPT · CTRL-78/58 | `MethodOverride`/`Head` · CORE · CTRL-16 |
| Proxy / forwarded-header handling | — (netip `Prefix.Contains` for trust checks · ROUTE-23 (P1); no X-Forwarded rewrite) | `RemoteIp` in default stack · CORE · CTRL-18 | — in P1–P2 (TrustProxies filed elsewhere) | `USE_X_FORWARDED_HOST/PORT`, `ALLOWED_HOSTS` check, documented multi-proxy rewrite pattern · CORE · CTRL-13 | `Plug.RewriteOn` (x-forwarded-*) · CORE · CTRL-18 |
| Response extensibility | — (wrap `ResponseWriter` by hand; `ResponseController` traverses `Unwrap` · CTRL-14) | — (reopen classes) | `Response::macro` builders · CORE · CTRL-22 | custom `HttpResponse` subclasses, `render()`-emulation · DIY · CTRL-31/36 | — (plain functions) |
| Documented request lifecycle | `Server` lifecycle: timeouts, graceful `Shutdown`, panic isolation, `ConnState`/contexts · STD · CTRL-25/26/27/28 | Rack stack + Executor docs · CORE · CTRL-17/18/19 | single `index.php` entry, HTTP/console kernels · CORE · CTRL-27 | — (implied by WSGI/ASGI handlers · CTRL-79) | Endpoint as shared entry pipeline · CORE · CTRL-3 |

**Notable divergences.** The deepest split is the handler model: Phoenix and Django treat a handler as a plain function over an explicit request value, Rails demands a framework base class with implicitly available state (`params`, `session`, `flash`), and Laravel sits between — plain classes but with container DI injecting anything type-hinted. Phoenix is alone in making the pipeline element and the application the same abstraction (everything is a plug over one immutable Conn); the others separate "middleware" from "controller" as different species, and only Rack/Plug are ecosystem-wide standards — Laravel and Django middleware are framework-private contracts (Laravel only bridges out via PSR-7). On input, three frameworks merge route/query/body params into one bag while Django refuses on principle (separate multi-value `GET`/`POST`), and only Rails enforces an allowlist (strong params) at the controller boundary — Laravel, Phoenix, and Django all push input shaping down to the validation layer (P6). Concurrency is answered by runtime in three cases (PHP process-per-request, BEAM processes, Ruby threads + Executor) but is a massive explicit API surface in Django (~20 inventory rows of sync/async adaptation rules). Finally, Django uniquely ships generic CRUD as request-layer class machinery (CBVs + mixins) where Rails/Phoenix reach for code generators and Laravel for resource controllers.

**Volt notes.**
- Plug's model — one immutable conn, handlers and middleware sharing a single `fn(conn) → conn` shape, halt to short-circuit — is the closest analog to Go's `http.Handler` and the strongest evidence that a unified handler/middleware abstraction works at framework scale; Rails' implicit-state base class is the least Go-translatable.
- All four ship a curated default middleware battery (request ID, static, parsers, session, CSRF, proxy-header rewriting) and a documented ordering; Go's ecosystem norm is à-la-carte, so choosing and ordering a default stack is itself a headline feature for Volt.
- The params question has three positions (merged bag / merged-with-allowlist / deliberately separate); in Go the real decision is where typed decoding happens — Laravel's typed getters (`$request->integer()`, `date()`, `enum()`) and Django's typed route converters both target exactly the `strconv` boilerplate Go handlers drown in.
- Sessions with pluggable stores plus signed/encrypted cookies are CORE in every framework (only tier varies — Django marks them OPT-contrib); net/http offers none of this, and Rails' key-rotation and Laravel's session-locking rows show the long tail beyond "a cookie store".
- Go 1.25's `CrossOriginProtection` (CTRL-24) reframes the CSRF row: the Origin/Sec-Fetch-Site check is now stdlib, so Volt's default stack can wrap it and only needs to add what the stdlib explicitly omits — the hidden-field token fallback and template integration.

## P3 — Views, templating & frontend assets

- **Go std**: `html/template` — parse-time *contextual* auto-escaping that understands HTML, attribute, CSS, JS and URL contexts — plus `define`/`block`/`FuncMap` composition and `embed` + `FileServer` static serving; no component system, no form rendering, no asset pipeline, no hot reload.
- **Rails**: ERB compiled to Ruby methods, a very deep helper/form-builder library, layouts + partials, Propshaft + importmap for a no-Node asset story, Hotwire (Turbo + Stimulus) as default interactivity.
- **Laravel**: Blade — a compiled, zero-overhead template language with a full class/anonymous component system — plus a first-party Vite plugin (HMR, SSR, CSP/SRI); Livewire and Inertia are the blessed interactivity paths.
- **Django**: DTL — a deliberately restricted, autoescaping text language with `{% block %}` inheritance and ~60 filters — behind a multi-engine abstraction (Jinja2 blessed); staticfiles collects/hashes assets; bundlers and JS toolchains are explicitly out of scope.
- **Phoenix**: HEEx — HTML-aware, compile-checked templates where everything is a *function component* with declared attrs and slots — plus zero-Node assets via Elixir-wrapped esbuild and Tailwind.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Template language & compilation model | `html/template`, injection-safe superset of `text/template`, parsed at startup · STD · VIEW-1 | ERB compiled/cached as Ruby methods · CORE · VIEW-1 | Blade compiled to PHP, cached · CORE · VIEW-1, VIEW-3 | DTL interpreted, restricted-by-design · CORE · VIEW-1, VIEW-2 | HEEx `~H` compiled into module functions · CORE · VIEW-2, VIEW-7 |
| Auto-escaping + raw opt-out | Contextual auto-escape (HTML/attr/CSS/JS/URL); typed trusted values `template.HTML` etc. · STD · VIEW-2, VIEW-5 | ERB auto-escapes · CORE · VIEW-1 | `{{ }}` escape, `{!! !!}` raw, `Js::from` · CORE · VIEW-4 | Autoescape on; `safe`, `{% autoescape %}` · CORE · VIEW-17, VIEW-4 | Auto-escape all interpolation; `raw/1` documented-dangerous · CORE · VIEW-3 |
| Compile-time template validation | Parse/escape-time error taxonomy for unsafe constructs · STD · VIEW-6 | Strict locals: checked partial signatures · CORE · VIEW-9 | — (runtime) | — | HTML validity = compile errors; `attr/3` misuse warnings · CORE · VIEW-1, VIEW-2 |
| Control flow + loop metadata | `if`/`with`/`range` + `break`/`continue`; `range $i, $v` (no loop object) · STD · VIEW-7, VIEW-8, VIEW-13 | Plain Ruby; collection `_counter/_iteration` · CORE · VIEW-1, VIEW-8 | `@if/@switch`, `@foreach` + `$loop` object · CORE · VIEW-5, VIEW-6 | `{% if %}`, `{% for %}` + `forloop.*` · CORE · VIEW-7 | `:if`/`:for` special attrs, `:key` diffing · CORE · VIEW-5 |
| Layouts / inheritance | `block` define+override pattern (no first-class extends) · STD · VIEW-10 | `yield`/named yield + `content_for`, nested/conditional layouts · CORE · VIEW-6 | Component layouts *or* `@extends/@section/@yield` · CORE · VIEW-12 | `{% extends %}/{% block %}` + `block.super` · CORE · VIEW-8 | Root layout plug + explicit `<Layouts.app>` function component · CORE · VIEW-8 |
| Partials / includes | `{{template "name" pipeline}}` w/ explicit data · STD · VIEW-9 | Partials w/ locals, collection render, spacers · CORE · VIEW-7, VIEW-8 | `@include*`, `@each`, `@once` · CORE · VIEW-8 | `{% include with ... only %}`; `{% partialdef %}` (6.0) · CORE · VIEW-9, VIEW-16 | Function components; `embed_templates` (file ≡ function) · CORE · VIEW-1, VIEW-7 |
| Components w/ props, slots, attribute bags | — | — in core (partials + strict locals are the tool; ViewComponent not inventoried) | Class + anonymous components, `@props`/`@aware`, slots, `$attributes` bag, dynamic components · CORE · VIEW-9, VIEW-10, VIEW-11 | — (closest: `inclusion_tag` · VIEW-30) | `attr/3` declared props, named/scoped slots, attr splat · CORE · VIEW-1, VIEW-4, VIEW-6 |
| Generated/blessed UI component library | — | — | — | — | `CoreComponents` generated into app · CORE · VIEW-9; community systems · ECO · VIEW-10 |
| Conditional attribute/class helpers | — | `tag` builder, `token_list` · CORE · VIEW-14 | `@class/@style/@checked/...` · CORE · VIEW-7 | — | `class={}` nil/false handling, `{@attrs}` splat · CORE · VIEW-4 |
| Form builders (model-bound) | — | `form_with` + full field suite, custom builders, nested `fields_for` · CORE · VIEW-15–18 | Form directives only: `@csrf/@method/@error` · CORE · VIEW-13 | — in P3 (forms framework under P6; widget assets via Media, VIEW-43) | `.form`/`.input` in CoreComponents; `phoenix_html` substrate · CORE · VIEW-9, VIEW-11 |
| URL / asset / link tag helpers | — (FuncMap DIY · VIEW-16) | `image_tag`, `link_to`, `button_to`, preload etc. · CORE · VIEW-10, VIEW-11 | `Vite::asset` · CORE · VIEW-18 | `{% url %}`, `{% static %}` · CORE · VIEW-10, VIEW-34 | `Plug.Static` paths (verified routes in P1) · CORE · VIEW-18 |
| Text/number/date formatting helpers | — (FuncMap DIY · VIEW-34) | Deep helper library (`time_ago_in_words`, `number_to_currency`, `truncate`...) · CORE · VIEW-13 | — in P3 (Str/Number in P25) | ~60 filters (string/list/number/date families) · CORE · VIEW-3–6; humanize · OPT · VIEW-49 | — |
| Untrusted-HTML sanitization | — (escape helpers only · VIEW-22) | `sanitize`/`strip_tags`/`sanitize_css` (rails-html-sanitizer) · CORE · VIEW-12 | — (escaping only) | `striptags`, escaping filter family · CORE · VIEW-4 | — (`raw` is the only door) |
| Shared/global template data injection | — (explicit `Execute` data only) | — (controller ivars, not inventoried) | `View::share`, composers/creators, `@inject` DI · CORE · VIEW-2, VIEW-15 | Context processors, built-in + custom · CORE/DIY · VIEW-26, VIEW-27 | — (explicit assigns only, by design) |
| Template-language extension API | `FuncMap` custom functions · STD · VIEW-16 | Alternate template handlers · CORE · VIEW-2 | `Blade::directive/if`, echo handlers · CORE · VIEW-17 | Custom filters, `simple_tag`/`inclusion_tag`/`simple_block_tag`, Node API · CORE/DIY · VIEW-29–31 | Any function is a component — no special API · CORE · VIEW-1 |
| Multi-engine abstraction | — (`text/template` sibling engine · VIEW-23) | Handlers per format (Builder XML, Jbuilder) · CORE · VIEW-2 | — (Blade only) | `TEMPLATES` multi-engine, Jinja2 backend, custom backend · CORE/ECO/DIY · VIEW-20–23 | — (HEEx + plain EEx) |
| Template resolution, loaders & overriding | `ParseFiles`/`ParseGlob`/`ParseFS` + `Lookup`; no conventions · STD · VIEW-17, VIEW-18, VIEW-20 | Implicit `views/<controller>/<action>.<format>` convention · CORE · VIEW-3 | Dot notation, `View::first/exists` · CORE · VIEW-1 | Loaders (filesystem/app_dirs/cached/locmem), app-template overriding · CORE/DIY · VIEW-24, VIEW-25, VIEW-28, VIEW-32 | Per-format view modules co-located with controllers · CORE · VIEW-7 |
| Template precompilation/caching for deploys | Parsed once at startup; `//go:embed` into binary · STD · VIEW-18 | Compiled at first render · CORE · VIEW-1 | `view:cache`/`view:clear` · CORE · VIEW-3 | Cached loader (auto-on) · CORE · VIEW-24 | Compiled into bytecode at build · CORE · VIEW-2, VIEW-7 |
| Inline/string rendering | `New(...).Parse(string)` · STD · VIEW-17 | `render inline:` · CORE · VIEW-3 | `Blade::render` from string · CORE · VIEW-16 | `render_to_string`, Engine API · CORE · VIEW-28, VIEW-21 | `~H` sigil in any module · CORE · VIEW-1 |
| Fragment responses (htmx/turbo-style partial pages) | `ExecuteTemplate(w, name, data)` for any named template · STD · VIEW-20 | Turbo Frames/Streams · CORE · VIEW-23, VIEW-24 | `@fragment` + `->fragment()` · CORE · VIEW-16 | `{% partialdef %}` + `template.html#name` loading (6.0) · CORE · VIEW-16, VIEW-28 | — in P3 (LiveView owns this, P10) |
| Blessed interactivity layer | — | Hotwire: Turbo Drive/Frames/Streams + Stimulus, default · CORE · VIEW-22–26 | Livewire (no-JS) / Inertia (SPA glue, SSR) · ECO · VIEW-23, VIEW-24 | — (explicitly out of scope) | LiveView (core, inventoried under P10); component ecosystems · ECO · VIEW-10 |
| Bundler/build-tool integration | — | importmap no-build default; `jsbundling/cssbundling` escape hatches · CORE/OPT · VIEW-20, VIEW-21 | First-party Vite plugin: `@vite`, HMR, presets, env vars · CORE · VIEW-18, VIEW-19 (legacy Mix · OPT · VIEW-25) | — (non-goal: frontend tooling out of scope) | Elixir-wrapped esbuild + Tailwind, no Node; documented replacements · CORE · VIEW-12, VIEW-13, VIEW-17 |
| Asset fingerprinting / cache-busting | — | Propshaft digests, url() rewriting · CORE · VIEW-19 | Vite build manifest · CORE · VIEW-18 | `ManifestStaticFilesStorage` hashed names · OPT · VIEW-40 | `mix assets.deploy` + `phx.digest` manifest · CORE · VIEW-16 |
| Static file collection & serving | `FileServer`/`FileServerFS` over `embed.FS`; `ServeContent` ETag/Range · STD/TOOL · VIEW-24–27 | Propshaft logical paths, precompile/clean · CORE · VIEW-19 | public/ convention (not inventoried) · — | staticfiles app: `collectstatic`/`findstatic`, dev serving, storage/finders, deployment recipes · OPT/DIY · VIEW-33–42 | `Plug.Static` over `priv/static` · CORE · VIEW-18 |
| Third-party JS package handling | — | `bin/importmap pin` (CDN or vendored ESM) · CORE · VIEW-20 | npm via Vite · CORE · VIEW-19 | — | vendor / `npm --prefix assets` / Mix git deps · CORE · VIEW-15 |
| Production asset hardening (SSR, CSP nonce, SRI, prefetch) | — | — | Vite SSR builds, `useCspNonce`, SRI, prefetch strategies · CORE · VIEW-20 | `{% csp_nonce_attr %}` (6.1) · CORE · VIEW-18 | — |
| Font/icon optimization | — | — | Vite font providers + `@fonts` · CORE · VIEW-21 | — | Heroicons as Tailwind classes (only used icons ship) · CORE · VIEW-14 |
| Per-page asset injection (stacks / content_for / Media) | — | `content_for`/`provide`, `capture` · CORE · VIEW-6, VIEW-14 | `@push/@stack/@prepend` · CORE · VIEW-14 | Form/widget `Media` class, `Script`/`Stylesheet` objects (6.1) · CORE · VIEW-43, VIEW-44 | — (slots can serve) |
| Rich text editing | — | Action Text: `has_rich_text`, Trix, Active Storage attachments · CORE · VIEW-27, VIEW-28 | — | — | — |
| Template variants & localized templates | — | `+phone` variants, `index.es.html.erb` · CORE · VIEW-29, VIEW-4 | — | — (i18n tag libraries · OPT · VIEW-19) | Per-format modules (`HelloHTML`/`HelloJSON`) · CORE · VIEW-7 |
| Pagination rendering in views | — | — | `links()` w/ Tailwind/Bootstrap views, customizable · CORE · VIEW-22 | — | — |
| Template dev ergonomics (formatter, debug annotations) | — (no hot reload either · VIEW-32) | — | — | `{% debug %}`, `string_if_invalid` · CORE · VIEW-13, VIEW-21 | HEEx debug annotations, `mix format` for HEEx · CORE · VIEW-19, VIEW-20 |
| Non-HTML template output (XML/CSV/PDF) | `text/template` (unescaped twin engine) · STD · VIEW-23 | Builder XML, Jbuilder JSON handlers · CORE · VIEW-2 | — | CSV via csv module/streaming · DIY · VIEW-45; PDF via ReportLab · ECO · VIEW-46 | `home.xml.eex` + `put_format` · CORE · VIEW-21 |
| CMS-lite flat pages | — | — | — | contrib.flatpages + fallback middleware · OPT · VIEW-47, VIEW-48 | — |

**Notable divergences.** The deepest split is *when errors surface*: Phoenix makes malformed HTML and undeclared component attributes compile errors, Rails type-checks partial signatures with strict locals, while Blade and DTL fail (or silently render) at runtime. The component model splits the field: Laravel and Phoenix are component-first (declared props, named/scoped slots, attribute merging), Rails deliberately stays with partials + a huge helper library, and Django restricts template logic so hard that "components" only exist as custom tags. On assets the four take four philosophies: Laravel embraces the Node ecosystem via a first-party Vite plugin (HMR, SSR, CSP/SRI); Rails and Phoenix both engineer Node *away* (importmap CDN/vendored pins vs Elixir-wrapped esbuild binaries) but keep a build; Django declares the whole problem out of scope and only handles collection + fingerprinting of finished files. Django alone abstracts over multiple template engines; the other three bet on one language. Interactivity tiers differ too: Hotwire is CORE for Rails and LiveView is core Phoenix, while Laravel's equivalents (Livewire/Inertia) are ecosystem packages and Django offers nothing.

**Volt notes.**
- Go can plausibly match Phoenix's headline feature — compile-checked, HTML-aware templates (cf. templ) — which none of the interpreted-template frameworks offer; alternatively `html/template` gives contextual auto-escaping for free but no component/slot model, which is now the 2/4-and-rising baseline (props + slots + attribute merging).
- All four converged on fingerprinted assets with a manifest; the Phoenix pattern (framework-managed standalone esbuild/tailwind binaries, no Node) maps naturally onto Go's single-binary + `embed` deployment story and avoids Laravel's Node dependency without Django's abdication.
- Fragment/partial responses (Blade fragments, Turbo Streams, Django 6.0 `partialdef`, LiveView) appear in all four — first-class named-fragment rendering is now table stakes for htmx-style frontends.
- Rails and Django invest heavily in form/field helper suites tied to their model layers; Laravel and Phoenix push forms into components. Whichever Volt picks, form rendering is coupled to the validation/ORM design (P6/P4), not a standalone view concern.
- The Go baseline inverts the safety/ergonomics picture: `html/template`'s per-context escaper chains (HTML/attr/CSS/JS/URL) are *stronger* than the blunt HTML escaping all four frameworks apply, so Volt inherits best-in-class XSS safety for free — its P3 work is purely ergonomic: components/slots, view conventions, form rendering, hot reload and the asset pipeline are all stdlib NOs (VIEW-28–34).

## P4 — Data layer: models, ORM & queries

- **Go std:** `database/sql` only — a driver-agnostic pool/query/transaction layer (prepared statements, isolation levels, context cancellation, `Scanner`/`Valuer` type bridge) over third-party drivers; everything above the raw SQL string — models, struct mapping, query building, relations — simply does not exist.
- **Laravel:** two layers — a fluent SQL query builder over PDO plus Eloquent, a maximal ActiveRecord (relationships, casts, events, factories), with first-party Redis/MongoDB.
- **Rails:** the archetypal Active Record — schema-reflected classes, lazy chainable relation algebra, associations/callbacks/dirty-tracking making the model object the center of business logic.
- **Phoenix:** Ecto's data-mapper split — Repo (only thing that touches the DB), Schema (pure struct mapping), compile-checked composable Query DSL, Changeset; no lazy loading, no global model objects.
- **Django:** declarative active-record models carrying fields + Meta (constraints, indexes, managers); lazy QuerySets with lookups/F/Q/expressions/window functions; explicit `save()`; growing async surface.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| ORM paradigm | — (ORM-29) | Active Record, convention mapping · CORE · ORM-1 | Eloquent ActiveRecord · CORE · ORM-25 | Declarative AR `models.Model` · CORE · ORM-1 | Repo+Schema data mapper · CORE · ORM-1,3 |
| Field/attribute declaration | Struct tags — substrate only, no semantics · TOOL · ORM-27 | Schema-reflected, no declarations; Attributes API override · CORE · ORM-1,36 | Schema-reflected + declared casts · CORE · ORM-25,52 | Explicit field classes (~30 types) · CORE · ORM-19–36 | Explicit typed `field` decls · CORE · ORM-3 |
| Supported databases | — (zero drivers ship; SPI only, ORM-28) | SQLite (prod-blessed)/PG/MySQL/Trilogy + adapter API · CORE · ORM-52 | MariaDB/MySQL/PG/SQLite/SQL Server · CORE · ORM-1 | PG/MySQL/MariaDB/SQLite/Oracle (agnostic charter; capability tiers) · CORE · MIG-4, P23 preamble | PG/MySQL/MSSQL/SQLite/ClickHouse/ETS · CORE/OPT · ORM-2 |
| Connection pooling | `sql.DB` pool + `SetMax*` tuning + `Stats()` · STD · ORM-1,3,4 | Per-role/shard pools sized to threads · CORE · ORM-51 | PgBouncer-style pooled PG · CORE · ORM-3 | — (per-request connections; no pool row) | Repo-owned pools in supervision tree · CORE · ORM-1 |
| Read/write splitting & replicas | — (ORM-35) | Auto role-switching middleware + `connected_to` · CORE · ORM-45; LB explicitly DIY · ORM-47 | `sticky` read-your-writes split · CORE · ORM-2 | `using()` + DB routers · CORE/DIY · ORM-198,199 | Replica repo modules + `replica/0` picker · CORE · ORM-35 |
| Multiple DBs / sharding | — (one `*DB` per database; ORM-35) | `connects_to`, horizontal sharding + shard-selection middleware · CORE · ORM-44,46 | Multi-connection config, per-query selection · CORE · ORM-1 | Router `db_for_read/write` · DIY · ORM-199 | Dynamic repos (`start_link` + `put_dynamic_repo`) · CORE · ORM-36 |
| Multi-tenancy | — | Via sharding · CORE · ORM-46 | — | — | Query prefixes (PG schemas) · CORE · ORM-33; FK-scoping via `prepare_query` · DIY · ORM-34 |
| PK strategies (UUID/ULID) | — | UUID PKs via migration/generator config · CORE · MIG-13 | `HasUuids`/`HasUlids` (UUIDv7) · CORE · ORM-26 | Auto BigAutoField; native UUIDField · CORE · ORM-2,22; `UUID4/UUID7` funcs · OPT · ORM-185 | Custom `@primary_key`, `binary_id`, UUIDv7 helpers · CORE · ORM-4,38 |
| Composite primary keys | — | Full support incl. assoc + routing · CORE · ORM-48 | — | `CompositePrimaryKey` with real holes (no FK to CPK, no admin/forms) · OPT · ORM-31,32 | — (custom `@primary_key` only · ORM-4) |
| Auto timestamps | — (ORM-34) | Framework-maintained · CORE · ORM-2 | created/updated_at, disable/rename/touch · CORE · ORM-25 | `auto_now`/`auto_now_add` field opts · CORE · ORM-23 | `timestamps/1` macro · CORE · ORM-4 |
| Enum attributes | — | `enum` macro (predicates, scopes, prefix/suffix) · CORE · ORM-15 | Enum casts + `AsEnumCollection` · CORE · ORM-52 | `TextChoices`/`IntegerChoices` · CORE · ORM-4,5 | `Ecto.Enum` typed field · CORE · ORM-6 |
| Query builder / composition model | — (hand-concatenated SQL strings; ORM-31) | Lazy chainable relation algebra + overrides (`unscope/rewhere/reorder`) · CORE · ORM-6–8 | Fluent builder, `when()` conditional, `tap/pipe` query objects · CORE · ORM-8–20,23 | Lazy chainable QuerySets, result caching, slicing · CORE · ORM-130–132 | Compile-checked keyword+pipe DSL; queries are composable values · CORE · ORM-12,17 |
| Where/lookup vocabulary | — (ORM-31) | String/placeholder/hash conditions, `where.not/.or/.and` · CORE · ORM-6 | Huge `where*` family incl. date/JSON/full-text · CORE · ORM-15–17 | `__lookup` families (text/date-part/regex/JSON) + `Q` · CORE · ORM-157–165 | Full clause coverage; keyword-list/map shortcuts · CORE · ORM-14,18 |
| Runtime/dynamic query building | — (ORM-31) | Scope merging, `merge` · CORE · ORM-14 | `when($value, cb)` conditional · CORE · ORM-20 | `Q()` composition with `&`/`\|`/`~` · CORE · ORM-165 | `dynamic/2` macro composed via `Enum.reduce` (sanctioned filter pattern) · CORE · ORM-19 |
| Joins | — (raw SQL only) | `joins`/`left_outer_joins`, `where.associated/missing` · CORE · ORM-11 | inner/left/right/cross, closure, subquery, lateral · CORE · ORM-13 | Implicit via `__` traversal; `FilteredRelation` for ON-clause control · CORE/OPT · ORM-162,154 | `join on:`, assoc joins, named bindings, `inner_lateral_join` · CORE · ORM-15 |
| Subqueries / CTEs / window fns | — (raw SQL only) | — (not in corpus; Arel undocumented) | Subquery selects, `fromSub`, `whereExists` · CORE · ORM-11,17 | `Subquery`/`OuterRef`/`Exists`, `Window` + frames · OPT · ORM-166–168,186 | `subquery/1`, `parent_as` correlated; windows/CTEs · CORE · ORM-16,14 |
| Union / set operations | — (raw SQL only) | — | `union/unionAll` · CORE · ORM-14 | `union/intersection/difference`, `&`/`\|`/`^` · OPT/CORE · ORM-149,155 | — (not in corpus) |
| Aggregates | — (raw SQL only) | Grouped aggregate hashes · CORE · ORM-19 | count/max/min/avg/sum · CORE · ORM-10 | `annotate`/`aggregate`, rich fn set, conditional `filter=Q` · CORE/OPT · ORM-174–186 | `Repo.aggregate` w/ auto subquery wrap · CORE · ORM-11 |
| Column shortcuts (no hydration) | — (positional `Scan` only; ORM-30) | `pluck/pick/ids` · CORE · ORM-18 | `pluck/value` · CORE · ORM-8 | `values`/`values_list` · CORE · ORM-141,142 | `select:` maps/tuples/`%{id => email}` shapes · CORE · ORM-14 |
| Single-record finders | `QueryRow` + `ErrNoRows` sentinel · STD · ORM-5,7 | `find` (raises), `find_by(!)`, `take` · CORE · ORM-4 | `find/sole/firstOrFail` (404-throwing) · CORE · ORM-29 | `get()` raising DoesNotExist; `first/last/latest` · CORE · ORM-130,140 | `get/get_by/one` + `!` variants · CORE · ORM-7 |
| Find-or-create / upsert-one | — | `find_or_create_by(!)/find_or_initialize_by` · CORE · ORM-16 | `firstOrCreate/updateOrCreate/incrementOrCreate` · CORE · ORM-30 | `get_or_create/update_or_create` (atomic only w/ DB uniqueness) · CORE · ORM-134 | — (use `on_conflict` upsert · ORM-28) |
| Bulk writes | — | `update_all/delete_all` (no bulk insert in corpus) · CORE · ORM-3 | `insert/upsert/update/delete`, `insertUsing` · CORE · ORM-21 | `bulk_create/bulk_update/update()/delete()` · CORE · ORM-135–138 | `insert_all/update_all/delete_all` w/ placeholders · CORE · ORM-9 |
| Upsert / conflict targets | — | — (not in corpus) | `upsert(conflict, update)` · CORE · ORM-21 | `bulk_create(update_conflicts/unique_fields)` · CORE · ORM-135 | `on_conflict:`+`conflict_target:` incl. `:replace_changed` HOT · CORE · ORM-28 |
| Race-free atomic updates | — (raw SQL only) | — (not in corpus) | `increment/decrement/incrementEach` · CORE · ORM-21 | `F()` expressions · CORE · ORM-117,164 | `update_all` `:set/:inc/:push/:pull` (documented answer) · CORE · ORM-23 |
| Pessimistic locking | — (raw SQL only) | `lock/with_lock` · CORE · ORM-10 | `sharedLock/lockForUpdate` · CORE · ORM-22 | `select_for_update(nowait, skip_locked, of, no_key)` · OPT · ORM-152 | — (not in corpus) |
| Optimistic locking | — (ORM-34) | `lock_version` column · CORE · ORM-10 | — | — | — (not in corpus; Ecto has it upstream) |
| Transactions | `BeginTx`/`Commit`/`Rollback` + 8 isolation levels · STD · ORM-15,16 | Rollback-on-exception; `after_commit/rollback` callbacks · CORE · ORM-33,34 | Closure w/ deadlock retry, manual API, `afterCommit` · CORE · ORM-6 | `atomic` (nest/savepoint/durable), `on_commit`, autocommit control · CORE/OPT · ORM-192–196 | `Repo.transact` + named `Ecto.Multi` · CORE · ORM-29,30 |
| Associations: core types | — (ORM-32) | belongs_to (required by default)/has_one/has_many + full method family · CORE · ORM-22 | hasOne/hasMany/belongsTo, one-of-many, through · CORE · ORM-40 | ForeignKey/OneToOne; reverse managers; recursive/lazy refs · CORE · ORM-39,46–48 | belongs_to/has_one/has_many/:through · CORE · ORM-24 |
| Many-to-many + pivot data | — (ORM-32) | HABTM + has_many :through join models · CORE · ORM-23 | `belongsToMany`, pivot models, wherePivot · CORE · ORM-41 | `ManyToManyField` + `through` model · CORE · ORM-43,45 | `many_to_many` join table/schema, custom keys · CORE · ORM-24 |
| Polymorphic / generic relations | — | `polymorphic: true` type+id pairs · CORE · ORM-24 | Full morph family + morph maps · CORE · ORM-42 | Generic FK via contenttypes (outside P4; `GenericPrefetch` ref) · OPT · ORM-147 note | — (by design; embedded/abstract patterns instead) |
| Association config (dependent, touch, autosave) | — | `dependent:/touch:/autosave:/counter_cache:` + assoc callbacks/extensions · CORE · ORM-26–28 | `$touches`, cascade via FK or events · CORE · ORM-49 | `on_delete` Python-emulated + DB-level DB_CASCADE (6.1) · CORE/OPT · ORM-40,41 | DB-level `on_delete` in migrations instead · CORE · MIG-3 |
| Eager loading | — (ORM-32) | `includes`/`preload`/`eager_load` 3 strategies · CORE · ORM-12 | `with` (nested/constrained), `load/loadMissing` · CORE · ORM-46 | `select_related` (JOIN) / `prefetch_related` + `Prefetch` · CORE · ORM-146,147 | Explicit-only: `preload:`/`Repo.preload`/join+preload · CORE · ORM-25 |
| Lazy-loading & N+1 guardrails | — (no relations exist; ORM-32) | `strict_loading` per relation/model/app · CORE · ORM-13 | Auto eager-load mode; `preventLazyLoading`; `shouldBeStrict` · CORE · ORM-27,47 | `FETCH_PEERS` fetch mode auto-solves N+1; `RAISE` mode · OPT · ORM-151,201 (6.1) | No lazy loading exists, structurally · CORE · ORM-25 |
| Relationship aggregates / counters | — | Counter caches w/ backfill workflow · CORE · ORM-27 | `withCount/withSum...`, deferred `loadCount` · CORE · ORM-45 | `annotate(Count(...))` · CORE · ORM-174 | Via aggregate queries · CORE · ORM-11 |
| Persisting through relations / nested writes | — | Autosave + association build/create methods · CORE · ORM-22,26 | `save/createMany`, `associate`, m2m `attach/sync/toggle` · CORE · ORM-48 | Related managers `add/set/clear`; no nested-write API (formsets in P6, VAL-65) · CORE · ORM-45 | `cast_assoc` (external params) vs `put_assoc` (internal), `build_assoc` · CORE · ORM-26,27 |
| Scopes / reusable query logic | — (ORM-34) | `scope`/`default_scope`, merging, `unscoped` · CORE · ORM-14 | Global scopes + `#[Scope]` locals, pending attributes · CORE · ORM-36,37 | Custom managers + `QuerySet.as_manager`/`from_queryset` · CORE · ORM-118–126 | Plain query functions; `Ecto.Query` composition · CORE · ORM-17 |
| Lifecycle hooks / model events | — (ORM-29) | before/after/around callbacks, `throw :abort`, transactional callbacks · CORE · ORM-32,33 | Full event set, observers, `saveQuietly` muting · CORE · ORM-39 | Signal pipeline pre/post save/delete; skipped by bulk ops · CORE · ORM-102,37 | — (by design: no callbacks; explicit pipelines) |
| Dirty / change tracking | — (ORM-29) | ActiveModel::Dirty on every model · CORE · ORM-35 | `isDirty/wasChanged/getOriginal` · CORE · ORM-32 | Manual `update_fields`; `from_db` recipe · CORE/DIY · ORM-107,98 | Changesets carry `changes`; minimal-diff UPDATEs · CORE · P6 VAL-6,12 |
| Attribute casting / custom types | `sql.Scanner` + `driver.Valuer` on user types · STD · ORM-12 | `attribute :name, :type`, custom types · CORE · ORM-36 | Rich casts + `CastsAttributes` value objects · CORE · ORM-52,53 | Custom `Field` subclass API · DIY · ORM-34 | Custom driver types (`Postgrex.Types.define`) · OPT · ORM-37 |
| Accessors / normalization | — | `normalizes :email` on assignment+finders · CORE · ORM-39 | `Attribute::make(get:, set:)` · CORE · ORM-51 | `@property`, save() override · CORE · ORM-37 | — (transform inside changeset pipeline) |
| Attribute-level encryption | — | AR Encryption: deterministic mode, key rotation, contexts · CORE · ORM-41–43 | `encrypted:*` casts, `hashed` · CORE · ORM-52 | — | `redact: true` (log-hiding only, not encryption) · CORE · ORM-5 |
| Soft deletes & pruning | — (ORM-34) | — (ecosystem) | `SoftDeletes` trait; `Prunable` scheduled pruning · CORE · ORM-33,34 | — (ecosystem) | — (by design) |
| Inheritance strategies | — | STI + Delegated Types alternative · CORE · ORM-29,30 | — | Abstract bases, multi-table, proxy models · CORE · ORM-87,90,93 | — (structs, no inheritance) |
| Embedded documents / JSON columns | — | jsonb via PG-native types · CORE · ORM-49 | JSON `->` queries, `whereJsonContains`; array/object casts · CORE · ORM-16,52 | `JSONField` + full lookup family · CORE/OPT · ORM-29,163 | `embeds_one/many` structs in JSONB, jsonpath queryable · CORE · ORM-31 |
| Schemaless / table-only queries | Raw SQL strings are the only mode · STD · ORM-5 | — | Query builder is table-first by nature · CORE · ORM-8 | `values()`/raw; no schemaless model layer · ORM-141,188 | `from "posts"`, schemaless CRUD, `type/2` · CORE · ORM-22 |
| Generated/computed columns | — | — | `storedAs/virtualAs` migration modifiers · CORE · MIG-8 | `GeneratedField` (stored/virtual) · CORE · ORM-30 | — |
| PostgreSQL-native types | — (driver territory; ORM-28) | bytea/array/hstore/ranges/composite/enums/inet... · CORE · ORM-49 | — (vector only, ORM-18) | contrib.postgres: ArrayField/HStore/Ranges + PG indexes, exclusion constraints · OPT · PG-1–3,7,8 | Via custom postgrex types · OPT · ORM-37 |
| Full-text search (query-level) | — | PG full-text patterns · CORE · ORM-49 | `whereFullText` (per-driver) · CORE · ORM-17, SRCH-1 | `__search`/trigram/unaccent via contrib.postgres · ECO/OPT · ORM-173, PG-4–6 | — (`ilike` only, ORM-14) |
| Vector similarity search | — | — | `whereVectorSimilarTo`, distance select/order, auto-embedding · CORE · ORM-18, SRCH-2 | — (not in corpus) | — |
| Batch/streaming reads | `Rows.Next`/`Scan` cursor iteration · STD · ORM-8 | `find_each/in_batches` cursors · CORE · ORM-5 | `chunk/lazy/cursor` LazyCollections · CORE · ORM-9,28 | `iterator(chunk_size)` server-side cursors · OPT · ORM-203 | `Repo.stream` · CORE · ORM-10 |
| Pagination | — (ORM-34) | — (ecosystem; not in corpus) | Length-aware/simple/cursor paginators · CORE · ORM-56 | Core `Paginator`/`AsyncPaginator` (inventoried at P2 CTRL-118–120) · CORE | — (explicit gap: API-13) |
| Async query surface | Goroutine-safe pool + `*Context` cancellation · STD · ORM-1,6 | `load_async` thread-pool executor · CORE · ORM-21 | — | `a`-prefixed methods, `async for`; async transactions unsupported · CORE · ORM-200,197 | Whole runtime is concurrent (BEAM) — no special API |
| Raw SQL escape hatch | `Exec`/`Query` — raw SQL *is* the layer · STD · ORM-5 | `find_by_sql/select_all` · CORE · ORM-17 | `DB::select/statement`, raw expressions · CORE · ORM-4,12 | `raw()`, cursors, `RawSQL`, legacy `extra()` · DIY · ORM-188–191,156 | `fragment` + `Ecto.Adapters.SQL.query` · CORE · ORM-20,21 |
| Injection-safety mechanism | Bound params + prepared stmts (no placeholder-dialect unification) · STD · ORM-14,18,33 | Placeholder arrays (string interpolation warned) · CORE · ORM-6 | Bound parameters in builder/raw · CORE · ORM-12 | Params required; `RawSQL` warned · CORE/DIY · ORM-191 | Structural: `^var` pin required; string fragment = compile error · CORE · ORM-13,20 |
| Query debugging / instrumentation | — (only via wrapping the driver SPI; ORM-24,36) | `explain(analyze)`, `annotate` comments, optimizer hints · CORE · ORM-20 | `DB::listen`, slow-budget alerts, `dd/dumpRawSql` · CORE · ORM-5,24 | `explain()`, `execute_wrapper` · OPT/DIY · ORM-202,205 | — (telemetry in P20) |
| Strictness modes | — | `strict_loading` + deprecated-association reporting · CORE · ORM-13,31 | `shouldBeStrict` (lazy-load/discard/missing-attr) · CORE · ORM-27 | `RAISE` fetch mode · OPT · ORM-201 | Compile-time field checks (inherent) · CORE · ORM-12 |
| Model factories (test data) | — | — (fixtures only, P5 MIG-14; factory_bot is ecosystem) | First-party factories: states, sequences, relationship builders, `recycle` · CORE · ORM-55 | — (fixtures, P5 MIG-49) | — (seeds; ExMachina is ecosystem) |
| Redis / NoSQL integration | — | — (Redis appears only as infra elsewhere) | Redis facade (clusters, pipelines, pub/sub) · CORE · ORM-57; MongoDB pkg · OPT · ORM-58 | Explicitly none ("no NoSQL" charter) · non-goals | ETS adapter · OPT · ORM-2 |
| Schema cache | — | Dump/load schema cache per DB · CORE · ORM-50 | — | — | — (compile-time schemas make it moot) |

**Notable divergences.** The deepest split is object model: Laravel/Rails/Django put persistence, query state, and business behavior on the model object (ActiveRecord), while Ecto refuses — structs are inert, only `Repo` does I/O, and mutation intent lives in changesets, which eliminates whole categories (lazy loading, lifecycle callbacks, global scopes) rather than guarding them. N+1 answers span the full spectrum accordingly: structural impossibility (Phoenix), opt-in strictness/auto-eager modes (Laravel, Rails), and a brand-new fetch-mode system retrofitted onto lazy loading (Django 6.1). Rails and Laravel treat callbacks/events as core architecture; Django keeps them at arm's length (signals, skipped by bulk ops); Phoenix has none on principle. Portability philosophy diverges: Django is DB-agnostic by charter with a sanctioned Postgres contrib exception, Rails embraces PG-native types in core, Laravel ships pgvector/full-text in the core builder, Ecto pushes DB-specific work to adapters/fragments. Only Rails ships optimistic locking and attribute encryption in core; only Laravel ships soft deletes, pagination (incl. cursor), and model factories in core.

**Volt notes.**
- Ecto is the natural comparison point for Go (explicit, no runtime magic, compile-checked queries) — but note how much convention-driven productivity Rails/Laravel extract from schema reflection and how Django derives forms/admin/migrations from one model declaration; Go's static types could play the "single source of truth" role that Django's model class plays.
- Every framework converged on the same guardrail set from different directions: N+1 prevention, parameterization-by-construction, race-free atomic updates, and keyset/cursor iteration for large tables — table stakes for Volt's query layer.
- Absence patterns are informative: nobody ships replica load-balancing (Rails marks it explicitly out of scope), and pagination/optimistic-locking/factories are each core in exactly one framework — cheap differentiation opportunities.
- Laravel 13's pgvector-in-the-query-builder (`whereVectorSimilarTo`, auto-embedding) is the only first-party vector story among the four; Django's answer is still "contrib.postgres has no vector module."
- The Go baseline sharpens Volt's scope: `database/sql` already supplies pooling, transactions, and the `Scanner`/`Valuer` bridge, and its small wrappable driver SPI (ORM-24) is the natural hook for query logging/tracing — so the build surface is precisely everything above the SQL string, starting with scan-by-column-name into structs (ORM-30), the one absence sqlx/scany exist to patch.

## P5 — Schema evolution: migrations & seeding

- **Go std:** no migration concept at all — only the raw materials third-party migrators are built from: `//go:embed` to compile `.sql` files into the binary, `io/fs` lexical ordering for version-numbered files, and `database/sql` transactions to apply DDL.
- **Laravel:** class-based up/down migrations with a fluent 5-dialect schema builder, batch-tracked rollback, schema squashing, seeder classes composing factories.
- **Rails:** timestamped `change` migrations that auto-reverse, name-aware generators, a dumped canonical `schema.rb`/`structure.sql` used to build fresh DBs, `db/seeds.rb`.
- **Phoenix:** timestamped modules with reversible `change/0`, mix tasks, a `schema_migrations` ledger, plain-Elixir `seeds.exs`; release-friendly migration story without Mix.
- **Django:** autodetected per-app migration files as "schema version control" — `makemigrations` diffs model state into operation graphs; dependencies, reversal, squashing, fake-apply, `RunPython` data migrations with historical models; fixtures for seed data.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Migration authoring model | — (MIG-5) | Hand-written `change` (auto-reversed) · CORE · MIG-1 | Hand-written `up`/`down` classes · CORE · MIG-1,2 | **Autogenerated** operation lists diffed from models · CORE · MIG-1,9 | Hand-written `change/0` (auto-reversed) · CORE · MIG-2 |
| Generation / codegen | — (MIG-7) | Name-aware generators (`AddXToY x:string:index`) · CORE · MIG-2 | `make:migration` with name-based inference · CORE · MIG-1 | `makemigrations` autodetector (`--empty/--dry-run`) · CORE · MIG-1 | `ecto.gen.migration`; `phx.gen.*` emit migrations · CORE · MIG-1 |
| Ordering & dependencies | Lexical filename sort via `fs.ReadDir`/`Glob` · STD · MIG-2 | Timestamp order · CORE · MIG-1 | Timestamp order · CORE · MIG-1 | Explicit per-app dependency graph, `run_before`, swappable deps · CORE · MIG-6–8 | Timestamp order · CORE · MIG-1 |
| Version ledger | — (MIG-5; `sha256` checksums as raw material, MIG-4) | `schema_migrations` + `db:migrate:status` · CORE · MIG-12 | Batch-tracked table · CORE · MIG-3 | Recorded graph + history-consistency check, merge-conflict linearization prompt · CORE · MIG-10 | `schema_migrations` · CORE · MIG-7 |
| Rollback / reversal | — (MIG-5) | `db:rollback`, `redo`, STEP/VERSION, `revert`, IrreversibleMigration · CORE · MIG-8,9 | `rollback --step/--batch`, reset/refresh · CORE · MIG-3 | `migrate app 0002` / `zero`; IrreversibleError · CORE · MIG-12 | `ecto.rollback` (`--step/--to`) · CORE · MIG-8 |
| Table/column DSL | — (raw DDL strings) | Full type set + modifiers, `change_table` · CORE · MIG-3,4 | ~100 column types, rich modifiers, `change()` · CORE · MIG-6–9 | Operation classes (`CreateModel/AddField/AlterField/Rename*`) · CORE · MIG-29–39 | `create/alter/add/drop`, precision/null/default · CORE · MIG-2,5 |
| Index DSL | — (raw DDL strings) | unique/multi-column/named · CORE · MIG-6 | primary/unique/fulltext/spatial/vector(HNSW) · CORE · MIG-10 | AddIndex/RemoveIndex/RenameIndex + `Meta.indexes` (P4) · CORE · MIG-40, ORM-64 | `create index/unique_index`, composite + leftmost-prefix guidance · CORE · MIG-4 |
| FK / constraint DSL | — (raw DDL strings) | `add_foreign_key`, `add_check_constraint`, `add_reference` · CORE · MIG-5,6 | `foreignId()->constrained()`, cascade helpers · CORE · MIG-11 | AddConstraint/AlterConstraint + model-declared Check/Unique constraints (P4 ORM-66) · CORE · MIG-41 | `references(on_delete:)` — DB integrity favored over app cleanup · CORE · MIG-3 |
| Raw SQL in migrations | `Exec`/`Tx` on embedded `.sql` — the only mode · STD · MIG-1,3 | `execute` · CORE · MIG-7 | — (no dedicated row; `DB` facade usable) | `RunSQL` (reverse_sql, state_operations, elidable) · CORE · MIG-42 | — (not in corpus) |
| Data migrations | — | Explicitly out: keep data out of schema migrations (gems/rake) · DIY · MIG-15 | — (seeders are separate) | First-class: `RunPython` + historical models, batched non-atomic recipes · CORE · MIG-13,15,16,43 | — (seeds/contexts instead) |
| Atomic / transactional DDL | `Tx`-wrapped DDL where the DB allows (PG yes, MySQL no) · STD · MIG-3 | — (not in corpus) | — (only FK-constraint toggling, MIG-2) | Atomic per-migration on capable backends, opt-out · CORE · MIG-5 | — (not in corpus) |
| SQL preview | — | — | `--pretend` · CORE · MIG-2 | `sqlmigrate` · CORE · MIG-1 | — |
| Squashing / schema snapshot | — (MIG-9) | `schema.rb`/`structure.sql` dump is *canonical*; squash-to-schema workflow · CORE · MIG-11,18 | `schema:dump --prune` → SQL file + remaining migrations · CORE · MIG-5 | `squashmigrations` w/ operation optimizer, `replaces`, elidable · CORE · MIG-18 | — (not in corpus) |
| Fresh-environment bootstrap | — | `db:prepare` idempotent create+load+migrate+seed; fresh DB loads schema first (8.0) · CORE · MIG-9,10 | `migrate:fresh --seed`; dump-then-migrate · CORE · MIG-3,5 | Run full migration graph; `--fake-initial` for existing DBs · CORE · MIG-11 | `ecto.create` + `ecto.migrate` · CORE · MIG-6,7 |
| DB create/drop tasks | — (MIG-7) | `db:create/drop/setup/reset` · CORE · MIG-10 | — (via `db` CLI only, ORM-7) | — (test DBs only) | `ecto.create` / `ecto.drop` · CORE · MIG-6 |
| Production deploy safety | `//go:embed` migrations into the single binary · TOOL · MIG-1 | — | `--force`, `--isolated` cache-lock (one server migrates) · CORE · MIG-4 | — | `phx.gen.release` `bin/migrate` overlay (no Mix at runtime); dev out-of-date checker w/ migrate button · CORE · MIG-11,12 |
| Multi-DB / tenant migrations | — | Per-DB `migrations_paths`, scoped tasks · CORE · MIG-17 | — | Router `allow_migrate` + `schema_editor.connection.alias` · OPT · MIG-17 | `--prefix` + per-table prefix + `flush()` · CORE · MIG-9 |
| Migration hooks / events | — | — | Full event set (Started/Ended/SchemaDumped...) · CORE · MIG-13 | — | — |
| Fake / out-of-band application | — (MIG-10) | — | — | `--fake/--fake-initial/--prune`; `SeparateDatabaseAndState` · CORE/OPT · MIG-1,44 | — |
| Custom operations / DDL abstraction | — | — | — | `SchemaEditor` interface + custom `Operation` subclasses · OPT/DIY · MIG-28,47 | — |
| Serialization of model state | n/a (no model layer; ORM-29) | n/a | n/a (no autodetection) | Value serializer rules, custom serializers, `deconstruct()` · CORE/DIY · MIG-19–21 | n/a |
| Extension enablement (pgvector etc.) | — | — | `ensureVectorExtensionExists` + `vector()` columns · CORE · MIG-12,7 | `CreateExtension` + per-extension ops, `CreateCollation`, concurrent index ops · OPT · PG-10 | — |
| Zero-downtime recipes | — | — | `--isolated` only · MIG-4 | Documented: add-unique-non-null 3-step, concurrent indexes, NOT VALID constraints · OPT · MIG-24, PG-10 | — |
| Seeding | — (embed a seed `.sql` and `Exec` it yourself; MIG-8) | `db/seeds.rb`, `db:seed:replant` · CORE · MIG-14 | Seeder classes + factories, `db:seed --class` · CORE · MIG-14 | Data migrations (auto-applied incl. tests) · CORE · MIG-48 | `priv/repo/seeds.exs` via `mix run` calling context fns · CORE · MIG-10 |
| Fixtures (serialized data files) | — (MIG-8) | `db:fixtures:load` · CORE · MIG-14 | — | JSON/XML/YAML fixtures, discovery, compression, per-DB, raw-save semantics · CORE/OPT · MIG-49–55 | — |
| Third-party/engine migrations | — | `railties:install:migrations` copies engine migrations · CORE · MIG-16 | — | Third-party app compat rules · OPT · MIG-23 | — |

**Notable divergences.** The core split is authored vs derived: Laravel/Rails/Phoenix have developers write migrations (with generators helping), while Django *derives* them by diffing model state — which then forces a large secondary apparatus (dependency graphs, historical models, value serialization, `deconstruct()`, fake-apply) that the other three simply don't need. Source-of-truth differs too: Rails treats the dumped schema file as canonical (fresh DBs load it, not the migration chain), Laravel makes squashing an optional optimization, Django replays the graph, Phoenix's corpus doesn't mention squashing at all. Data migrations are first-class in Django (`RunPython` runs even during test-DB setup), explicitly rejected by Rails ("keep it out, use gems"), and absent from Laravel/Phoenix, which route data through seeders. On production deployment, only Laravel (`--isolated` lock) and Phoenix (Mix-free release binaries, dev drift checker) address the multi-server/runtime story; Django alone addresses transactional DDL and zero-downtime patterns (concurrent indexes, NOT VALID).

**Volt notes.**
- Autodetected migrations (Django-style) require serializing model state into files — heavy in Go without reflection-friendly metaprogramming; the authored-with-smart-generators model (Rails/Laravel/Phoenix) plus a canonical schema snapshot (Rails) looks like the lower-complexity path.
- Phoenix's release-binary migration story (`bin/migrate` without the build toolchain) is the closest analogue to Go's deploy reality (single static binary, no interpreter at runtime) — worth first-class treatment, as is Laravel's `--isolated` multi-server lock.
- Auto-reversal of a single `change` function (Rails/Phoenix) vs explicit up/down (Laravel) is a real DX divide; auto-reverse depends on a closed DSL vocabulary, which suits a typed builder API.
- Seeding splits into code-that-calls-your-app (Rails/Phoenix/Laravel) vs serialized data files (Django fixtures, whose raw-save signal semantics generate their own caveat list); the code path ages better with schema drift.
- `//go:embed` + `io/fs` lexical ordering (MIG-1,2) let the deployed binary carry and deterministically order its own `.sql` migrations with no runtime filesystem — which makes the Phoenix-style `bin/migrate` pattern even cheaper for Volt: a `migrate` subcommand on the single app binary.

## P6 — Validation & data integrity

- **Go std:** primitives, no framework — strict one-value parsers (`strconv`, `time.Parse`, RE2 `regexp`, `net/mail`, `net/url`, `net/netip`) plus `errors.Join`; no declarative rules, no field-keyed errors, no request-to-struct binding.
- **Laravel:** one ~100-rule vocabulary used three ways (inline `validate()`, FormRequest classes, manual Validator) at the HTTP boundary, with automatic redirect-or-422 behavior and deep array validation.
- **Rails:** model-level validations as the primary line of defense — declarative validator macros, an errors object wired into forms and i18n, DB constraints framed as complementary.
- **Phoenix:** `Ecto.Changeset` — explicit cast-then-validate pipelines that whitelist fields, split cheap in-app validations from race-proof DB-backed constraints, and double as the error-carrying contract between layers.
- **Django:** the forms system *is* the validation layer — Form/ModelForm field pipelines, `clean_<field>`/`clean()` hooks, widgets and formsets; shared validator callables; model `full_clean()` + DB constraints as the back-stop (but `save()` never validates).

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Primary validation locus | — (no framework; one-value parsers composed by hand, VAL-16) | Model object, runs on save · CORE · VAL-1 | HTTP boundary (request) · CORE · VAL-1 | Form classes; model `full_clean` as second layer · CORE · VAL-1,54 | Changeset pipeline (context boundary) · CORE · VAL-1 |
| Coupling to persistence | — | Automatic on `save/create/update`; bang variants raise; skip hatches · CORE · VAL-1 | None — validation precedes model | Decoupled: `save()` does NOT validate; `full_clean` explicit · CORE · VAL-54 | Repos accept changesets; invalid never reaches DB · CORE · VAL-12 |
| Built-in rule vocabulary | Strict one-value parsers: `strconv`/`time`/`regexp` (RE2)/`mail`/`url`/`netip` · STD · VAL-1–8 | Presence/absence, acceptance/confirmation, format/inclusion, length/numericality/comparison · CORE · VAL-2–5 | ~100 rules (types, comparisons, presence matrix, sets) · CORE · VAL-9 | Validator callables (regex/email/URL/range/length/step/file) shared by models+forms · OPT · VAL-45–48 | Small core: required/length/format/number · CORE · VAL-2–5 |
| Type coercion at the boundary | JSON decode enforces types · STD · VAL-10; forms manual per field (VAL-1,18) | Attributes API casting (P4 ORM-36) | — (validates, doesn't cast; casts live on the model, ORM-52) | Per-field `to_python()` → typed `cleaned_data` · CORE · VAL-40,3 | `cast/3` casts params against schema types · CORE · VAL-1 |
| Mass-assignment defense | JSON-only: `Decoder.DisallowUnknownFields` · STD · VAL-10 | Strong params (P2, controller layer) | `$fillable/$guarded` on model · CORE · ORM-31 | Explicit `Meta.fields` on ModelForm ("strongly recommended") · CORE · VAL-50 | `cast` whitelist — unknown keys dropped by design · CORE · VAL-1 |
| Form-object / write-model layer | — (VAL-16) | ActiveModel for non-DB objects (P4 ORM-37) · CORE | FormRequest classes (rules+authorize+hooks) · CORE · VAL-4 | Form/ModelForm classes; `modelform_factory` · CORE/OPT · VAL-1,50,55 | `embedded_schema` write-models; schemaless `{data, types}` changesets · CORE/DIY · VAL-10,11, ORM-32 |
| Cross-field validation | — (VAL-19) | Comparison validator; `validates_with` whole-record classes · CORE · VAL-5,7 | `confirmed/same/different/gt-lt` rules, `after()` hooks · CORE · VAL-9,4 | `Form.clean()` + `add_error` · CORE · VAL-42 | Custom changeset functions in pipeline (composition) · CORE · VAL-1 |
| Conditional validation | — (VAL-19) | `:if/:unless`, `:on` contexts, `with_options` · CORE · VAL-8,9 | `sometimes`, `Rule::when/unless`, `exclude` family · CORE · VAL-11 | — (partial: `full_clean(exclude=)`) | Per-action changeset functions (idiomatic; not a DSL) |
| Custom validators | — (any hand-written func; no shared contract, VAL-16) | Validator/EachValidator classes, `validate` methods · CORE · VAL-10 | Rule objects + closures + implicit rules · CORE · VAL-15 | Callables raising ValidationError (+deconstruct for migrations) · DIY · VAL-49,30 | Any `changeset -> changeset` function · CORE · VAL-1 |
| Uniqueness handling | — (raw driver error only; VAL-20) | Uniqueness validator + documented race caveat → pair with unique index · CORE · VAL-6 | `unique`/`exists` DB query rules (`Rule::unique()->ignore()`) · CORE · VAL-10 | `validate_unique` in full_clean + `UniqueConstraint` validated too · CORE · VAL-53,54, ORM-70,72 | `unique_constraint` converts DB violation into changeset error — race-proof by construction · CORE · VAL-8 |
| DB-constraint philosophy | — (VAL-20) | "App validation primary, DB complementary" · CORE · VAL-14 | Constraints in migrations; no error translation | Constraints declared on model, checked during validation AND enforced in DB · CORE · ORM-66,70–78 | DB as source of truth (not-null, unique, cascades); constraints ≠ validations, explicit split · CORE · VAL-8,15 |
| Error data structure | `errors.Join` flat multi-error — no field map · STD · VAL-12,17 | `errors` object (`where/full_messages/details/add`) · CORE · VAL-11 | MessageBag (wildcards, named bags) · CORE · VAL-3,7 | `errors` dict, `as_data/as_json`, `add_error/non_field_errors` · CORE/OPT · VAL-3–5 | Keyword errors w/ metadata + `traverse_errors` flattening · CORE · VAL-6,7 |
| Message customization & i18n | — (VAL-21) | Model/attribute-scoped i18n lookup chain · CORE · VAL-13 | Lang-file per-rule/attribute overrides · CORE · VAL-8 | `ValidationError(code, params)` + gettext · OPT · VAL-43 | `%{count}`-style interpolation metadata (gettext in P15) · CORE · VAL-6 |
| HTML form error integration | — | Auto `.field_with_errors` wrapper, error-list pattern · CORE · VAL-12 | `$errors` shared to views, `@error` directive · CORE · VAL-3 | Full rendering system: BoundField, CSS/aria hooks, templates · CORE/OPT · VAL-9–14 | Changeset implements FormData protocol; forms render from it · CORE · VAL-13 |
| Untouched-field UX | — | — | Precognition per-field client helpers · CORE · VAL-16 | — | `_unused_` params + `used_input?` suppress premature errors · CORE · VAL-14 |
| JSON API error shape | — (VAL-17) | — (not in corpus) | Auto-422 `{message, errors}` for XHR · CORE · VAL-1 | `errors.as_json()` · OPT · VAL-4 | Generated `ChangesetJSON` via traverse_errors · CORE · VAL-7 |
| Nested / collection validation | — | `validates_associated` · CORE · VAL-7 | `*` wildcards, `array:keys`, index placeholders · CORE · VAL-12 | Formsets (management form, min/max, ordering/deletion, inline FK editing) · CORE/OPT · VAL-56–65 | `cast_assoc/cast_embed` (child errors invalidate parent) · CORE · VAL-9 |
| File upload validation | — | — (not in corpus) | Fluent `File::types()->max()`, image dimensions · CORE · VAL-13 | `FileExtensionValidator`, image validation, multi-file recipe · OPT/DIY · VAL-48,66–68 | — (not in corpus; uploads in P13) |
| Password strength rules | — | `has_secure_password` confirmation/challenge (P4 ORM-38) | `Password::min()->uncompromised()` (pwned check) app defaults · CORE · VAL-14 | — (password validators live in auth, P7) | — (in phx.gen.auth, P7) |
| Live / predictive validation | — | — | Precognition middleware + npm form helpers · CORE · VAL-16 | — | LiveView `phx-change` per-field validation · CORE · VAL-14 |
| Validated-data access | — | — (model attrs are the data) | `validated()`/`safe()->only/except` · CORE · VAL-6 | `cleaned_data` (declared fields only) · CORE · VAL-3 | `changes`, `apply_changes/1` · CORE · VAL-6 |
| Pre-validation normalization | `strings.TrimSpace`/`Map`/`Cut` primitives · STD · VAL-9 | `normalizes` on assignment (P4 ORM-39) · CORE | `prepareForValidation` hook · CORE · VAL-4 | `to_python` coercion; `clean_<field>` returns replacement · CORE · VAL-40,41 | Transform steps in pipeline (plain functions) |
| Widget / input rendering layer | — | — (form helpers in P3) | — (Blade/frontend; not part of validation) | Full widget system (30+ widgets, Media assets, templates) bundled *inside* the validation layer · CORE/OPT · VAL-31–39 | — (function components in P3) |
| Stop-on-first-failure control | — | `:strict` raise option · CORE · VAL-8 | `bail` per field, `stopOnFirstFailure` · CORE · VAL-2,4 | — (all errors collected) | Pipeline order is explicit code |
| DB rules querying the DB | — | Uniqueness/associated validators · CORE · VAL-6,7 | `exists`/`unique` rules · CORE · VAL-10 | `ModelChoiceField` queryset membership · CORE · VAL-29 | Rejected — constraints handle DB truth · VAL-8 |

**Notable divergences.** Four genuinely different answers to "where does validation live": the request boundary (Laravel), the model object (Rails), an explicit pipeline value between boundary and repo (Phoenix), and a form class that also owns HTML rendering (Django — uniquely, widgets and template machinery are inside the validation layer, and `save()` never validates, so skipping `full_clean` silently skips validation). The uniqueness race is the sharpest philosophical marker: Rails documents the TOCTOU caveat and says add an index; Laravel queries the DB as a rule; Phoenix refuses to pretend — `unique_constraint` translates the actual DB violation into a user-facing error; Django does both (validator query + constraint validation + DB enforcement). Mass assignment is likewise structural in Phoenix (cast whitelist per call site) but configuration in the others (fillable/guarded, strong params, Meta.fields). Only Laravel and Phoenix have a first-party predictive/live validation story (Precognition; LiveView change events).

**Volt notes.**
- The changeset insight most portable to Go: constraint errors are *data*, not exceptions — translating driver unique/FK violations into the same error structure as field validations gives race-proof uniqueness with one UX; Go's `(T, error)` returns fit `{:ok, _}/{:error, changeset}` naturally.
- All four converge on one error-value contract consumed by both HTML forms and JSON APIs (MessageBag / errors object / changeset / errors dict) — designing that struct first, independent of transport, appears to be the load-bearing decision.
- Cast-then-validate (Phoenix/Django) doubles as mass-assignment defense and type coercion in one step; in Go, decoding `map[string]any` → typed struct with a field whitelist is the same move and avoids a separate "rules language."
- Laravel's ~100-rule string DSL trades compile-time safety for terseness — the exact opposite tradeoff a Go framework would make; Rails' `:on` contexts and Phoenix's per-action changeset functions are two ways to express "different rules for create vs update," a problem Volt must answer either way.
- The stdlib already covers the two halves Volt need not rebuild — strict per-value parsing (with RE2's no-ReDoS guarantee, VAL-3) and `errors.Is/As/Join` plumbing — but `errors.Join` is flat (VAL-12,17), so the field-keyed error value all four frameworks converge on is exactly the piece left for Volt to design.

### Aside — database-native extras (Django P23, Laravel P22)

Already folded into P4/P5 above: vector search (Laravel SRCH-2/ORM-18 vs nothing elsewhere), PG-native types/indexes/exclusion constraints (Django PG-1–8 vs Rails ORM-49 in core), extension/concurrent-index migration ops (Django PG-10 vs Laravel MIG-12), and PG full-text/trigram (Django PG-4–6, Laravel SRCH-1). Not carried in: Laravel Scout (index-synced external search engines, SRCH-5–8) — an application-search product layer rather than a data-layer capability; Django PG form fields (PG-13) — belongs to the forms layer.

## P7 — Authentication & authorization

- **Go std**: the cryptographic bedrock and nothing above it — CSPRNG (`rand.Text` token minting), HMAC + constant-time comparison, PBKDF2/HKDF, AEAD, Ed25519/ECDSA signatures, HTTP Basic parsing, and full cookie plumbing; no user model, session store, login flow, memory-hard hasher (x/crypto), or permission layer — a vault full of parts and no lock.
- **Rails**: since 8.0, a *generated* session auth system (User + DB Session models, controllers, mailer) built on core primitives (`has_secure_password`, `authenticate_by`, `generates_token_for`, `rate_limit`, `Current`); authorization is deliberately left to app code or gems.
- **Laravel**: guard/provider architecture with session auth in core, plus a layered first-party package stack — starter kits (UI), Fortify (headless + 2FA + passkeys), Sanctum (tokens/SPA), Passport (OAuth2 server), Socialite (social login) — and a full gate/policy authorization system in core.
- **Django**: `django.contrib.auth` — a swappable User model, pluggable backends behind `authenticate()`, model-level permissions + groups, packaged views/forms for login/password flows, and a tunable auto-upgrading hasher stack; contrib (OPT) but enabled by `startproject`.
- **Phoenix**: `mix phx.gen.auth` generates a complete owned-by-you auth system (magic links by default, confirmation, sudo mode, hashed token table), and v1.8 **Scopes** thread an authorization context through every generated controller, LiveView, and context function.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Identity architecture | — | generated User + DB Session models (app code, IP/UA metadata) · CORE · AUTH-1 | guard + user-provider config, multiple guards · CORE · AUTH-1 | contrib.auth User model · OPT · AUTH-1 | phx.gen.auth generated, explicitly owned code · CORE(gen) · AUTH-1/2 |
| Full-flow scaffolding (UI + flows) | — | generator: controllers, concern, mailer, routes · CORE · AUTH-1; signup/settings flows tutorial-only · DIY · AUTH-10 | starter kits (Inertia/Livewire) · OPT · AUTH-17; headless Fortify backend · OPT · AUTH-19 | login/logout/password views + forms (no signup view) · OPT · AUTH-9/10/11 | LiveView or controller flavor generated · CORE(gen) · AUTH-1 |
| Current-user access | — | `Current.user`/`Current.session` (CurrentAttributes) · CORE · AUTH-8 | `Auth::user()`, `$request->user()`, per-guard · CORE · AUTH-2 | `request.user` / async `auser()`; `AnonymousUser` null-object · OPT · AUTH-6/7 | `:current_scope` assign; `current_user` as authz subject · CORE · AUTH-3/7/15 |
| Route/view login guards | — | generated `Authentication` concern `before_action` · CORE · AUTH-1 | `auth` middleware (guard-param) · CORE · AUTH-3 | `login_required` decorator, site-wide `LoginRequiredMiddleware`, CBV mixins · OPT · AUTH-24/25/27 | `require_authenticated_user` plug + `on_mount` LiveView hooks · CORE(gen) · AUTH-3 |
| Manual login/credential API | — | `authenticate_by` (enumeration-hardened), `authenticate` · CORE · AUTH-2/3 | `Auth::attempt`/`login`/`once` + remember-me · CORE · AUTH-4 | `authenticate()`/`login()`/`logout()` + async variants · OPT · AUTH-4/5 | generated context functions · CORE(gen) · AUTH-1 |
| Password hashing | PBKDF2 only, nothing memory-hard · STD · AUTH-5 (bcrypt/argon2 in x/crypto · AUTH-6) | `has_secure_password` (bcrypt) · CORE · AUTH-2 | bcrypt default, argon2/argon2id, tunable · CORE · SEC-5 (P8) | PBKDF2 default; scrypt in-box; argon2/bcrypt via libs; standalone utils · OPT/ECO · AUTH-30/31/32/35 | Comeonin: bcrypt/pbkdf2/argon2 via `--hashing-lib` · CORE(gen) · AUTH-4 |
| Hash upgrade / auto-rehash | — | — (bcrypt only) | rehash on work-factor change · CORE · AUTH-9 | upgrade-on-login, work-factor tuning, wrapped-hasher offline upgrade · OPT/DIY · AUTH-33/34 | — |
| Password strength/compromise rules | — | presence/length/confirmation validations · CORE · AUTH-2 | `uncompromised()` HIBP k-anonymity rule · CORE · SEC-11 (P8) | `AUTH_PASSWORD_VALIDATORS` incl. 20k common-password list · OPT · AUTH-36/37 | — (changeset validations implicit in AUTH-1) |
| Password reset | — | generated flow, `find_by_password_reset_token!`, mailer · CORE · AUTH-4 | token broker, DB/cache storage, expiry + throttle · CORE · AUTH-16 | `PasswordReset*` view suite + token generator · OPT · AUTH-10 | — (magic-link login fills the role · AUTH-1) |
| Email verification / confirmation | — | email-change confirmation pattern (tutorial) · DIY · AUTH-10 | `MustVerifyEmail` + signed links + `verified` middleware · CORE · AUTH-15 | — | registration + email confirmation generated · CORE(gen) · AUTH-1 |
| Magic-link / passwordless login | — | — | — (WorkOS variant only · ECO · AUTH-18) | — | default login mode; password auth is the opt-in · CORE(gen) · AUTH-1 |
| Re-auth for sensitive actions (sudo) | — | `password_challenge` · CORE · AUTH-2 | `password.confirm` middleware + timeout · CORE · AUTH-7 | — | sudo mode + `require_sudo_mode` plug · CORE(gen) · AUTH-1/3 |
| Two-factor auth (TOTP) | — (hmac+sha primitives suffice, nothing packaged · AUTH-23) | — | Fortify TOTP + QR + recovery codes · OPT · AUTH-20 | — | — |
| Passkeys / WebAuthn | — | — | Fortify passkeys + `@laravel/passkeys` JS · OPT · AUTH-21 | — | — |
| Social login (OAuth client) | — (x/oauth2 client · AUTH-21) | — | Socialite, normalized user object · OPT · AUTH-26 | — (explicitly not provided · SEC-16 (P8)) | Ueberauth (blessed) · ECO · AUTH-16 |
| OAuth2 authorization server | — | — | Passport: all grants + PKCE + device flow, scopes · OPT · AUTH-24/25 | — | — |
| API token auth | — (`rand.Text` mints tokens, no auth machinery · AUTH-2) | HTTP Token auth · CORE · AUTH-9 | Sanctum hashed tokens, abilities, expiry, prune · OPT · AUTH-22 | — (DRF ECO) | hashed api-token recipe on generated `UserToken` · DIY · AUTH-11 |
| SPA cookie auth for APIs | — | — | Sanctum stateful domains + CSRF-cookie handshake · OPT · AUTH-23 | — | — |
| HTTP Basic/Digest auth | Basic only: `Request.BasicAuth`/`SetBasicAuth`; constant-time check is caller's job · STD · AUTH-12 | Basic/Digest/Token · CORE · AUTH-9 | `auth.basic`, stateless `onceBasic` · CORE · AUTH-5 | — | — |
| External/SSO delegation | — | — | WorkOS AuthKit (SSO, social, magic) · ECO · AUTH-18 | `REMOTE_USER` middleware + backend (web-server auth) · OPT · AUTH-16 | — |
| Server-side session store, per-device revocation | — | DB-backed Session model enables revocation · CORE · AUTH-1/7 | `logoutOtherDevices` + `AuthenticateSession` · CORE · AUTH-6 | logout flushes session; invalidation on password change via session-auth hash · OPT · AUTH-5/12 | hashed `users_tokens` table, per-device tracking, global invalidation on password change · CORE(gen) · AUTH-5 |
| Long-lived connection revocation | — | — | — | — | `live_socket_id` broadcast disconnect kills all LiveViews/channels · CORE · AUTH-13 |
| WebSocket/channel auth | — | — | Sanctum private broadcast channel authz · OPT · AUTH-23 | — | `Phoenix.Token` sign/verify; dual-entry (plug + mount) security model, re-check in `handle_event` · CORE · AUTH-12/14 |
| Purpose-scoped expiring tokens | — (hmac/HKDF/AEAD bricks, no verifier API · AUTH-3/8/15) | `generates_token_for :purpose, expires_in:`, state-invalidated · CORE · AUTH-5 | signed URLs · CORE · SEC-9 (P8) | `Signer`/`TimestampSigner` · CORE · SEC-10/11 (P8) | `Phoenix.Token` with `max_age:` · CORE · AUTH-12 |
| Login throttling | — (x/time/rate · SEC-18 (P8)) | controller-level `rate_limit` · CORE · AUTH-6 | username+IP via kits/Fortify; DIY via rate limiter · CORE · AUTH-11 | — (explicit non-goal · SEC-16 (P8)) | — |
| Enumeration/timing hardening | `subtle.ConstantTimeCompare` + DIT, opt-in primitive · STD · AUTH-4 | `authenticate_by` hardened by default · CORE · AUTH-3 | — | hasher `harden_runtime` hook · DIY · AUTH-34 | explicitly **not** attempted, tradeoff documented · — · AUTH-17 |
| Declarative authorization framework | — | none: role flags + `before_action` guards · DIY · AUTH-11 | Gates + Policies (auto-discovery, before/after, Response objects w/ status) · CORE · AUTH-12/13 | model-level Permission + Group models, custom perms · OPT · AUTH-18/20/22/23 | `%Scope{}` struct threaded as first arg to every context fn · CORE · AUTH-7 |
| Object-level authorization | — | scope queries to `Current.user` (privilege-escalation pattern) · DIY · AUTH-11 | policy methods receive the model instance · CORE · AUTH-13 | `obj` param hooks exist but core returns empty; third-party · ECO · AUTH-21 + SEC-16 (P8) | generated `where: post.user_id == ^scope.user.id` filters · CORE · AUTH-8 |
| Enforcement surfaces (controller/route/template) | — | `before_action` guards · DIY · AUTH-11 | `can:` middleware, `#[Authorize]`, `Gate::authorize`, Blade `@can` · CORE · AUTH-14 | `permission_required`, `user_passes_test`, CBV mixins, `{{ perms }}` context · OPT · AUTH-15/26/27 | plugs + `on_mount` hooks + `handle_event` re-checks · CORE · AUTH-3/14 |
| Multi-tenancy scoping of generated code | — | — | — | — | scope config drives generators: FK columns, `route_prefix`, org scopes, scoped PubSub · CORE · AUTH-8/9/10 |
| Pluggable backends / custom guards | — (handler-wrapping seam, no contract · AUTH-19) | all app code — edit directly · CORE · AUTH-1/2 | `Auth::extend`, `viaRequest` closures, custom `UserProvider` · CORE · AUTH-8 | `AUTHENTICATION_BACKENDS` chain, async backend iface · OPT/DIY · AUTH-17/28 | generated code is application code · CORE · AUTH-2 |
| Swappable/custom user model | — | User model is app code · CORE · AUTH-1 | `Authenticatable` contract · CORE · AUTH-8 | `AUTH_USER_MODEL` + `AbstractBaseUser`/`AbstractUser` bases · DIY · AUTH-29 | generated schema is app code · CORE(gen) · AUTH-1 |
| Auth lifecycle events/signals | — | — | full event set (Login, Failed, Lockout, Verified…) · CORE · AUTH-10 | `user_logged_in/out/login_failed` signals · OPT · AUTH-13 | — |
| Admin/user management CLI | — | — | — | `createsuperuser`/`changepassword` · OPT · AUTH-3 | — |
| Auth testing helpers | — | — | `actingAs`, `Sanctum::actingAs`/`Passport::actingAs`, assertions · CORE/OPT · AUTH-27 | — | — |
| Full-auth ecosystem alternative | — | Devise named · ECO · AUTH-12 | — (first-party stack is the answer) | — | Ueberauth named · ECO · AUTH-16 |

**Notable divergences.** The deepest split is *where the auth code lives*: Rails 8 and Phoenix generate editable application code (Phoenix even documents that security fixes will NOT auto-apply, AUTH-2), Laravel installs framework packages you configure but don't own, and Django ships a contrib app with a swappable-model escape hatch. Authorization gets four unrelated answers: Laravel has a complete gate/policy system in core; Django has a model-level permission/group framework whose object-level hooks are deliberately stubbed (ECO); Phoenix invented Scopes — an authz context threaded through generated queries, effectively compile-time row-level security; Rails ships nothing and documents role-flag patterns as DIY. Modern credential mechanisms diverge sharply: Phoenix defaults to magic links, Laravel is the only one with first-party 2FA, passkeys, an OAuth2 server, and a social-login client; Rails and Django remain password-first with ecosystem pointers. On account enumeration, Rails hardens by default (`authenticate_by`) while Phoenix explicitly declines and documents why — opposite defaults from the two generator-based frameworks.

**Volt notes.**
- The 2024–25 convergence among the newest designs (Rails 8, Phoenix 1.8) is *generated, owned auth code* over installed libraries. This fits Go unusually well (explicit code beats reflection), but the Phoenix AUTH-2 tradeoff — no automatic security patches for generated code — must be confronted, e.g. versioned generators + advisory tooling.
- Phoenix Scopes are the only structural answer to "authorization context flows through every query": a struct passed as the first argument everywhere. That is idiomatically identical to Go's `ctx`/first-param convention and is the most Go-transplantable idea in either section.
- The table-stakes union beyond login is large and consistent: email verification, password reset (or magic link), sudo re-auth, per-device server-side session revocation, login throttling, purpose-scoped expiring tokens, API tokens, auth test helpers. All four cover most of it; 2FA/passkeys/OAuth-server are where they stop and only Laravel continues (as OPT packages).
- Everyone hashes passwords with bcrypt/argon2-class algorithms and three of four handle work-factor upgrades transparently on login; hasher chains + auto-rehash are a framework concern, not something `x/crypto` gives you.
- The Go std column draws the build-vs-ship line exactly: std supplies every primitive beneath this table (`rand.Text` token minting, HMAC + constant-time compare, HKDF, AEAD, cookie attributes) and not one row-level capability — sessions and signed cookies (AUTH-15/16) are the deepest gaps — so the entire table-stakes union is Volt-owned code, with HKDF (AUTH-8) the doc-blessed mechanism for the one-master-secret, per-purpose-subkey design the other frameworks all converge on.

## P8 — Security

- **Go std**: unusually strong for a standard library — contextual autoescaping (`html/template`), parameterized queries (`database/sql`), header-based CSRF via `CrossOriginProtection` (1.25), request-size/timeout DoS guards, and a production TLS 1.3 stack with post-quantum key exchange on by default; what's missing is the *policy* layer — no security-header/CSP middleware, host validation, token CSRF, rate limiting, or secrets story.
- **Rails**: secure-by-default stack (auto-escaping, CSRF, parameterized SQL, signed+encrypted cookies, headers, CSP DSL) plus a unique encrypted-credentials story and a guide-length threat manual; Brakeman scanning in default CI.
- **Laravel**: security is ambient — forgery protection (now `Sec-Fetch-Site`-first), cookie encryption, auto-escaping, and hashing are on by default; encryption/signing are one-liner services keyed from `APP_KEY`.
- **Django**: secure-by-default behavior plus a stack of dedicated middleware all present in `startproject` — CSRF with Origin checking, `SecurityMiddleware` header suite, host validation, CSP (new in 6.0), and a signing API with key-fallback rotation.
- **Phoenix**: safe-by-default primitives (HEEx escaping, pin-operator SQL params, CSRF plug, secure headers with default CSP) plus an unusually frank guide cataloguing how to defeat them; distinctive DoS focus for stateful connections.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| CSRF protection | `CrossOriginProtection`: Sec-Fetch-Site/Origin rejection, trusted origins, deny handler (1.25); no token fallback · STD · SEC-4/5 | per-session/per-form tokens auto-injected in `form_with`, strategies · CORE · SEC-1 | `Sec-Fetch-Site` check first, session token fallback, strict `originOnly`, XSRF cookie for JS · CORE · SEC-1/2 | `CsrfViewMiddleware`: secret cookie, BREACH masking, Origin/Referer + trusted origins, rotation on login; AJAX header, decorators suite · CORE · SEC-5/6/7 | `Plug.CSRFProtection` in `:browser`, hidden `_csrf_token`, token handed to LiveSocket; GET-action-reuse pitfall documented · CORE · SEC-1/2 |
| XSS / auto-escaping | `html/template` contextual autoescaping (HTML/JS/CSS/URI); `html.EscapeString` · STD · SEC-1/2 | ERB auto-escaping, `html_safe`/`raw` opt-outs, allowlist `sanitize` · CORE · SEC-2 | Blade `{{ }}` escaping, `Js::from` · CORE · SEC-7 | template autoescaping + `mark_safe` caveats · CORE · SEC-1 | HEEx auto-escaping; bypass vectors (`raw/1`, content-type from input) documented · CORE · SEC-3 |
| SQL injection defense | `database/sql` placeholders end-to-end; no fragment/identifier helpers · STD · SEC-3 | parameterized query API + `sanitize_sql*` helpers · CORE · SEC-3 | — (parameterized ORM; no P8 row) | queryset parameterization; `raw()`/`extra()` cautions · CORE · SEC-2 | pin-operator params; fragment interpolation is a *compile error* · CORE · SEC-5 |
| Mass-assignment defense | — | Strong Parameters required (`expect`/`permit`) · CORE · SEC-4 | — (guarded attributes live in P4) | — (form `fields` whitelists; no P8 row) | changeset `cast` whitelist; `is_admin` canonical example · CORE · SEC-9 |
| Password hashing service | — (pbkdf2 primitive only · AUTH-5 (P7)) | — (AUTH-2, P7) | bcrypt/argon2 drivers, `needsRehash`, unknown-hash rejection · CORE · SEC-5 | — (AUTH-30, P7) | — (AUTH-4, P7) |
| Symmetric encryption API | `cipher.AEAD` + `NewGCMWithRandomNonce` (primitive, no keyed service) · STD · AUTH-11 (P7) | — (encrypted cookies/credentials only, SEC-5/6) | `Crypt` AES+MAC keyed from `APP_KEY` · CORE · SEC-3 | — (signing only, SEC-10) | — (`Plug.Crypto` adjacent, SEC-6) |
| Signing / tamper-proof data & URLs | — (hmac + base64 bricks, no verifier · AUTH-3/15 (P7)) | signed-cookie infrastructure · CORE · SEC-6 | signed URLs, hash-verified verification links · CORE · SEC-9 | `Signer`/`TimestampSigner`, `dumps()`/`loads()`, swappable backend · CORE · SEC-10/11 | signed session; `Phoenix.Token` · CORE · SEC-13 + AUTH-12 (P7) |
| Key rotation | — (HKDF derives, nothing rotates · AUTH-8/15 (P7)) | cookie rotation for secret/config changes · CORE · SEC-6 | `APP_PREVIOUS_KEYS` decrypt fallback + `key:generate` · CORE · SEC-4 | `SECRET_KEY_FALLBACKS` (validate-only) · CORE · SEC-12 | — |
| Secrets management | — | `credentials.yml.enc` + master key, per-env files, `credentials:fetch` CLI · CORE · SEC-5 | env encryption (cross-ref CONF) · CORE · SEC-10 | — | — (runtime env config, P18) |
| Cookie encryption/signing defaults | — (attributes incl. SameSite/Partitioned, no crypto · AUTH-14/15 (P7)) | signed & encrypted jars from `secret_key_base` · CORE · SEC-6 | encrypted + signed by default, exemptions configurable · CORE · SEC-6 | — (secure/HttpOnly flags via settings · SEC-8 note) | session cookie signed (optionally encrypted) · CORE · SEC-13 |
| Default security headers | — | `X-Frame-Options`, nosniff, referrer-policy, etc. by default · CORE · SEC-7 | — | `SecurityMiddleware`: HSTS, nosniff, referrer policy, COOP · CORE · SEC-8 | `put_secure_browser_headers` incl. default CSP when unset · CORE · SEC-4 |
| Clickjacking | — | `X-Frame-Options` default · CORE · SEC-7 | — | `XFrameOptionsMiddleware` (DENY) + per-view decorators · CORE · SEC-3/4 | `frame-ancestors 'self'` in default CSP · CORE · SEC-4 |
| Content Security Policy | — | DSL: global + per-controller, auto nonces, report-only mode · CORE · SEC-8 | — (nonce via Vite; documented gap) · SEC-12 | middleware + nonce sentinel/context/tag + per-view overrides (new 6.0/6.1) · CORE · SEC-13/14/15 | default baseline CSP via secure headers · CORE · SEC-4 |
| TLS enforcement / HSTS | in-proc TLS 1.3 stack, PQ key exchange default; no HSTS/redirect policy · STD · SEC-9/10 | `force_ssl` (HSTS, secure cookies, redirect), `assume_ssl` · CORE · SEC-9 | — | `SECURE_SSL_REDIRECT` + HSTS settings suite · CORE · SEC-8 | `force_ssl` + `Plug.SSL`, OWASP cipher-suite profiles, `phx.gen.cert` dev certs · CORE · SEC-10/11/12 |
| Host-header / DNS-rebinding defense | — | `HostAuthorization` allowlist middleware · CORE · SEC-10 | `trustHosts` middleware · CORE · SEC-8 | `ALLOWED_HOSTS` validation in `get_host()` · CORE · SEC-9 | — (socket `check_origin` lives in P10) |
| Reverse-proxy trust | — | `assume_ssl` · CORE · SEC-9 | `trustProxies` (header selection, wildcard) · CORE · SEC-8 | `SECURE_PROXY_SSL_HEADER`, `USE_X_FORWARDED_HOST` · CORE · SEC-8/9 | `rewrite_on: [:x_forwarded_proto]` · CORE · SEC-11 |
| Open-redirect protection | — | cross-origin `redirect_to` raises unless allowed · CORE · SEC-11 | — | — (login views' `success_url_allowed_hosts` only · AUTH-9 (P7)) | — |
| CORS | — | `rack-cors` recommended · ECO · SEC-18 | — | — (django-cors-headers, not in inventory) | `CORSPlug` shown, wildcard anti-pattern warned · ECO · SEC-8 |
| Sensitive-data log/param filtering | — | `filter_parameters` deep filtering + redirect filtering, encrypted attrs auto-added · CORE · SEC-15 | hidden attrs, encrypted casts, encrypted jobs (cross-refs) · CORE · SEC-10 | — | `password`/`token` params masked by default · CORE · SEC-16 |
| Session-attack countermeasures | — (no session framework · AUTH-16 (P7)) | fixation/replay/hijack documented, `reset_session`, CookieStore guidance · CORE · SEC-12 | invalidate/regenerate on logout · CORE · AUTH-6 (P7) | session-key rotation via auth hash · OPT · AUTH-12 (P7) | token-table invalidation · CORE · AUTH-5 (P7) |
| ReDoS mitigation | — | global `Regexp.timeout = 1s` default + anchoring guidance · CORE · SEC-13 | — | — | — |
| Request/connection DoS limits | `MaxBytesReader`/`MaxBytesHandler` + `Server` timeouts, all off by default · STD · SEC-14/15 | — | — (rate-limiting pointer · SEC-12) | — | `Plug.Parsers` body/upload limits; `max_channels_per_transport`, longpoll batch caps · CORE · SEC-14/15 |
| Safe deserialization / RCE defense | — (memory-safe language baseline · SEC-24) | command-injection guidance · CORE · SEC-19 | — | — | `non_executable_binary_to_term` (safe `binary_to_term`), never-eval guidance · CORE · SEC-6 |
| SSRF guidance | — | — | — | — | base-URLs-not-barriers patterns · DIY · SEC-7 |
| File-upload hardening | FileServer `..` rejection; `os.Root` traversal/symlink-safe FS · STD · SEC-16 | filename sanitization, traversal, executable placement patterns · DIY · SEC-16 | — | — | content-type validation warnings; size limits · CORE · SEC-3/14 |
| Compromised-password check | — | — | `uncompromised()` HIBP rule · CORE · SEC-11 | — (CommonPasswordValidator · AUTH-36 (P7)) | — |
| Security scanning toolchain | native fuzzing + race detector in toolchain · TOOL · SEC-23/24 (govulncheck is x/ · SEC-22) | Brakeman + `bundler-audit` + `importmap audit` wired into generated CI · ECO · SEC-14 | — | — | — |
| Brute-force / CAPTCHA guidance | — (x/time/rate · SEC-18) | CAPTCHA/negative-captcha guidance · DIY/ECO · SEC-17 | rate limiter cross-ref · CORE · SEC-12 | — (explicit non-goal · SEC-16) | — |
| Threat-catalog documentation | — | guide-length threat manual (injection, header splitting, hijack flows) · CORE · SEC-19 | per-page gap documentation · CORE · SEC-12 | topics/security.md overview (section preamble; no dedicated row) | frank security guide + EEF hardening pointers · CORE · SEC-17 |

**Notable divergences.** All four agree on the safe-by-default core (auto-escaped templates, parameterized queries, CSRF for browser routes), but Laravel 13 is alone in flipping CSRF to `Sec-Fetch-Site` origin verification first with tokens as fallback — the other three remain token-first. Default security headers and CSP split the field: Rails, Phoenix, and Django all ship header middleware with CSP support in core (Phoenix even applies a baseline CSP when none is set), while Laravel ships no header middleware and documents CSP as a gap. Secrets are philosophical: Rails uniquely ships encrypted-credentials-in-repo; Laravel encrypts env files; Django and Phoenix defer entirely to the environment. Rails is alone in treating static security analysis (Brakeman in generated CI) and ReDoS (global regex timeout) as framework concerns, while Phoenix is alone in treating connection-level DoS (channel caps, slow-client body limits) as first-class — a direct consequence of its stateful-connection architecture. Django and Phoenix both publish explicit non-goals (throttling/object-perms/OAuth for Django; enumeration protection for Phoenix), which the other two do not.

**Volt notes.**
- Go starts ahead on two universal rows: `html/template` does contextual auto-escaping and `database/sql` placeholders parameterize queries — but Phoenix shows the bar is higher (making unsafe interpolation a *compile error*, not a runtime footgun), which Go's `vet`-style static tooling could match.
- Every framework derives cookies/signing/encryption from one app secret and three of four support graceful rotation via fallback key lists (`APP_PREVIOUS_KEYS`, `SECRET_KEY_FALLBACKS`, Rails rotations). Multi-key verify/single-key sign should be in Volt's crypto core from day one, not retrofitted.
- Laravel's `Sec-Fetch-Site`-first CSRF is the modern, mostly-stateless design a new framework can adopt outright, keeping tokens only as a fallback for old clients.
- Security-header + CSP middleware is now table stakes (3 of 4 in core, on by default via project template); Rails' Brakeman-in-CI precedent maps naturally to wiring `govulncheck`/`gosec` into Volt's generated CI rather than building a scanner.
- Go 1.25's `CrossOriginProtection` (SEC-4) is exactly the Sec-Fetch-Site-first CSRF design praised above, already in std — Volt needs only the legacy token fallback (SEC-5); and unlike all four peers, std terminates production TLS in-process with post-quantum defaults (SEC-9/10), so Caddy-style auto-HTTPS (ACME is x/-only, SEC-11) is a deployment differentiator none of them can offer.

## P9 — Background work: jobs, queues & scheduling

- **Go std**: no queue, no worker, no scheduler — but the strongest in-process substrate of the five: goroutines, channels + `select`, `WaitGroup.Go`, `context` cancellation and `time.Timer`/`Ticker` (JOB-1–10); everything durable (queue, retry, cron, results, transactional enqueue) is absent (JOB-15–20).
- **Rails**: Active Job as backend-agnostic declaration/enqueue API; Solid Queue (DB-backed, `FOR UPDATE SKIP LOCKED`) as the zero-extra-infrastructure CORE default, with a built-in recurring-task scheduler and Mission Control dashboard (OPT).
- **Laravel**: the maximal answer — driver-abstracted queue (Redis/DB/SQS/…) with the deepest job feature set of the four (uniqueness, debounce, middleware, chains, batches), a code-defined cron scheduler, and Horizon (OPT) as Redis dashboard/supervisor.
- **Django**: Tasks framework (new 6.0) — `@task` + `.enqueue()` + results over pluggable backends, but deliberately **no worker process, no durable queue, no scheduler** in core; those are ECO.
- **Phoenix**: ships **no job framework** — its largest deliberate gap. OTP supervised processes (P22) are the low-level answer; Oban is the blessed ECO answer.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Job/task declaration API | `go f()` per-job spawn · TOOL · JOB-1 | Active Job subclasses + generator · CORE · JOB-1 | Job classes `ShouldQueue` + `make:job` · CORE · JOB-3 | `@task` decorator → `Task` · CORE · JOB-1 | — (supervised GenServer · DIY · JOB-3; Oban · ECO · JOB-2) |
| Backend/driver abstraction | — | Pluggable adapters (Sidekiq, Resque, GoodJob…) + async/inline/test · CORE/ECO · JOB-17 | One API over database/redis/SQS/beanstalkd/sync/null · CORE · JOB-1 | Pluggable backends + `supports_*` feature flags · CORE · JOB-9 | — |
| Default durable backend shipped | — | Solid Queue (DB, SKIP LOCKED, separate queue DB) · CORE · JOB-3 | database/redis drivers · CORE · JOB-1 | — (only dev Immediate/Dummy · CORE · JOB-7/8; durable = ECO · JOB-10) | — (Oban ECO) |
| Worker process shipped | — (DIY: goroutines ranging over a channel · JOB-19) | `bin/jobs` or Puma plugin; topology in `queue.yml` · CORE · JOB-3/4 | `queue:work` daemon + `queue:listen` dev reload · CORE · JOB-18 | — by design; execution is external infra · ECO · JOB-10 | — (OTP processes stand in · DIY · JOB-3) |
| Backend failover | — | — | Ordered connection list, auto fallback on push failure · CORE · JOB-2 | — | — |
| Delayed/deferred execution | `time.Timer`/`AfterFunc`, in-process volatile · STD · JOB-6 | `set(wait:, wait_until:)` · CORE · JOB-2 | `delay()` / `withoutDelay` · CORE · JOB-8 | `run_after` via `Task.using` · CORE · JOB-3 (needs `supports_defer`) | — |
| Priorities | — | `queue_with_priority` within queues · CORE · JOB-6 | Worker `--queue` priority lists · CORE · JOB-18 | `priority` kwarg · CORE · JOB-1/3 | — |
| Named queues & routing | — (typed channels as in-memory queues · TOOL · JOB-2) | `queue_as` (static/block), name prefixes · CORE · JOB-5 | `onQueue/onConnection` + central `Queue::route` per class · CORE · JOB-8/12 | `queue_name` · CORE · JOB-1 | — |
| Retry/backoff policy | — (hand-rolled `for`+`Sleep` loops · JOB-16) | `retry_on` (wait/backoff/jitter) + `discard_on` · CORE · JOB-11 | `$tries`/`retryUntil`/`maxExceptions`/backoff arrays/timeouts · CORE · JOB-13 | — (attempt counter exposed via `TaskContext` · JOB-4; retry loop is the ECO worker's job) | — (supervisor restarts are process-level · CORE · BEAM-1/2) |
| Failed-job store & ops | — | `retry_on` dead-handler block · CORE · JOB-11; retry/discard UI · OPT · JOB-18 | `failed_jobs` table, retry/forget/flush/prune CLI, `failed()` hook · CORE · JOB-21 | `TaskResult` FAILED + `TaskError` traceback · CORE · JOB-5/6 | — |
| Job result retrieval | — (DIY result channels · JOB-18) | — | — (fire-and-forget; batches expose progress only) | `TaskResult`/`get_result`, statuses enum · CORE · JOB-5/6 | — (`Task.await` on BEAM · DIY · BEAM-5) |
| Unique jobs / dedup | — (x/sync/singleflight coalescing · X · JOB-12) | — | `ShouldBeUnique(UntilProcessing)`, SQS dedup · CORE · JOB-4/14 | — | — |
| Debounced jobs | — | — | `#[DebounceFor]` last-dispatch-wins · CORE · JOB-5 | — | — |
| Concurrency limiting / overlap control | in-process only: `sync` primitives · STD · JOB-8; `errgroup.SetLimit` · X · JOB-11 | `limits_concurrency to:, key:, group:` · CORE · JOB-8 | `WithoutOverlapping`, `RateLimited`, `ThrottlesExceptions` middleware · CORE · JOB-7 | — | — (process primitives · DIY · BEAM-5) |
| Job middleware / callbacks | — (`context.AfterFunc` cancellation hooks only · STD · JOB-5) | `before/around/after_enqueue`/`_perform` · CORE · JOB-12 | Custom + shipped middleware · CORE · JOB-7 | `TaskContext` only · CORE · JOB-4 | — |
| Bulk enqueue | — | `perform_all_later` → `enqueue_all` single round-trip · CORE · JOB-7 | `Bus::bulk` · CORE · JOB-9 | — | — |
| Chains (sequential workflows) | — | — | `Bus::chain` + `catch`, in-job append/prepend · CORE · JOB-11 | — | — |
| Batches (fan-out with completion callbacks) | in-process fan-out/join: `wg.Go`/`Wait` · STD · JOB-3 | — | `Bus::batch` (then/catch/finally, cancellation, nesting) · CORE · JOB-16 | — | — |
| Long-job resumability | — | Continuations: `step`/cursor resume after restarts · CORE · JOB-10 | — | — | — |
| Transaction-safe enqueue | — (no `database/sql` hooks · JOB-20) | `enqueue_after_transaction_commit` opt-in · CORE · JOB-15 | `after_commit` option + per-dispatch afterCommit · CORE · JOB-10 | `on_commit` pattern · CORE · JOB-2 | — |
| Argument/model serialization | — | GlobalID + custom serializers · CORE · JOB-13/14 | `SerializesModels` re-fetch on dequeue · CORE · JOB-3 | JSON-serializable args/returns · CORE · JOB-2 | — |
| Encrypted payloads | — | — | `ShouldBeEncrypted` · CORE · JOB-6 | — | — |
| Manual in-job control | — | — | `release($delay)`/`fail`/`delete` · CORE · JOB-15 | — | — |
| Recurring tasks / scheduler | `time.Ticker` in-process only, no cron/catch-up · STD · JOB-7 (JOB-17) | `config/recurring.yml` (Fugit), no OS cron · CORE · JOB-9 | Code-defined cron DSL (`routes/console.php`, `everySecond`…`cron()`) · CORE · JOB-27/28 | — (no scheduler in core) | — (`:telemetry_poller` interval MFA, measurement-oriented · CORE · JOB-4) |
| Multi-server schedule coordination | — | — | `withoutOverlapping`/`onOneServer` locks · CORE · JOB-29 | — | — |
| Scheduler execution & hooks | — | — | Single cron → `schedule:run`, sub-minute, output capture, HTTP pings · CORE · JOB-30 | — | — |
| Ops dashboard | — | Mission Control — Jobs (inspect/retry/discard) · OPT · JOB-18 | Horizon (Redis-only): supervisors-as-code, balancing, tags, metrics, alerts · OPT · JOB-25/26 | — | — (`forward "/jobs"` to external job UI · DIY · JOB-5) |
| Worker lifecycle ops | `signal.NotifyContext` + WaitGroup drain pattern · STD · JOB-14 | Process/thread/signal lifecycle documented · CORE · JOB-4 | `queue:restart/pause/resume`, signal handlers · CORE · JOB-19; Supervisor recipe · DIY · JOB-20 | — | Supervision tree restarts, no external PM needed · CORE · BEAM-1/2 |
| Queue monitoring/alerts | — | — | `queue:monitor` → `QueueBusy` event; Horizon long-wait alerts · CORE/OPT · JOB-22/26 | — | — |
| Lifecycle events / error reporting | — | Wrapped in `Rails.error`, locale propagation · CORE · JOB-16 | `JobQueued/Processing/Processed/Failed` events · CORE · JOB-24 | Errors on `TaskResult` · CORE · JOB-5/6 | — (Oban telemetry · ECO · JOB-2) |
| Testing support | — | `:test`/`:inline` adapters · CORE · JOB-17 | `Queue::fake`/`Bus::fake`, chain/batch assertions · CORE · JOB-23 | Immediate/Dummy backends · CORE · JOB-7/8 | — |
| In-process parallel execution helper | goroutines/channels/`select` native · TOOL · JOB-1/2 (+ `errgroup` · X · JOB-11) | — | `Concurrency::run/defer` (child processes) · CORE · JOB-31 | — | GenServer/Task/OTP native · CORE · BEAM-5 |
| One-off prod tasks | — | — | — (CLI covered elsewhere) | — | Release `eval` / custom commands, minimal-boot pattern · CORE · JOB-6 |

**Notable divergences.** This is the section where the four disagree most on *what belongs in a framework at all*: Laravel says everything (31 CORE-heavy rows, from debouncing to circuit-breaker middleware), Rails says the essential 80% with zero extra infrastructure (Solid Queue over the DB you already have), Django says only the *interface* (define/enqueue/result — the worker is explicitly someone else's product), and Phoenix says nothing (the runtime's processes are the primitive; a queue is an ecosystem concern, Oban). Durability defaults split the same way: Rails is durable out of the box, Laravel needs you to pick a driver, Django and Phoenix are not durable at all without third parties. Scheduling repeats the pattern — Laravel a rich code-defined cron DSL with multi-server locks, Rails a declarative YAML scheduler, Django and Phoenix nothing. Workflow orchestration (chains, batches, unique/debounced jobs) is Laravel-only; Rails's unique contribution is instead Continuations (resumable long jobs), which nobody else has.

**Volt notes.**
- Goroutines dissolve the "move work off the request path" motivation (Phoenix proves a concurrent runtime does), but they do not provide the *durability* half: retries across restarts, dead-letter stores, and schedulers still need a queue. Rails's DB-backed Solid Queue (`SKIP LOCKED`) shows a zero-new-infrastructure durable default is possible and popular — a natural fit for Go.
- Django's split is instructive: a small, stable define/enqueue/result API with backend feature flags, letting the ecosystem compete on workers. A Go framework could ship the interface + a DB backend and still swap in River/Asynq-style engines.
- The union feature list is dominated by Laravel one-offs (debounce, encryption, batches-with-callbacks, failover drivers); Rails demonstrates which subset (retry/backoff, priorities, bulk enqueue, concurrency limits, transactional enqueue, recurring tasks, dashboard) is enough for a defaults-first framework.
- Transactional enqueue (enqueue-after-commit) appears in all three frameworks that have jobs — table stakes when the queue and the app DB can diverge, and free when the queue *is* the app DB.
- Phoenix's supervision substrate (BEAM-1/2) has no Go equivalent out of the box: goroutines aren't supervised or isolated, so "just spawn a goroutine" loses crash containment and restart that OTP gives Elixir. A Volt answer needs explicit lifecycle/panic-recovery machinery.
- Go-baseline check (gostd P9): `database/sql` exposes no commit hooks (JOB-20), so even the "free when the queue *is* the app DB" transactional-enqueue story isn't free in Go — Volt must own the transaction wrapper to offer `on_commit` semantics at all.

## P10 — Real-time: websockets & server push

- **Go std**: no WebSockets at all (LIVE-7) — but SSE and long-polling fall directly out of the Handler model (`Flusher`, `ResponseController` deadlines, disconnect-canceled contexts), with `Hijacker` as the raw-conn escape hatch; everything above the transport (hubs, presence, channels, clients) is absent.
- **Rails**: Action Cable — full-stack in-process WebSockets with Ruby channel classes and cookie-shared auth, defaulting to DB-backed Solid Cable pub/sub (no Redis); Turbo Streams rides on top.
- **Laravel**: event broadcasting — server events flow through a driver (first-party Reverb websocket server · OPT, or Pusher/Ably · ECO) to the Echo JS client; SSE is the core-only fallback.
- **Django**: nothing in core — ASGI + async streaming responses give SSE/long-poll; WebSockets are ecosystem (Channels, outside the corpus).
- **Phoenix**: the deepest answer, in three layers on the BEAM: PubSub (zero-broker cluster messaging), Channels (process-per-client topic sockets, documented wire protocol), LiveView (server-rendered reactive UI over the channel transport), plus CRDT Presence.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| WebSocket server | — (`Hijacker` raw-conn escape hatch · STD · LIVE-6; x/net/websocket frozen · LIVE-7) | Action Cable mounted at `/cable` or standalone · CORE · LIVE-1/8 | Reverb standalone (Pusher protocol, multi-app, SSL) · OPT · LIVE-2 | — · ECO · LIVE-4 (ASGI servers speak it; core has no API) | `socket` endpoint declaration · CORE · LIVE-1 |
| Long-poll transport fallback | block in handler; ctx canceled on disconnect · STD · LIVE-3 | — | — | — | Built-in per-socket, `longPollFallbackMs` + clustering guidance · CORE · LIVE-1/47 |
| SSE / one-way HTTP streaming | `http.Flusher` hand-written frames + `ResponseController` deadlines · STD · LIVE-1/2 | `ActionController::Live` · CORE · LIVE-11 | `eventStream` + `useEventStream` · CORE · LIVE-14 | `StreamingHttpResponse` async iterator on ASGI · OPT/CORE · LIVE-1/3 | — (LiveView occupies this niche) |
| Connection authentication | — | `identified_by`, shares session cookies · CORE · LIVE-1 | Auth via channel routes/guards · CORE · LIVE-6 | — | `connect/2..3` + `Phoenix.Token` `auth_token` (tokens over cookies) · CORE · LIVE-2/6 |
| Channel/topic abstraction | — (DIY hub · LIVE-13) | Channel classes + `stream_from`/`stream_for` · CORE · LIVE-2/4 | Public/private/presence channels · CORE · LIVE-1 | — | `channel "room:*"` wildcard routing, process per client-topic · CORE · LIVE-2/3 |
| Per-channel authorization | — | Subscription rejection · CORE · LIVE-2 | Closures/classes in `routes/channels.php`, model binding · CORE · LIVE-6 | — | `join/3` per topic · CORE · LIVE-3 |
| Server broadcast API | — (DIY map+mutex hub · LIVE-13) | `ActionCable.server.broadcast` / `broadcast_to` from anywhere · CORE · LIVE-4/5 | `ShouldBroadcast` events, `Broadcast::on` anonymous · CORE · LIVE-1/7/8 | — | `broadcast!/3` + `Phoenix.PubSub` cluster-wide · CORE · LIVE-4/10 |
| Client→server RPC | — (full-duplex HTTP/1 via `EnableFullDuplex` · STD · LIVE-5) | `perform` → channel actions · CORE · LIVE-3 | — (Echo is subscribe/listen) | — | `handle_in/3` + sync replies · CORE · LIVE-3/4 |
| Client-to-client events (no server hop) | — | — | Whisper events · CORE · LIVE-11 | — | — |
| Per-recipient broadcast filtering | — | — | `toOthers()` exclude-sender, `via()` · CORE · LIVE-8 | — | `intercept` + `handle_out/3` customize per recipient · CORE · LIVE-5 |
| Pub/sub backplane | — | Solid Cable (DB) default; Redis, PG NOTIFY, async/test adapters · CORE · LIVE-6 | Redis pub/sub for Reverb horizontal scale · OPT · LIVE-3 | — | PG2 native distribution default; Redis adapter; `local_broadcast` · CORE · LIVE-10 |
| Zero-extra-infra default | SSE/long-poll zero-infra · STD · LIVE-1/3 | Solid Cable over existing DB · CORE · LIVE-6 | — (needs Reverb process or hosted service) | — | BEAM clustering, no broker at all · CORE · LIVE-10, BEAM-3/4 |
| Presence ("who's online") | — | — | Presence channels, here/joining/leaving · CORE · LIVE-9 | — | `Phoenix.Presence` CRDT-replicated, self-healing, no external dep; fetch/handle_metas hooks; JS sync class · CORE · LIVE-11/12/13 |
| Official JS client | — | `@rails/actioncable` · CORE · LIVE-3 | Echo (`laravel-echo`) · CORE · LIVE-4 | — | `phoenix` npm package · CORE · LIVE-8 |
| React/Vue integration hooks | — | — | `useEcho/useEchoPresence/useEchoModel` · CORE · LIVE-5 | — | — (LiveView is the alternative) |
| Documented wire protocol / polyglot clients | — | — | Pusher protocol compat (any Pusher client) · OPT · LIVE-2 | — | V2 JSON protocol documented; Swift/Java/Kotlin/C#/Elixir clients · CORE · LIVE-8 |
| Model-driven auto-broadcast | — | `stream_for @post` / `broadcast_to` model-scoped · CORE · LIVE-4 | `BroadcastsEvents` trait on CRUD · CORE · LIVE-10 | — | — |
| HTML-over-the-wire DOM updates | — | Turbo Streams over Action Cable · CORE · LIVE-9 | — (Livewire is ecosystem, outside corpus) | — | LiveView minimal template diffs · CORE · LIVE-14/17 |
| Server-rendered reactive UI framework | — | — | — | — | LiveView: process-per-view, HTTP first paint, `mount`/`handle_event`/`handle_info`, change-tracked assigns · CORE · LIVE-14–18 |
| Stateful server components / nesting | — | — | — | — | LiveComponent (in-process) + nested LiveViews (isolated process) · CORE · LIVE-21/22 |
| Declarative client bindings & rate limiting | — | — | — | — | `phx-click/keydown/…` + `phx-debounce`/`phx-throttle` · CORE · LIVE-23/24 |
| Server↔client JS command channel | — | — | — | — | `Phoenix.LiveView.JS` commands, `push_event`, hooks lifecycle, colocated JS/hooks · CORE · LIVE-25/28/29/30 |
| Forms over socket + crash recovery | — | — | — | — | `<.form>` change/submit, in-flight sync guarantees, auto form recovery on reconnect · CORE · LIVE-31–34 |
| Large-collection streaming / infinite scroll | — | — | — | — | `stream/3` + `phx-update="stream"` + viewport bindings · CORE · LIVE-19/20 |
| File uploads over the socket | — | — | — | — | `allow_upload`, progress, drag-drop, direct-to-cloud presign · CORE · LIVE-36/37 |
| Live navigation (patch/navigate) | — | — | — | — | `push_patch`/`push_navigate`, loading events, `@page_title` · CORE · LIVE-38–40 |
| Reliability semantics documented | disconnect ⇒ `r.Context()` canceled · STD · LIVE-3 (no SSE `Last-Event-ID` · LIVE-15) | — | — | Disconnect via `asyncio.CancelledError` · OPT · LIVE-2 | At-most-once delivery stated; client backoff/rejoin, PushBuffer outbox; catch-up is app code · CORE · LIVE-7 |
| Crash/deploy recovery posture | `Shutdown` skips hijacked conns; drain via `RegisterOnShutdown` · STD · LIVE-11 | — | — | — | Post-mount crash → process restart + live remount; state-in-URL/DB guidance; `static_changed?` · CORE · LIVE-41–43 |
| Origin/security controls | — | Allowed request origins allowlist (regex) · CORE · LIVE-7 | Reverb allowed origins · OPT · LIVE-2 | — | — (not in corpus) |
| Hosted-service alternative | — | — | Pusher Channels / Ably recipes · ECO · LIVE-13 | — | — |
| Horizontal scaling story | — (goroutine-per-conn scales vertically · TOOL · LIVE-4) | — (adapter-dependent) | Reverb + Redis pub/sub, ext-uv tuning, nginx recipe · OPT · LIVE-3 | — | Add nodes; native distribution, "2M websocket connections" · CORE · BEAM-3 |
| Notifications over websocket | — | — | Broadcast notification channel + `Echo.notification()` · CORE · LIVE-12 | — | — |
| Testing support | `net.Pipe` in-memory conn pair (+ synctest) · STD · LIVE-16 | Connection/channel test cases, `assert_broadcast_on` · CORE · LIVE-10 | — (not in corpus for broadcasting) | — | Generators emit channel tests · CORE · LIVE-9 |
| Dev/debug tooling | — | — | Reverb debug/restart commands · OPT · LIVE-2 | — | `enableDebug()`, built-in latency simulator; LiveDebugger · CORE/ECO · LIVE-44/46 |
| Scaffolding/generators | — | — | `install:broadcasting` · CORE · LIVE-1 | — | `mix phx.gen.socket/channel/presence` · CORE · LIVE-9/12 |

**Notable divergences.** Four entirely different architectures: Rails runs the socket *inside* the app process with channels as Ruby classes; Laravel externalizes it — the framework only *broadcasts events at* a separate driver process (Reverb) or a paid service, and the deep logic lives in the JS client; Phoenix makes the socket the center of the programming model (a process per client, and LiveView turning real-time into the default UI paradigm — 47 rows, more than the other three combined); Django simply refuses, offering ASGI/SSE plumbing and delegating WebSockets wholesale to the ecosystem. The backplane question splits identically to P9: Phoenix needs nothing (native clustering + CRDT presence), Rails defaults to the database (Solid Cable), Laravel reaches for Redis, Django abstains. Presence exists only in Laravel (server-assisted channel type) and Phoenix (masterless CRDT); Rails and Django have no answer. Phoenix is also alone in documenting delivery semantics (at-most-once, catch-up is your code) — the others don't state guarantees at all.

**Volt notes.**
- Go is unusually strong here: goroutine-per-connection is the native idiom (gorilla/nhooyr websockets scale like BEAM processes for this purpose), so a CORE in-process websocket layer à la Action Cable/Channels is cheap — the hard part Go lacks is Phoenix's *cluster* story (PG2, CRDT presence); any multi-node Volt needs an explicit backplane (Redis/NATS/Postgres) like Rails/Laravel.
- Channels-style topic routing + per-topic auth (join callback) is the convergent abstraction across all three implementers — a stable design target independent of transport.
- LiveView is the outlier bet: 30+ of Phoenix's rows only exist because server-rendered reactive UI is in scope. That's a product decision, not a websocket-layer decision — templ/datastar-style HTML-over-the-wire could be layered later if the pub/sub + socket substrate exposes the right hooks (cf. Turbo Streams riding on Action Cable).
- Explicitly documenting delivery guarantees (Phoenix's at-most-once + client outbox + rejoin) is rare and valuable; silence elsewhere pushes users to discover semantics in production.
- Go-baseline check (gostd P10): production streaming has std-level traps the frameworks never surface — the server-wide `WriteTimeout` kills long-lived responses unless each stream clears it via `ResponseController` (LIVE-2), and `Shutdown` neither closes nor waits for hijacked conns (LIVE-11) — deadline and drain plumbing Volt must own beneath any channel abstraction.

## P11 — Mail & notifications

- **Go std**: protocol bricks only — RFC 5322 parsing, a frozen `net/smtp` client (STARTTLS, PLAIN/CRAM-MD5), and all the MIME encoders needed to hand-assemble a message; no builder, no backend abstraction, no dev capture, no notification concept (MAIL-13–18).
- **Rails**: Action Mailer mirrors controllers (classes, views, layouts, previews) with Active Job delivery; Action Mailbox handles *inbound* email; non-email channels are an acknowledged gap (DIY).
- **Laravel**: Mailable classes (envelope/content/attachments) over Symfony Mailer transports, plus a full parallel Notification abstraction — one class fanned out to mail/database/broadcast/SMS/Slack channels.
- **Django**: `django.core.mail` — `EmailMessage` over pluggable backends, 6.1's named `MAILERS` config; console/file/locmem backends for dev/test; no notification framework (flash messages only).
- **Phoenix**: Swoosh wired into every new app with a dev mailbox; production adapters and everything beyond email are explicitly your integration work.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Transport/backend abstraction | — (bare frozen `net/smtp` only; PLAIN/CRAM-MD5, no XOAUTH2 · STD · MAIL-4–6) | SMTP/sendmail/file/test delivery methods · CORE · MAIL-9 | Symfony transports: SMTP/SES/Postmark/Resend/Mailgun/sendmail/log/array · CORE · MAIL-1 | SMTP/console/file/locmem/dummy backends · CORE · MAIL-7–11; ESP APIs · ECO · MAIL-12 | Swoosh adapters (prod adapter wiring is your work) · CORE · MAIL-1/3 |
| Named multi-mailer config | — | Per-delivery `delivery_method_options` · CORE · MAIL-9 | Per-mailer config, per-mailer send · CORE · MAIL-1/6 | `MAILERS` setting + `using=` (new 6.1) · CORE · MAIL-14/17 | — |
| Transport resilience | — | — | `failover` + `roundrobin` mailers · CORE · MAIL-2 | — | — |
| Message composition class | — (the core gap · MAIL-13) | Mailer classes mirroring controllers · CORE · MAIL-1 | Mailables: `envelope()`/`content()` fluent API · CORE · MAIL-3 | `EmailMessage` (cc/bcc/reply-to/headers) · CORE · MAIL-4 | Swoosh compose/deliver interface · CORE · MAIL-1 |
| Template-rendered bodies | `html/template` for the body; no mail layer · STD · MAIL-12 | Per-action ERB views, layouts, fragment caching · CORE · MAIL-1/7 | Blade views + view data · CORE · MAIL-3 | — (string bodies; template rendering is your call) | — (notifier modules compose in code) · MAIL-3 |
| HTML+text multipart | hand-assembled `mime/multipart.Writer` · STD · MAIL-8 | Implicit multipart from sibling `.html/.text` templates · CORE · MAIL-2 | View/text/`htmlString` in `content()` · CORE · MAIL-3 | `EmailMultiAlternatives`/`content_subtype` · CORE · MAIL-5/6 | — |
| Pre-styled component templates | — | — | Markdown mailables (`<x-mail::button>` etc.), themeable · CORE · MAIL-5 | — | — |
| Attachments & inline embeds | — (raw MIME bricks: multipart/base64/qp · STD · MAIL-8–10) | Attachments incl. inline cid URLs · CORE · MAIL-5 | Path/disk/raw + inline embeds + `Attachable` models · CORE · MAIL-4 | `attach()`/`attach_file()`, MIMEPart · CORE · MAIL-4 | — |
| Queued/async delivery | — (bare goroutines, no retry/outbox · MAIL-15) | `deliver_later` via Active Job · CORE · MAIL-3 | `queue()`/`later()`, `ShouldQueue`, afterCommit · CORE · MAIL-6 | — (via task-queue backends · ECO · MAIL-12) | — |
| Bulk sending / connection reuse | `smtp.Client` session reuse · STD · MAIL-4 | — | Collection recipients · CORE · MAIL-6 | `send_mass_mail`, `open()`/`close()` reuse · CORE · MAIL-2/15 | — |
| Dev preview / capture | — | `/rails/mailers` preview UI · CORE · MAIL-11 | Route-return browser preview, log mailer, Mailpit, universal `to` · CORE · MAIL-7/10 | Console/file backends · CORE · MAIL-8/9 | `/dev/mailbox` viewer · CORE · MAIL-2 |
| Test doubles & assertions | — | `test` delivery method · CORE · MAIL-9 | `Mail::fake`, content/attachment assertions · CORE · MAIL-9 | locmem → `mail.outbox` · CORE · MAIL-10 | — (Swoosh "testing" per glossary · MAIL-1) |
| Localized sending | — | i18n subject lookup · CORE · MAIL-10 | `locale()`, `HasLocalePreference` · CORE · MAIL-8 | — | — |
| Hooks: intercept/observe/events | — | Interceptors + observers; `before/after_action`, `after_deliver`, `rescue_from` · CORE · MAIL-8/12 | `MessageSending/Sent` events · CORE · MAIL-11 | — | — |
| Custom transport extension | — | — | `Mail::extend`, extra Symfony transports · CORE · MAIL-11 | Subclass `BaseEmailBackend` · DIY · MAIL-13 | Swoosh adapter integration · CORE · MAIL-3 |
| Header-injection protection | `Address.String()` RFC 2047-safe formatting · STD · MAIL-1 | — (not in corpus) | — (not in corpus) | CRLF `ValueError`, safe `Address` building · CORE · MAIL-16 | — |
| URL generation in mail | — | `default_url_options[:host]`, asset host · CORE · MAIL-6 | — | — | — |
| Admin/ops mail shortcuts | — | — | — | `mail_admins`/`mail_managers` · CORE · MAIL-3 | — |
| Auth-flow email scaffolding | — | — | — (covered by starter kits, P7) | — | `phx.gen.auth` notifier modules (confirm/magic-link/reset) · CORE · MAIL-3 |
| **Inbound** email processing | parse-only: `ReadMessage` + header/address access · STD · MAIL-2 (no IMAP/POP3 · MAIL-17) | Action Mailbox: routing DSL, Mailgun/Postmark/SendGrid/relay ingresses, lifecycle state machine, bounce replies, incineration, conductor test UI · CORE · MAIL-13–16 | — | — | — |
| Multi-channel notification abstraction | — | — · DIY · MAIL-17 (named gap) | Notifications: one class, `via()` → mail/database/broadcast/SMS/Slack · CORE · MAIL-12 | — (contrib.messages flash only) | — · MAIL-5 (named gap: "integrate it yourself") |
| In-app/database notifications | — | — · DIY · MAIL-17 | Database channel, unread tracking, mark-as-read · CORE · MAIL-16 | — | — |
| WebSocket notification delivery | — | — | Broadcast channel + `Echo.notification()` · CORE · MAIL-12 (LIVE-12) | — | — |
| SMS channel | — | — · DIY · MAIL-17 | Vonage channel pkg · OPT · MAIL-17 | — | — · MAIL-5 |
| Chat (Slack) channel | — | — | Block Kit builder, interactivity · OPT · MAIL-18 | — | — |
| On-demand notify (non-users) | — | — | `Notification::route(...)->notify()` · CORE · MAIL-13 | — | — |
| Queued notifications, per-channel tuning | — | — | Per-channel queue/delay maps, `shouldSend` cancel · CORE · MAIL-14 | — | — |
| Rich per-channel message builders | — | — | `MailMessage` fluent lines/actions, full Mailable substitution · CORE · MAIL-15 | — | — |
| Notification testing/localization/custom channels | — | — | `Notification::fake`, locale, custom channel classes; community channel matrix · CORE/ECO · MAIL-19 | — | — |

**Notable divergences.** Outbound email is the one place all four agree on shape (message class over pluggable transports with dev/test backends) and differ only in depth — Laravel adds transport failover and styled markdown components, Django adds multi-mailer config and header-injection hardening, Phoenix ships only the thinnest wiring. The real fault line is everything *around* email: Laravel is alone with a first-class multi-channel notification framework (database, websocket, SMS, Slack, on-demand routing) — Rails and Phoenix both explicitly name this a gap, and Django doesn't even frame the problem. Rails is alone in the opposite direction: Action Mailbox makes *inbound* email a framework concern (ingress webhooks, lifecycle state machine, test conductor), which no other corpus touches. Template philosophy also splits: Rails/Laravel render mail through the same view layer as pages; Django and Phoenix treat bodies as strings you assemble.

**Volt notes.**
- The convergent core is small and clear: message struct + pluggable transports + console/memory dev-test backends + a browser preview (all four have a preview/capture story — the single most universal DX feature in this section).
- Laravel's notification layer is really a *routing* abstraction (notifiable → channel → per-channel renderer) sitting on top of mail/broadcast/queue subsystems; it's only possible because those substrates exist first — sequencing matters for Volt.
- Queued delivery is the standard bridge between P11 and P9 (Laravel `ShouldQueue`, Rails `deliver_later`); if Volt has any job story, mail should ride it rather than growing its own async path. In Go the tempting shortcut — fire a goroutine — loses retries and observability, the exact things the queue bridge buys.
- Inbound email (Action Mailbox) is a genuine differentiator nobody copied; its ingress-webhook + normalize + route design is transport-agnostic and would port cleanly to Go, but it drags in storage (raw eml) and lifecycle-state dependencies.
- Go-baseline check (gostd P11): the four frameworks wrap living mailer libraries, but `net/smtp` is officially frozen and lacks LOGIN/XOAUTH2 (MAIL-4/6), so even Volt's minimal transport is a build-or-adopt (go-mail) decision rather than a thin std wrapper.

## P12 — Caching & performance

- **Go std**: no cache framework at any granularity — no unified store API, no per-view/fragment caching, no Redis client, no TTL store; what ships instead is raw material (sync.Map/Pool/Once, atomics, maphash), conditional GET built into `ServeContent`, and a best-in-class profiling/observability stack (pprof, runtime/metrics, flight recorder, PGO).
- **Rails**: layered *view* caching (fragment/russian-doll/collection) over a pluggable `Rails.cache`, with database-backed Solid Cache as the default store, HTTP conditional GET as core, and a documented Puma/GVL tuning playbook.
- **Laravel**: unified multi-store cache API (redis/memcached/db/file/dynamodb/…) with stampede protection, atomic locks and a general rate limiter built on it, plus Octane resident-process serving as the throughput answer.
- **Django**: one multi-backend cache API used at four granularities (per-site, per-view, template fragment, low-level) plus the richest HTTP cache-header/middleware toolkit (Vary, Cache-Control, conditional GET, gzip).
- **Phoenix**: deliberately no cache framework — performance is structural (precompiled templates/routes, LiveView diffs, BEAM concurrency); ETS is the raw in-memory store when needed.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Cache backends behind one API | — (headline gap: no Cache interface, no clients) · CACHE-1 | Solid Cache (DB, default) + Memory/File/MemCache/Redis/Null stores · CORE/ECO · CACHE-7/8 | redis/memcached/db/file/dynamodb/mongodb/array/null · CORE · CACHE-1 | locmem (default)/db/file/redis/memcached/dummy · CORE/OPT · CACHE-1–6 | — (no cache framework; explicit gap) · CACHE-1 |
| Default-store philosophy | sync.Map/Pool/Once as raw in-process substrate · STD · CACHE-4–6 | DB-backed Solid Cache: disk-size economics over RAM · CORE · CACHE-7 | config-selected, `Cache::store()` per call · CORE · CACHE-1 | per-process LRU LocMemCache · CORE · CACHE-5 | ETS in-memory terms as BEAM-native answer · CORE · CACHE-6 |
| Failover / fallback store | — | Redis store failure handling only · CORE · CACHE-8 | ordered store list, auto fallback (new in 13) · CORE · CACHE-2 | — | — |
| Low-level read/write API | sync.Map Load/Store/LoadOrStore (no TTL) · STD · CACHE-4 | fetch/read/write/delete/exist? + expires_in · CORE · CACHE-5 | get/put/pull/many/incr/forget/flush, TTL types · CORE · CACHE-3 | set/get/add/get_or_set/*_many/touch/incr/clear · CORE · CACHE-12 | — (raw ETS ops) · CACHE-6 |
| Compute-through + stampede protection | — (x/sync/singleflight) · CACHE-8 | `fetch` with `race_condition_ttl` · CORE · CACHE-5 | remember/rememberForever, `flexible` stale-while-revalidate w/ bg refresh · CORE · CACHE-4 | `get_or_set` (no SWR/stampede story) · CORE · CACHE-12 | — |
| TTL extension without re-write | — (no TTL store at all) · CACHE-9 | — | `Cache::touch` (new in 13) · CORE · CACHE-5 | `touch` · CORE · CACHE-12 | — |
| In-request memoization | sync.Once/OnceValue lazy init (process-lifetime, not per-request) · STD · CACHE-6 | per-request SQL query cache · CORE · CACHE-6 | `Cache::memo()` memory layer over any store · CORE · CACHE-6 | `cached_property` · CORE · CACHE-19 | — (assigns/process state idiomatic) |
| Grouped invalidation: tags | — | — (key versioning instead) | tag-scoped write/flush (redis/memcached/dynamodb) · CORE · CACHE-7 | — | — |
| Key versioning / prefixing | — (maphash/fnv for key routing only) · CACHE-10 | versioned keys, model `cache_key_with_version` · CORE · CACHE-1/5 | — (tags instead) | `version` arg + incr/decr_version, KEY_PREFIX/KEY_FUNCTION · CORE · CACHE-13 | — |
| Atomic/distributed locks | — (in-process sync.Mutex only; nothing distributed) | — | `Cache::lock` w/ owner tokens, block, forceRelease · CORE · CACHE-8 | — | — (BEAM processes serialize state instead) |
| Concurrency limiting / funnels | — (x/sync/semaphore) · (P9) JOB-12 | — | `withoutOverlapping` + funnel slots on locks · CORE · CACHE-9 | — | — |
| Rate limiting (server-side) | — (x/time/rate token bucket) · (P8) SEC-18 | controller `rate_limit` backed by cache store · CORE · (P2) CTRL-15 | general `RateLimiter` facade on cache · CORE · CACHE-11 | — (explicitly not provided) · (P8) SEC-16 | — (explicit gap; client-side debounce only) · API-13, LIVE-24 |
| Fragment caching in views | — (no cache action in html/template) · CACHE-3 | fragment + russian-doll + collection multi-fetch, template-digest busting · CORE · CACHE-1–4 | — | `{% cache %}` tag + fragment key helper · CORE · CACHE-11 | — (LiveView diff engine replaces it) · CACHE-3 |
| Per-view / per-page caching | — (no middleware, no key derivation) · CACHE-2 | — (page/action caching removed from core; non-goal) | — | `cache_page` decorator + per-site middleware pair · CORE · CACHE-9/10 | — |
| HTTP conditional GET (ETag/Last-Modified) | ServeContent: If-None-Match/If-Modified-Since/If-Range → 304, auto Last-Modified · STD · CACHE-12 | `stale?`/`fresh_when`, strong/weak ETags, Rack::ETag · CORE · CACHE-9 | — | ConditionalGetMiddleware + per-view `condition` · OPT · CACHE-20 | — (only default `max-age=0, private` header) · CACHE-4 |
| Cache-Control / Vary header helpers | manual Header().Set only; no helpers/defaults · STD · CACHE-13 | `http_cache_forever`, ConditionalGet middleware · CORE · CACHE-9 | — | `cache_control`/`never_cache`/`vary_on_*` decorators + patch helpers · CORE · CACHE-15–17 | — |
| Shared/proxy HTTP caching | — (ReverseProxy forwards, never stores) · CACHE-16 | Rack::Cache backed by Rails cache · OPT · CACHE-10 | — | downstream Expires/Cache-Control auto-set · CORE · CACHE-16 | — |
| Response compression | — (gzip codec only; no Accept-Encoding middleware) · CACHE-14 | Thruster front proxy (compression + X-Sendfile) · CORE · CACHE-13 | — | GZipMiddleware w/ BREACH mitigation · OPT · CACHE-21 | — |
| Static-asset cache headers / fingerprinting | — (FileServer inherits conditional GET; no fingerprinting) · CACHE-12 | Propshaft far-future fingerprints · CORE · CACHE-14 | — (Vite; outside P12) | ManifestStaticFilesStorage content hashes · CORE · CACHE-22 | `mix phx.digest` manifest + digests · CORE · CACHE-4 |
| Compile/deploy-time caches | — (templates/routes parsed at runtime; the binary itself is AOT) | — | config/route/view/event caching via `optimize` · CORE · CACHE-15 | cached template loader (auto) · CORE · CACHE-23 | templates & routes precompiled by design · CORE · CACHE-2/5 |
| Runtime/app-server acceleration | natively resident + concurrent; pprof/metrics/FlightRecorder + PGO tuning stack · STD/TOOL · CACHE-17–21/25 | Puma workers-vs-threads/GVL tuning playbook · CORE · CACHE-12 | Octane resident workers (FrankenPHP/Swoole/RoadRunner) + state pitfalls + Swoole extras · OPT · CACHE-12–14 | — (WSGI/ASGI left to deployment) | — (BEAM is natively resident/concurrent) · CACHE-3 |
| Async cache API | — (goroutines subsume it) | — | — | a-prefixed variants (aget/aset/…) · CORE · CACHE-14 | — (everything is async on BEAM) |
| Cached sessions backend | — | — (not in P12) | — (not in P12) | cached-sessions backend pointer · CORE · CACHE-18 | — |
| Retention/culling/encryption of store | — (sync.Map never evicts) · CACHE-9 | Solid Cache max_age/max_size, optional encryption, sharding · CORE · CACHE-7 | — | MAX_ENTRIES/CULL_FREQUENCY tuning · CORE · CACHE-8 | — |
| Custom store + cache events | — | custom store API · CORE · CACHE-8 | `Cache::extend`, hit/miss/write events · CORE · CACHE-10 | dotted-path custom backend · DIY · CACHE-7 | — |
| Dev-mode cache toggle | — | `bin/rails dev:cache` · CORE · CACHE-11 | array/null stores · CORE · CACHE-1 | DummyCache · CORE · CACHE-6 | — |
| **Full-text search (cluster; Laravel P22 / Django P23)** | | | | | |
| Database full-text search | — (database/sql passes raw SQL through) | PG full-text query *patterns* only (non-goal beyond) · CORE · ORM-49 | `whereFullText` → MATCH/tsvector, fullText indexes · CORE · SRCH-1 | SearchVector/Query/Rank/Headline, Lexeme; PG-only · OPT · PG-4 | — (no search rows in corpus) |
| Search-engine index sync framework | — | — (explicit non-goal) | Scout: Searchable trait, engines (db/collection/Algolia/Meilisearch/Typesense), ops + querying · OPT/ECO · SRCH-5–8 | — (ecosystem; absent from corpus) | — |
| Vector / semantic search | — | — | `vector()` cols + HNSW, `whereVectorSimilarTo`, embeddings + AI rerank · CORE/OPT · SRCH-2–4, SRCH-9 | — | — |
| Fuzzy match: trigram / unaccent | — | — | — | Trigram similarity lookups + `unaccent` · OPT · PG-5/6 | — |

**Notable divergences.** Laravel and Django converge on "one KV cache API over many backends," but disagree on invalidation (Laravel tags, Django versioned/prefixed keys) and on what to build atop it (Laravel: locks, funnels, rate limiting; Django: HTTP middleware layers). Rails' center of gravity is entirely different — the view layer (russian-doll fragment caching with automatic digest busting) plus a contrarian default store (Solid Cache betting on cheap disk over RAM). Phoenix flatly refuses the whole category: no cache store, no fragment cache, no HTTP caching — its inventory records this as deliberate, arguing precompiled templates, LiveView diffs, and BEAM concurrency remove the need. HTTP conditional GET is core in Rails, opt-in middleware in Django, and absent in Laravel and Phoenix. Runtime acceleration is a tier tell: Laravel must sell Octane (OPT) to escape per-request PHP, Rails documents tuning around the GVL, Phoenix gets it free, Django doesn't engage. On search, Laravel is far ahead (DB FTS + pgvector + Scout as a layered story); Django offers PG-native FTS/trigram in contrib; Rails names FTS a non-goal; Phoenix has nothing.

**Volt notes.**
- Go is already the "Octane runtime" — resident process, real concurrency — so the entire acceleration tier (Octane, Puma tuning) is moot; what remains contested and relevant is the store API + stampede protection (Laravel `flexible` SWR, Rails `race_condition_ttl`), which maps naturally onto `singleflight`.
- Tags vs versioned-keys is a genuine fork with backend implications: tags need backend support (Laravel limits them to 3 stores), versioning works on any KV store.
- HTTP-layer caching (ETag/conditional GET, Cache-Control helpers) is cheap, backend-free, and shipped by the two batteries-included frameworks — natural middleware territory in Go.
- Cache-backed locks/rate-limiting (Laravel-only combo) matters more for Go's multi-instance deployments than it did for PHP; Phoenix's "the runtime is the cache" stance is the counter-argument that a framework store can be skipped entirely.
- Go-std baseline reframe: conditional GET is the one HTTP-cache layer already in std — `ServeContent` evaluates If-None-Match/If-Modified-Since/If-Range and emits 304s for anything file-shaped — so Volt's HTTP-caching middleware is helpers-and-defaults over dynamic responses, while the store API, TTL/eviction, and stampede layers must be built from sync primitives up (CACHE-1/8/9 are all std gaps).

## P13 — Files & storage

- **Go std**: half of each concern, done well — `mime/multipart` streams uploads with a caller-set memory→temp-file spill and built-in DoS caps, and `fs.FS` is a genuinely universal read-side storage abstraction (OS dirs, embedded assets, archives, in-memory test maps); but `fs.FS` is read-only, so there is no writable storage interface, no cloud backend, and no attachment layer.
- **Rails**: Active Storage — attachment macros on models, three-table blob design, pluggable cloud services, on-the-fly image variants and non-image previews, direct-from-browser uploads.
- **Laravel**: `Storage` facade over Flysystem — named disks (local/public/S3/FTP/SFTP), one read/write/URL/metadata API, temporary URLs, fakeable tests; a filesystem abstraction, not an attachment framework.
- **Django**: `File` wrapper hierarchy + chunked streaming upload handlers (2.5 MB memory→tempfile crossover) + pluggable Storage API via `STORAGES`; cloud backends are ecosystem (django-storages).
- **Phoenix**: `Plug.Upload` multipart plumbing into temp files + LiveView reactive/direct-to-cloud uploads; durable storage abstraction explicitly left to libraries (recorded gap).

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Storage abstraction layer | fs.FS universal read side; no writable interface · STD/NO · FILE-1/34 | Active Storage services API · CORE · FILE-1/3 | named disks over Flysystem · CORE · FILE-1 | Storage API + `STORAGES` aliases · CORE · FILE-8/11 | — (explicit gap: "use a library") · FILE-7 |
| Local-disk backend | os.DirFS + full os file CRUD · STD · FILE-5/8 | Disk service (dev/test) · CORE · FILE-3 | local + symlinked public disk · CORE · FILE-1 | FileSystemStorage · CORE · FILE-9 | Plug.Static extra mounts (serving, not storage) · CORE · FILE-4 |
| Cloud object storage | — (ecosystem: aws-sdk-go, gocloud.dev blob) · FILE-35 | S3 + S3-compatible, GCS (Azure removed 8.1) · CORE · FILE-3 | S3 + any S3-compatible (MinIO/R2/Spaces) · CORE (adapter pkg) · FILE-1 | django-storages et al. · ECO · FILE-13 | — (ExAws/S3 referenced for presigning only) · FILE-7 |
| FTP / SFTP | — | — | drivers incl. key auth · CORE · FILE-2 | — | — |
| Scoped / read-only / on-demand disks | fs.Sub re-rooting; os.Root traversal-proof confinement · STD · FILE-4/10 | per-attachment `service:` selection · CORE · FILE-3 | path-prefixed + read-only disks, `Storage::build` inline config · CORE · FILE-3/4 | per-alias storage instances, callable/lazy · CORE · FILE-11 | — |
| Mirror / multi-service replication | — | mirror service for zero-downtime provider migration · CORE · FILE-4 | — | — | — |
| ORM attachment integration | — (no FileField equivalent) · FILE-36 | `has_one/has_many_attached`, 3-table blob/attachment/variant design · CORE · FILE-1/2 | — (store paths yourself; upload helpers only) | `FileField`/`ImageField` with `.url/.path`, save/delete · CORE · FILE-4 | — |
| Multipart upload handling | multipart.Reader pull parser; r.FormFile/ParseMultipartForm · STD · FILE-15/18/19 | form `file_field` → attach · CORE · FILE-1 | `$request->file()`, validity/mime helpers · CORE · FILE-8, (P2) CTRL-17 | `request.FILES` UploadedFile w/ `.chunks()` · CORE · FILE-5 | `Plug.Upload` temp-file structs auto in params, `give_away/3` · CORE · FILE-1/2 |
| Streaming / memory-safety discipline | ReadForm(maxMemory) disk spill, part/header DoS caps, MaxBytesReader · STD · FILE-16/17/20 | — (direct upload sidesteps the app) | automatic stream detection on writes · CORE · FILE-7 | pluggable upload handlers, 2.5 MB crossover, per-request override · CORE/DIY · FILE-6/7 | `Plug.Parsers` length/read_length/read_timeout limits · CORE · FILE-3 |
| Direct-to-cloud browser uploads | — (no cloud, no presigning) · FILE-35 | `direct_upload: true` w/ JS lifecycle, CORS guidance · CORE · FILE-11 | S3 `temporaryUploadUrl` presigning · CORE · FILE-5 | — | LiveView presigned external uploads · CORE · FILE-5 (LIVE-37) |
| Reactive upload UX (progress/preview/cancel) | — | JS events + progress tracking · CORE · FILE-11 | — (Livewire territory, not in P13) | — | LiveView uploads: progress, preview, cancel · CORE · FILE-5 (LIVE-36) |
| URL generation: public vs signed temporary | — (no signed-URL story) · FILE-35 | public permanent vs signed short-lived per service · CORE · FILE-5 | `url()` + `temporaryUrl` (S3/local) · CORE · FILE-5 | `.url` via storage; no signed-URL story in core · CORE · FILE-8 | — |
| Serving strategies | ServeFile/ServeContent (Range, ETag, sniffing) + FileServer(FS) · STD · FILE-22/23 | redirect vs proxy modes + auth controllers; Thruster X-Sendfile · CORE · FILE-6, (P12) CACHE-13 | streamed `download` responses · CORE · FILE-5 | — (serving left to web server) | `send_file/5` or Plug.Static mounts · CORE · FILE-4 |
| Image variants / transformation | — (DecodeConfig probe only; x/image/draw for scaling) · FILE-32/33/37 | lazy `variant()` via libvips/ImageMagick, named + preprocessed variants · CORE · FILE-7 | — | — (ImageField reads dimensions only · FILE-3) | — |
| Previews for non-images (video/PDF) | — | ffmpeg/poppler previewers, custom API · CORE · FILE-8 | — | — | — |
| Metadata & checksums | FileInfo size/modtime/mode + sha256 via io.TeeReader while storing · STD · FILE-2/31 | blob checksums + background analyzers (dimensions, duration) · CORE · FILE-2/9 | size/lastModified/mime/checksum · CORE · FILE-6 | size, image width/height · CORE · FILE-1/3 | content_type/filename only on struct · CORE · FILE-1 |
| Visibility / permissions | FileMode bits on OpenFile/Chmod (local perms only) · STD · FILE-2/8 | public vs private mode per service · CORE · FILE-5 | public/private at write, per-driver maps · CORE · FILE-9 | `permissions` on FileSystemStorage · CORE · FILE-9 | — |
| Directory operations / listing | fs.WalkDir/Glob/ReadDir, MkdirAll/RemoveAll · STD · FILE-4/8 | — (flat blob keys) | files/directories listing, make/deleteDirectory · CORE · FILE-10 | `listdir` on Storage · CORE · FILE-8 | — |
| Write ops: copy/move/append | Rename, io.Copy (kernel-side where available), O_APPEND flag, os.CopyFS · STD · FILE-8/12/14 | purge/purge_later, replace-vs-add semantics · CORE · FILE-10 | put/prepend/append/copy/move, throw-or-bool · CORE · FILE-7 | save/open/delete, overwrite control · CORE · FILE-8/9 | — (stdlib `File`) |
| Unique/sanitized filenames | os.CreateTemp race-free names; Part.FileName() through filepath.Base · STD · FILE-9/13/18 | auto-generated blob keys · CORE · FILE-2 | hashName, `putFile` unique hashing · CORE · FILE-7/8 | get_valid_name/get_available_name/generate_filename · CORE · FILE-8 | — ("use a library" advice) · FILE-7 |
| Upload validation (type/size/dimensions) | DetectContentType + MaxBytesReader + image.DecodeConfig primitives; no policy layer · STD · FILE-20/24/32/36 | — no built-in validators; form patterns only · DIY · FILE-13 | fluent `File::types()->min()->max()`, image/dimensions · CORE · (P6) VAL-13 | form FileField/ImageField (Pillow) · CORE · FILE-5 | parser-level size limits only · CORE · FILE-3 |
| Temp-file lifecycle | CreateTemp/MkdirTemp + Form.RemoveAll() cleanup · STD · FILE-9/16 | — | — (implicit) | TemporaryUploadedFile w/ configurable dir · CORE · FILE-5/6 | auto-delete at request end, ownership transfer · CORE · FILE-1 |
| In-memory / fake storage for tests | fstest.MapFS + fstest.TestFS conformance (read side) · STD · FILE-7 | fixture attachments, cleanup guidance · CORE · FILE-12 | `Storage::fake` + `UploadedFile::fake()` assertions · CORE · FILE-11 | InMemoryStorage + ContentFile · CORE · FILE-10/2 | — |
| Custom storage backend | implement one-method fs.FS (read-only) · STD · FILE-1/34 | — (service list is what's documented) | `Storage::extend` any Flysystem adapter · CORE · FILE-12 | subclass `Storage` (`_open`/`_save`) · DIY · FILE-12 | — |
| Multi-instance local-disk caveats | — | — (cloud assumed) | — | — | documented: local disk breaks multi-node; use DB/object store · FILE-6 |

**Notable divergences.** The four pick different layers to own. Rails owns the most: attachments are an ORM concept with a schema, transformation pipeline (variants, previews), and provider-migration story (mirror) — yet ships zero upload validators (FILE-13, DIY), which Laravel covers in its validator instead. Laravel owns the filesystem: richest path/disk API (FTP/SFTP, scoped disks, directory ops) but no attachment model and no image processing. Django owns the ingress: it is the only one that treats memory-safe chunked upload handling as a first-class, pluggable pipeline, while pushing all cloud storage to the ecosystem. Phoenix owns almost nothing durable on purpose — its recorded gap says unique naming and S3 are "use a library" — yet its LiveView upload UX (progress/preview/cancel + presigned direct upload) is the most interactive of the four. Direct-to-cloud presigned upload appears in three of four (all but Django).

**Volt notes.**
- Three distinct scopes are on the table: filesystem-API (Laravel), attachment-lifecycle-on-the-ORM (Rails), upload-streaming-discipline (Django). They are separable; Rails is the only one that stacks all three.
- Django's chunked-handler design maps most naturally to Go (`io.Reader`/`multipart.Reader` streaming is idiomatic); Laravel/Rails-style whole-file convenience APIs would sit above that.
- S3-compatible object storage is the de facto floor (3 of 4 treat local disk as dev-only or caveated); FTP/SFTP is Laravel-only legacy surface.
- Image variants are Rails-only and imply a native-dependency choice (libvips/ImageMagick) — in Go that means cgo bindings or shelling out, a real cost worth flagging before copying the feature.
- Go-std baseline reframe: `fs.FS` already solves read-side portability (disk/embed/zip/in-memory) and `mime/multipart` already is Django-grade streaming discipline with DoS caps, so Volt's real gap is narrower than the four-framework picture suggests — a writable Storage interface with cloud backends plus the attachment/naming/validation policy layer (FILE-34/35/36).

## P14 — Building APIs: serialization & content negotiation

- **Go std**: struct tags + `encoding/json` are the whole serialization story — declarative field mapping compiled into the type, streaming Encoder/Decoder, custom marshaler hooks, and a far stricter experimental v2/`jsontext` layer — with no negotiation, resource layer, OpenAPI, pagination, versioning, or token auth above the codec.
- **Rails**: an `--api` subtractive mode with documented à-la-carte middleware; serialization is `render json:` + `as_json` + Jbuilder templates — a dedicated serializer framework is an explicit gap.
- **Laravel**: a real serializer framework — JsonResource/ResourceCollection with envelope + conditional-field control, plus spec-compliant JSON:API resources (new in 13), automatic 422 JSON errors, Sanctum/Passport for tokens.
- **Django**: core stops short of a REST framework — its serializers target fixtures/interchange (JSON/JSONL/XML/YAML, natural keys); REST/OpenAPI/GraphQL are explicitly ecosystem (DRF, django-ninja); contrib covers sitemaps and feeds.
- **Phoenix**: no serializer DSL on principle — JSON views are plain functions returning maps; `:accepts` pipelines negotiate formats; `action_fallback` + `ChangesetJSON` standardize errors.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| API app mode / scaffold | — | Subtractive `rails new --api` (trimmed middleware, no views) · OPT · API-1 | Additive `install:api` (routes, throttle, Sanctum) · CORE · API-1 | — (no API mode) | `phx.new --no-html --no-assets ...` · CORE · API-8; `phx.gen.json` full CRUD scaffold · CORE · API-1 |
| Serializer/transformer framework | Struct tags as schema, no resource layer · TOOL · API-2 (gap · API-24) | None built in — explicit gap · DIY · API-12 | JsonResource + ResourceCollection (`$collects`, meta) · CORE · API-2, API-3 | `serializers.serialize()` — fixture/interchange-oriented, not API payloads · CORE · API-1 | None by design — functions returning maps · CORE · API-2 |
| Direct model → JSON fallback | `json.Marshal` on any struct · STD · API-1 | `as_json`/`serializable_hash`/`to_json` on all models · CORE · API-3 | Return models/collections, `toJson`, hidden/appends · CORE · API-9 | JSON serializer + `DjangoJSONEncoder` · CORE · API-2 | — (maps are hand-built; changeset errors self-encode · API-11) |
| Templated JSON views | — | Jbuilder DSL templates · CORE · API-5 | — (resources instead) | — | JSON view modules (`UrlJSON.index/show`) · CORE · API-2 |
| Envelope/wrapping control | — | `wrap_parameters` (request side only) · CORE · API-7 | `data` wrap, `withoutWrapping`, nested rules · CORE · API-4 | — | — (you build the map) |
| Conditional/sparse field shaping | `omitempty`/`omitzero`/`-` — value-conditional only · STD · API-3 | `as_json(only:/except:)` options · CORE · API-3 | `when*`/`mergeWhen`/`whenLoaded/whenCounted` · CORE · API-5 | `fields=[...]` subset (pk always emitted) · CORE · API-6 | — (hand-written maps) |
| Spec-compliant JSON:API | — | — | `JsonApiResource`: `?include=`, sparse fieldsets, links/meta · CORE · API-7 (new in 13) | — | — |
| Content negotiation | — | `respond_to`/format blocks, `Mime::Type.register` · CORE · API-4 | `expectsJson`-aware rendering · CORE · API-8 | Request-object negotiation (cross-ref CTRL-18/19/37) · CORE | `:accepts` plug per pipeline, `_format` param, per-format views · CORE · API-3, API-4 |
| Multiple wire formats | JSON, XML, CSV, gob codecs · STD · API-1, API-19–21 | `render json:/xml:`, markdown format (8.1) · CORE · API-3, API-6 | JSON (+ JSON:API) · API-2, API-7 | JSON/JSONL/XML/YAML serializers · CORE/OPT · API-2–5 | JSON; XML via EEx templates (P3 VIEW-21) · CORE · API-2 |
| API error shape conventions | — (decode-error taxonomy for 400 mappers · API-10) | `debug_exception_response_format :api`, rescue_responses status mapping · CORE · API-8 | 422 `{message, errors}`, JSON-aware exceptions, `abort()` · CORE · API-8 | — (manual `JsonResponse`) | `action_fallback` + FallbackController → 422 `ChangesetJSON` / 404; `ErrorJSON` · CORE · API-5, API-7, API-11 |
| JSON request-body parsing | `NewDecoder(Body).Decode` + `DisallowUnknownFields` + `MaxBytesReader` · STD · API-7, API-8 | Content-Type-driven params + `wrap_parameters` · CORE · API-7 | — here (P2) | — | — here (Plug.Parsers, P2) |
| Token auth for APIs | — (`BasicAuth` only · API-29) | `HttpAuthentication::Token` · CORE · API-10 | Sanctum + Passport OAuth2 · OPT · API-10 | — (DRF territory) | Bearer-token recipe atop gen.auth · DIY · API-9 |
| Middleware slimming / à-la-carte re-add | — (bare by default) | ~19 middleware + ~13 controller modules individually add/removable; sessions/cookies re-addable · CORE/OPT · API-2, API-11 | Stateless `routes/api.php` group · CORE · API-1 | — | Pipelines per scope · CORE · API-10 |
| HTML + API coexistence in one app | Implicit (any handler encodes JSON) · STD · API-7 | Default app has both; `--api` drops HTML · API-1 | web + api route files · CORE · API-1 | Implicit (any view can return JsonResponse) | Separate pipelines/scopes · CORE · API-10 |
| REST conventions (201/location, 204, except new/edit) | — | — (resources routing in P1) | `apiResource` routing (P1 cross-ref) · API-1 | — | 201 + `location` on create, 204 on delete, `resources except:` · CORE · API-6 |
| API pagination | — | — | Pagination-aware wrapping with `links`/`meta`, customizable · CORE · API-4 | — (Paginator exists but not API-wired) | — · gap · API-13 |
| API versioning | — | None built in · DIY · API-12 | — | — | Nested router scopes only · CORE · API-10 |
| OpenAPI / schema generation | — | None built in · DIY · API-12 | — | — (ecosystem: DRF/ninja, per non-goals) | — · gap · API-13 |
| Rate limiting for APIs | — (x/time/rate · API-31) | — here | Throttle group wired by `install:api` · CORE · API-1 | — | — · gap · API-13 |
| HTTP caching for APIs | — | `stale?` + Rack::Cache, ETag middleware kept in API mode · CORE/OPT · API-9 | — here | — (ConditionalGetMiddleware noted re sitemaps · API-10) | — |
| Custom formats/encoders | `Marshaler`/`Unmarshaler` hooks; v2 per-call `WithMarshalers` · STD · API-4, API-14 | `Mime::Type.register` · CORE · API-4 | — | Custom encoder `cls=`, `SERIALIZATION_MODULES` · DIY · API-2, API-9 | Swappable `Phoenix.json_library()` (Jason) · CORE · API-2 |
| Deserialization / load controls | v1 strict decode; v2 `RejectUnknownMembers`, case-strict, dup rejection · STD · API-8, API-12, API-16 | `from_json` · CORE · API-3 | — | `DeserializedObject.save()`, `ignorenonexistent`, forward refs · CORE · API-7 | — (changesets, P6) |
| Natural keys / stable identity | — | — | — | `natural_key()`/`get_by_natural_key()`, dependencies · CORE · API-8 | — |
| Sitemaps & syndication feeds | — | `atom_feed` helper (P3 VIEW-14 cross-ref) | — | contrib.sitemaps (i18n, index) + contrib.syndication + feedgenerator · OPT/DIY · API-10–14 | — |
| GraphQL | — | — | — | — (ecosystem, per non-goals) | Absinthe · ECO · API-12 |

**Notable divergences.** The core disagreement is whether a framework should own the model→payload transformation: Laravel says emphatically yes (a resource layer with envelope, conditional fields, and now full JSON:API compliance), Phoenix says no on principle (plain functions returning maps beat a serializer DSL), Rails says no by omission (documents `as_json`/Jbuilder and labels a serializer framework a gap), and Django's "serializers" solve a different problem entirely (fixtures/interchange with natural keys) while delegating real APIs to DRF. API modes also diverge: Rails and Phoenix are subtractive (strip a full app down), Laravel is additive (`install:api` bolts API wiring onto a web app), Django has no mode at all. Error shaping is standardized by Laravel and Phoenix (automatic 422 validation/changeset JSON), only status-mapped by Rails, and absent in Django. Notably, *none* of the four ship API versioning, OpenAPI generation, or a dedicated serializer with schema in core — the richest problem-space the inventories collectively mark as gap/DIY/ecosystem.

**Volt notes.**
- Phoenix's "no serializer DSL, just functions returning maps" is the position most congruent with Go idiom (structs + `encoding/json` tags), but Laravel's popularity of conditional-field/envelope/pagination-meta tooling shows real demand the bare-struct approach leaves on the table.
- OpenAPI generation is absent from all four cores despite universal demand — a statically-typed Go framework could derive schemas from handler/struct types, a differentiator none of these dynamic-language frameworks could offer cheaply.
- A standardized validation-error JSON shape (Laravel's 422 `{message, errors}`, Phoenix's `ChangesetJSON`) is a proven cross-framework convention; wiring it to the validation layer by default costs little and removes a universal bikeshed.
- Content negotiation is uniformly thin (Accept header + format param + per-format render paths); Rails' à-la-carte middleware documentation and Phoenix's pipelines both suggest API-vs-HTML should be a composition/route-group concern, not a separate framework mode.
- The stdlib codec under Volt is already deeper than the substrate any of the four frameworks build on — a streaming token layer (`jsontext`), v2 strict decode (`RejectUnknownMembers`, case-strict matching, duplicate rejection), per-call marshaler overrides — so Volt's P14 surface is purely the conventions layer: negotiation, error shape, envelope/pagination and the type-derived OpenAPI none of the dynamic frameworks can do cheaply.

## P15 — Internationalization & localization

- **Go std**: the emptiest platform section — a UTF-8/Unicode substrate plus a genuinely complete IANA time-zone story, and nothing else; message catalogs, plural rules, locale formatting, collation, and Accept-Language negotiation all live in quasi-first-party golang.org/x/text (CLDR-backed, with a far thinner extraction/tooling story than gettext).
- **Rails**: the `i18n` gem wired through the whole stack — YAML trees, lazy scoped lookup, CLDR plurals, model/attribute/error translation, pluggable backends.
- **Laravel**: file-based translations (PHP key arrays + JSON source-string maps) via `__()`, locale/fallback config; the inventory itself calls the subsystem comparatively thin.
- **Django**: GNU gettext end-to-end — extraction/compilation commands, `LocaleMiddleware` negotiation, JS catalogs, locale-aware formatting, and a full timezone framework under the same umbrella.
- **Phoenix**: Gettext ships in every app, but locale *selection* is documented DIY plumbing (plugs, `on_mount` hooks); no negotiation or formatting framework (recorded gap).

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Message storage format | — (x/text/message + catalog) · I18N-7 | YAML/Ruby trees under config/locales · CORE · I18N-3 | PHP key arrays + JSON source-string maps · CORE · I18N-1 | GNU gettext PO/MO in locale/ dirs · CORE · I18N-20/21/23 | GNU gettext PO/MO · CORE · I18N-1 |
| Marking/retrieval API | — (x/text/message Printer) · I18N-7 | `I18n.t`/`l` + view/controller/mailer helpers · CORE · I18N-1 | `__()` (dot-key or literal source string) · CORE · I18N-3 | `gettext`/`gettext_lazy`/`gettext_noop` (`_` alias) · CORE · I18N-1 | gettext macros via generated backend module · CORE · I18N-1 |
| Extraction/compilation tooling | — (gotext CLI: thin, no fuzzy matching) · I18N-7 | — (YAML maintained by hand) | — (keys authored by hand; `lang:publish` scaffold) · I18N-1 | `makemessages`/`compilemessages` (+ customization) · CORE/DIY · I18N-20–22 | PO workflows via gettext tooling · CORE · I18N-1 |
| Locale selection / negotiation | — (std doesn't even parse Accept-Language; x/text/language Matcher) · I18N-9/16 | `around_action` + `I18n.with_locale`; sources documented (params/domain/header/user) · CORE · I18N-2 | config + runtime `App::setLocale` (manual) · CORE · I18N-2 | `LocaleMiddleware`: URL prefix → cookie → Accept-Language → default · CORE · I18N-19 | locale-from-params plug pattern; whitelist validation · DIY · I18N-2 |
| Locale-prefixed routing / translated URLs | — (ServeMux patterns are literal) · I18N-19 | `scope ":locale"` + `default_url_options` propagation · CORE · I18N-2 | — | `i18n_patterns` + `gettext_lazy` translatable routes · OPT · I18N-16/17 | — (gap) · I18N-5 |
| Locale-switch endpoint + cookie persistence | — | — (DIY within I18N-2 patterns) | — | `set_language` view + LANGUAGE_COOKIE_* settings · OPT · I18N-18/26 | — |
| Fallbacks / missing-key handling | — | `:default` chains, `raise_on_missing_translations`, custom exception handler · CORE · I18N-3/4/13 | fallback locale config · CORE · I18N-2 | territorial→generic fallback, LOCALE_PATHS precedence · CORE · I18N-23 | — (gettext default: msgid passthrough; undocumented) |
| Interpolation | — (fmt is locale-blind; x/text/message printf-style) · I18N-10 | `%{var}` w/ strict missing/reserved-key errors · CORE · I18N-5 | `:name` w/ case-mirroring, stringable object hooks · CORE · I18N-4 | `%(name)s` named (f-strings unsupported by xgettext) · CORE · I18N-5 | `%{count}`-style bindings in changeset errors · CORE · I18N-4 |
| Pluralization model | — (x/text/feature/plural, CLDR) · I18N-8 | CLDR plural categories, count-keyed subtrees, custom rules · CORE · I18N-6 | pipe alternatives + exact `{1}`/range `[2,*]` rules (non-CLDR) · CORE · I18N-5 | `ngettext`/`npgettext`, `{% plural %}` blocks · CORE · I18N-3/8 | — (gettext plural-forms implied, not documented) |
| Context disambiguation (msgctxt) | — | — (scoped keys serve instead) | — | `pgettext`/`pgettext_lazy` · CORE · I18N-2 | — |
| Scoped / inferred key lookup | — | lazy lookup: `t(".title")` resolves per controller/action/view · CORE · I18N-4 | dot-notation keys · CORE · I18N-3 | — (flat msgid model) | — |
| Deferred/lazy translation objects | — | — | — | `gettext_lazy` + `format_lazy` for module-load-time strings · CORE · I18N-4 | — |
| Template integration | — (no i18n hooks in text/html template) · I18N-17 | `translate`/`localize` helpers · CORE · I18N-1 | Blade `{{ __(...) }}` · CORE · I18N-3 | `{% translate %}`/`{% blocktranslate %}` + language tags · CORE · I18N-7–9 | — (call gettext in HEEx; no dedicated constructs) |
| HTML safety of translations | — (moot: html/template contextually escapes all output) | `_html` key suffix auto-escapes interpolations · CORE · I18N-8 | — (general Blade escaping) | noted: `{% translate %}` output NOT auto-escaped · CORE · I18N-7 | — |
| Per-locale template files | — | `index.es.html.erb` resolution · CORE · I18N-11 | — | — | — |
| Model/attribute-name translation | — | `model_name.human`, `human_attribute_name`, error cascade · CORE · I18N-9 | Eloquent pluralizer language override only · CORE · I18N-5 | — (lazy `verbose_name` convention via I18N-4) | — |
| Validation-error translation | — | error-message lookup cascade model→attribute→default · CORE · I18N-9 | — (lang files exist; not inventoried as a row) | — in P15 (form errors translated via framework catalogs) | generated `translate_error/1` through Gettext · CORE · I18N-4 |
| Framework strings pre-translated | — | helper outputs read locale data (time distances, numbers) · CORE · I18N-10 | — (scaffold ships English) | ships `django/conf/locale` catalogs · CORE · I18N-23 | — |
| Date/time/number locale formatting | time layouts hard-code English names; locale-aware is x/text/number,currency · STD/X · I18N-6/10/11 | `l(date, format:)`, per-locale formats, number/currency helpers · CORE · I18N-7 | `Number::useLocale`, currency/filesize; Carbon dates · CORE · I18N-8 | locale-aware render + localized form parsing, `{% localize %}`, filters, FORMAT_MODULE_PATH, separators · CORE/OPT · I18N-29–34 | — (explicit gap) · I18N-5 |
| Time-zone framework (as i18n concern) | LoadLocation + IANA tzdb (system/embedded), Time.In — genuinely complete · STD · I18N-5 | — in P15 (`Time.zone` lives in Active Support · EXT-10) | — (schedule tz only · P9 JOB-28) | USE_TZ UTC storage, helpers, override, template tags/filters, aware forms, per-DB tz · CORE/OPT/DIY · I18N-36–48 | — |
| Client-side (JS) translation catalogs | — | — | — | JavaScriptCatalog + JSONCatalog views (+ statici18n ECO) · OPT · I18N-12–15 | — |
| LiveView/SPA locale restoration | — | — | — | — | `on_mount RestoreLocale` hook pattern · DIY · I18N-3 |
| Localized mail/notifications | — | mailer subject lookup + mailer t/l integration · CORE · I18N-1/12 | mailable `locale()` + `HasLocalePreference` · CORE · I18N-7 | — | — |
| Bidi / RTL support | — (x/text/unicode/bidi) · I18N-15 | — | — | `LANGUAGE_BIDI` tags + language info API · CORE · I18N-9/10 | — |
| Translator comments in source | — | — | — | `# Translators:` surfaced into .po · CORE · I18N-6 | — |
| Vendor/package translation override | — | `I18n.load_path` ordering · CORE · I18N-3 | `lang/vendor/{package}` · CORE · I18N-6 | LOCALE_PATHS > app > framework precedence · CORE · I18N-23 | — |
| Pluggable translation backend | — | chained/KeyValue/DB backends · CORE · I18N-13 | — | — (gettext fixed) | — |
| Country-specific form fields | — | — | — | django-localflavor · ECO · I18N-35 | — |

**Notable divergences.** Message format splits the field in two camps: gettext PO/MO with real extraction tooling (Django, Phoenix) versus framework-native hand-maintained key files (Laravel PHP/JSON, Rails YAML) — which cascades into everything else (msgctxt, translator comments, and `makemessages` exist only on the gettext side; lazy scoped keys and pluggable backends only on the key-file side). Locale negotiation is shipped middleware only in Django; Rails documents the pattern, Phoenix explicitly makes it your plumbing, Laravel stops at a config setting. Depth is wildly uneven — Django 48 rows (including an entire timezone framework filed under i18n), Rails 13, Laravel 8 (self-described "comparatively thin"), Phoenix 5 (two of which are gap rows). Pluralization sophistication follows the same gradient: CLDR categories (Rails) vs gettext plural-forms (Django) vs a bespoke pipe/range syntax (Laravel) vs undocumented (Phoenix).

**Volt notes.**
- Go's `golang.org/x/text` gives CLDR plural rules and message catalogs natively; the gettext-vs-native fork here is exactly the Django/Phoenix-vs-Rails/Laravel split — gettext buys a mature translator-tool ecosystem, a native format buys type-safe keys and no C-heritage tooling.
- Extraction is the hidden cost: only the gettext camp gets it free (`xgettext`); a native-format Go framework would need its own static extractor (feasible — Go parses cleanly), or translations rot.
- Accept-Language → cookie → URL-prefix negotiation middleware is small, has no dependencies, and only one of four frameworks ships it — a cheap differentiator.
- Django is the only one to treat timezone correctness (UTC-in-DB, aware datetimes, per-request activation) as part of i18n; in Go, `time.Time` carries location natively, but the *conventions* (store UTC, activate per request/user) still need a framework opinion.
- Go-std baseline caveat on that negotiation middleware: net/http does not even parse Accept-Language, so Volt must supply the parser/matcher itself (x/text/language's `Matcher` or a small vendored equivalent) — the "no dependencies" claim only holds if that piece is written in-house.

## P16 — Testing support

- **Go std**: `go test` + `testing` are a complete, zero-config runner — subtests, `t.Parallel`, shuffle, fuzzing, benchmarks, coverage, race detector — with `httptest` for in-process and real-socket HTTP tests and `synctest` fake-clock bubbles; what's missing is everything framework-shaped: no assertion library, no mocks, no fixtures/factories, no database test-isolation story.
- **Rails**: A complete Minitest harness generated with every app — YAML fixtures + transactional rollback, integration & system tests (Capybara/Selenium bundled), a dedicated TestCase per framework component, parallel by default, 8.1 local-CI DSL.
- **Laravel**: Pest/PHPUnit scaffolding plus an unusually rich fake ecosystem — every facade subsystem ships `::fake()` + assertions, per-test DB reset, in-process HTTP tests, Dusk for real browsers.
- **Django**: A unittest test-case hierarchy (`SimpleTestCase` → `TransactionTestCase` → `TestCase`) with an in-process sync/async `Client`, framework-aware assertions, settings overrides, automatic email doubles, managed test-DB lifecycle, and a swappable runner.
- **Phoenix**: ExUnit + generated case templates (`ConnCase`/`DataCase`/`ChannelCase`); the SQL Sandbox gives every test its own rolled-back transaction, enabling `async: true` concurrent DB tests; every generator ships tests and fixtures.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Test framework & app scaffolding | `testing` + `go test`, `_test.go` name discovery · STD · TEST-1, TEST-2 | Minitest tree generated with app, `test "..."` DSL · CORE · TEST-1 | Pest (default)/PHPUnit, `tests/Feature`+`Unit`, `make:test` · CORE · TEST-1 | unittest hierarchy (Simple/Transaction/TestCase) · CORE · TEST-1..3 | ExUnit + generated case templates, passing suite on `phx.new` · CORE · TEST-1 |
| Test runner: filtering & selection | `-run` regex (incl. subtests), `-short`, `-count`, `-timeout` · TOOL · TEST-1, TEST-3 | `bin/rails test` (file/line/name, fail-fast) · CORE · TEST-2 | `artisan test` (filters, `--compact`) · CORE · TEST-1 | `test` cmd `-k`, `@tag`/`--tag` · CORE/OPT · TEST-19 (+P17 CLI-21) | per-file/line runs, `@tag` + `--only/--exclude` · CORE · TEST-10 |
| Test environment isolation | `T.Setenv`/`T.Chdir`/`T.TempDir`; no test-DB lifecycle · STD · TEST-7 | auto test-schema maintenance from schema dump · CORE · TEST-5 | auto `testing` env, array/sync drivers, `.env.testing` · CORE · TEST-2 | auto test-DB create/destroy, `--keepdb`, `TEST` dict, `DEBUG=False` · CORE · TEST-22 | `test` mix alias chains `ecto.create`+`migrate`+`test` · CORE · TEST-15 |
| Parallel execution | `t.Parallel()` per-test + parallel per-package binaries · STD/TOOL · TEST-4, TEST-1 | **default-on** forked processes w/ per-process DBs · CORE · TEST-13 | opt-in `--processes`, per-process DBs, `ParallelTesting` hooks · CORE · TEST-3 | opt-in `--parallel` per-process DB (needs `tblib`) · OPT · TEST-23 | per-test concurrency via async sandbox; CI `--partitions` w/ auto per-partition DBs · CORE · TEST-5, TEST-11 |
| Randomized ordering / seed control | `-shuffle` w/ seed · TOOL · TEST-1 | seed control in runner · CORE · TEST-2 | — (not in inventory; PHPUnit-level) | `--shuffle`/`--reverse` · OPT · TEST-23 | `--seed` deterministic randomization · CORE · TEST-11 |
| In-process HTTP/feature tests | `httptest.NewRequest` + `ResponseRecorder`; real-socket `Server` · STD · TEST-22, TEST-23 | `IntegrationTest` verb helpers, `xhr:`, `as: :json` · CORE · TEST-6 | `get/post/json`, headers/cookies/session seeding · CORE · TEST-4 | test `Client` (all verbs, JSON, file upload, CSRF toggle) · CORE · TEST-5 | `ConnCase` + `build_conn()`, verified `~p` routes · CORE · TEST-2, TEST-3 |
| Response assertions (HTML/JSON) | — (idiom is `if got != want`; std refuses an assert API · TEST-29) | `assert_response/redirected_to` + `assert_dom` (Nokogiri) · CORE · TEST-3, TEST-9 | 100+ assertion vocabulary + fluent `AssertableJson` · CORE · TEST-6, TEST-7 | `assertContains/Redirects/HTMLEqual/JSONEqual/FormError`… · CORE · TEST-13 | `html_response/json_response/redirected_to` · CORE · TEST-3 |
| Multi-request / multi-session flows | — | `follow_redirect!`, `open_session` multi-user · CORE · TEST-7 | session seeding on requests · CORE · TEST-4 | `follow=True` + `redirect_chain` · CORE · TEST-5 | — |
| Auth helpers in tests | — | `session`/`cookies` access · CORE · TEST-6 | `actingAs` (+guard) · CORE · TEST-4 | `login/force_login` (+async variants) · CORE · TEST-6 | generated `register_and_log_in_user` setup helpers · CORE · TEST-9 |
| DB isolation strategy | — (no DB test story at all · TEST-32) | transactional rollback per test, opt-out · CORE · TEST-5 | `RefreshDatabase` (rollback) / `DatabaseMigrations` / `Truncation` · CORE · TEST-11 | `TestCase` atomic rollback; `TransactionTestCase` truncation; `serialized_rollback` · CORE/OPT · TEST-2, TEST-3, TEST-24 | SQL Sandbox per-test transaction, **concurrent-safe** · CORE · TEST-5 |
| DB state assertions | — (TEST-32) | `assert_difference`/`assert_changes` · CORE · TEST-3 | `assertDatabaseHas/Count`, `assertModelExists`, `expectsDatabaseQueryCount` · CORE · TEST-12 | `assertNumQueries`, `assertQuerySetEqual` · CORE · TEST-14 | `errors_on/1` changeset helper; layered test guidance · CORE · TEST-6 |
| Test data: fixtures vs factories | — (`testdata/` dir + go:embed as raw material · TEST-31) | YAML fixtures per model (labels, ERB, attachments) · CORE · TEST-4 | model factories + `seed()` per test · CORE · TEST-11 | `fixtures` attr (dumpdata format) + `setUpTestData` per-class · CORE/OPT · TEST-11, TEST-12 | generated fixture modules; hand-rolled factory functions "sufficient" · CORE/DIY · TEST-9, TEST-14 |
| Tests generated with scaffolds | — | scaffold generates tests (see P17 CLI-2) · CORE · TEST-1 | `make:test` (manual) · CORE · TEST-1 | — | **every** generator emits tests + fixtures · CORE · TEST-9 |
| Browser / system testing | — (TEST-33) | Capybara + Selenium bundled, `driven_by`, failure screenshots · CORE · TEST-8 | Dusk: ChromeDriver DSL, waits, screenshots, page objects · OPT · TEST-16, TEST-17 | `LiveServerTestCase` + Selenium (third-party) · OPT/ECO · TEST-4, TEST-21 | — (LiveViewTest covers most UI needs w/o browser) |
| Stateful-frontend tests w/o browser | — | — | — | — | `Phoenix.LiveViewTest`: element selection, `render_hook`, DOM checks · CORE · TEST-8 |
| View/template unit tests (no HTTP) | — | partial & helper test cases, `assert_dom` · CORE · TEST-9 | `view()/blade()/component()` render + assert · CORE · TEST-9 | response `templates`/`context` inspection, `assertTemplateUsed` · CORE · TEST-10, TEST-13 | `render_to_string/4` · CORE · TEST-13 |
| Console-command testing | — | — | `artisan()->expectsQuestion/Output/Table`, exit codes · CORE · TEST-10 | `call_command` + `StringIO` capture · CORE · TEST-18 | — |
| Mail testing | — | `:test` delivery + `assert_emails` · CORE · TEST-10 | `Mail::fake` + assertions · CORE · TEST-15 | `mail.outbox` capture, per-test reset · CORE · TEST-17 | — (Swoosh test adapter lives in P11) |
| Job/queue testing | — | `assert_enqueued/performed_jobs`, `perform_enqueued_jobs` · CORE · TEST-11 | `Queue/Bus::fake` · CORE · TEST-15 | — (task doubles mentioned only in preamble) | — (Oban is OPT, outside P16) |
| WebSocket/channel testing | — | connection/channel tests, broadcast assertions · CORE · TEST-12 | `Event`/broadcast fakes · CORE · TEST-15 | — | `ChannelCase`: `subscribe_and_join`, `assert_push/broadcast/reply` · CORE · TEST-7 |
| Subsystem fakes (breadth) | fs/io/net only: `fstest.MapFS`, `iotest`, `net.Pipe` · STD · TEST-24..26 | per-component TestCases instead of fakes · CORE · TEST-10..12 | Event/Mail/Notification/Queue/Bus/Storage/Http/Process/Sleep/AI fakes · CORE · TEST-15 | email/storage doubles + settings override · CORE · TEST-15, TEST-17 | — (real processes + sandbox instead) |
| Outbound HTTP-client faking | `httptest.Server` as target; `net.Pipe` + Transport override · STD · TEST-23, TEST-24 | — (WebMock/VCR are ecosystem, not in inventory) | `Http::fake` (URL maps, sequences), `preventStrayRequests` · CORE · TEST-18 | — | — |
| File upload & storage testing | `T.TempDir`; `fstest.MapFS` (fs.FS-level only) · STD · TEST-7, TEST-25 | file-attachment fixtures · CORE · TEST-4 | `UploadedFile::fake` + `Storage::fake` · CORE · TEST-8 | Client file upload; `InMemoryStorage` · CORE/OPT · TEST-5, TEST-12 | — |
| Time control | `testing/synctest` fake-clock bubbles (stable 1.25) · STD · TEST-21 | `travel/travel_to/freeze_time` · CORE · TEST-14 | `travel()`, `freezeTime` · CORE · TEST-14 | — | — |
| Mocking/stubbing | — (idiom: accept interfaces, hand-write fakes · TEST-30) | — (not in inventory) | Mockery: `mock/spy`, facade mocking · CORE · TEST-13 | `mock.patch` guidance only · DIY · TEST-30 | — (hand-rolled patterns; DIY stance) · TEST-14, TEST-16 |
| Exception / error-path testing | `iotest` misbehaving-I/O injection (I/O only) · STD · TEST-26 | `assert_raises` documented flow · CORE · TEST-16 | `Exceptions::fake`, `withoutExceptionHandling` · CORE · TEST-5 | `assertRaisesMessage/WarnsMessage` · CORE · TEST-13 | `assert_error_sent 404` (exception→status) · CORE · TEST-4 |
| Settings/config override per test | `T.Setenv` · STD · TEST-7 | — | `.env.testing` + `phpunit.xml` vars · CORE · TEST-2 | `override_settings/modify_settings` + `setting_changed` signal · CORE · TEST-15 | — |
| Coverage & profiling | `-cover`/`-coverprofile` built-in; benchmarks + `-benchmem` · TOOL/STD · TEST-18, TEST-12, TEST-13 | — | `--coverage`, `--min`, `--profile` built-in · CORE · TEST-3 | `coverage.py` (named third-party); `--durations` · ECO/OPT · TEST-29, TEST-23 | — |
| Local CI orchestration | `T.Attr`/`T.ArtifactDir` CI metadata only · STD · TEST-10 | `config/ci.rb` DSL + `bin/ci` + `gh signoff` (8.1) · CORE · TEST-15 | Dusk CI recipes (GH Actions etc.) · OPT · TEST-17 | — | `MIX_TEST_PARTITION` CI partitioning · CORE · TEST-11 |
| Async client / async test support | native goroutines + `-race`; `synctest` determinism · STD/TOOL · TEST-19, TEST-21 | — | — | `AsyncClient`, `AsyncRequestFactory`, auto-wrapped `async def` tests · OPT · TEST-7, TEST-9 | (concurrency is execution-level, TEST-5) |
| Runner extensibility & multi-DB tests | `TestMain(m)` global hook only · STD · TEST-5 | — | — | swappable `TEST_RUNNER`/`DiscoverRunner`; `databases` attr, `MIRROR`/`DEPENDENCIES` · OPT · TEST-27, TEST-25 | — |
| Plug/middleware unit testing | handlers are plain funcs: `NewRequest` + Recorder, no server · STD · TEST-22 | — | — | `RequestFactory` (view w/o middleware) · CORE · TEST-8 | `Plug.Test`: `conn/2` + direct `call/2`, no server · CORE · TEST-12 |

**Notable divergences.** The four disagree most on *what substitutes for the real thing in a test*: Laravel fakes every subsystem behind its facades (TEST-15) and even the HTTP client (TEST-18); Rails prefers real objects with per-component TestCases and `:test` adapters; Phoenix rejects both fakes and factory libraries (DIY stance, TEST-14) and instead makes real DB access safe and concurrent via the SQL Sandbox; Django sits in between with a few automatic doubles (mail outbox) plus `override_settings`. Parallelism philosophy differs by grain: Rails parallelizes by default at the process level, Laravel and Django make it opt-in, and Phoenix is the only one with *per-test* concurrency against a real database. Browser testing tiers diverge fully — CORE and bundled in Rails, a separate OPT package in Laravel (Dusk), OPT/ECO in Django, and absent in Phoenix, which instead offers the unique LiveViewTest (browser-fidelity UI tests without a browser). Test data splits factories (Laravel) vs YAML fixtures (Rails, Django) vs generated fixture modules + plain functions (Phoenix).

**Volt notes.**
- `go test` already covers ~6 rows the frameworks had to build (runner, filtering, parallelism via `t.Parallel`, seed/shuffle, coverage, benchmarks) — Volt's leverage is in what `go test` lacks: DB isolation, an HTTP test client with framework-aware assertions, and subsystem doubles.
- Phoenix's SQL Sandbox is the only DB-isolation design compatible with `t.Parallel`; Rails/Laravel/Django-style single-transaction rollback breaks under Go's default concurrent tests. This is arguably the highest-value P16 feature to port.
- Laravel's fake ecosystem depends on global facades/container swapping — un-Go-like; the Go-idiomatic equivalent is interfaces + provided fake implementations (mail, queue, storage, clock), which Rails' adapter approach (`:test` delivery) resembles more than Laravel's.
- Time control (Laravel/Rails CORE) needs deliberate design in Go: there is no monkey-patching, so a framework-injected clock interface must exist from day one or clock-dependent code is untestable.
- `testing/synctest` (STD, stable 1.25) partly reframes the previous point: std now provides fake-clock bubbles for concurrency/timeout code, so Volt's injected clock is needed for *domain* time (timestamps, scheduling) rather than for taming `time.Sleep` in tests.

## P17 — CLI, code generation & developer experience

- **Go std**: The `go` tool *is* the developer experience — build/run/test/vet/fmt/generate/doc/mod, uniform, zero-config and fast — plus exactly one package (`flag`) for the app's own CLI; no scaffolding, no REPL, no hot reload, no prompts/TUI, no command framework beyond FlagSet-per-verb.
- **Rails**: The `bin/rails` suite — app/scaffold/model generators on Thor, an app-loaded REPL, runners, application templates — all extensible and configurable.
- **Laravel**: Artisan — one binary exposing ~all framework ops plus the `make:*` generator family — Tinker REPL, the Prompts TUI library, starter kits for whole-app scaffolding.
- **Django**: One command framework, three entry points, ~40 built-in ops commands (migrations, data dump/load, i18n, inspection); minimal codegen (project/app skeletons only); custom commands as a first-class extension point.
- **Phoenix**: `mix phx.new` + `phx.gen.*` generators that write code *into your app* (contexts, LiveViews, auth) as an explicit learning tool/starting point; hot code reload; IEx-first workflow.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| CLI entry point & command framework | `flag` + FlagSet-per-verb; no command tree · STD · CLI-9, CLI-12 | `bin/rails` command suite (Thor) · CORE · CLI-1, CLI-10 | Artisan: `list/help`, `--env`, extensible `about` · CORE · CLI-1 | `django-admin`/`manage.py`/`python -m django` · CORE · CLI-1 | Mix tasks, `mix help` self-documenting · CORE · CLI-11 |
| New-app generator & presets | — (gonew is experimental x/tools · CLI-17) | `rails new`: `-d`, `--api`, `--devcontainer`, `--skip-*`, `--minimal` · CORE · CLI-1 | `laravel new` + starter-kit picker (React/Vue/Svelte/Livewire) · CORE/ECO · CLI-15 | `startproject`/`startapp` with `--template` dir/archive/URL · CORE · CLI-2, CLI-3 | `phx.new` + composable `--no-*` flags (ecto/html/assets/live/mailer) · CORE · CLI-1, CLI-2 |
| Zero-toolchain bootstrap | — | `rails-new` tool (no local Ruby) · OPT · CLI-15 | php.new one-liner, Laravel Herd · CORE/ECO · CLI-15 | — | Phoenix Express curl installer (installs Erlang/Elixir too) · CORE · CLI-1 |
| Resource/CRUD scaffolding | — (`go generate` + `text/template` as raw substrate · CLI-6, CLI-15) | `scaffold` full CRUD incl. tests; `model` field:type syntax · CORE · CLI-2 | `make:*` family (model `-mfsc --all`, controller variants, 20+ types) · CORE · CLI-3 | — (only skeletons; no CRUD codegen) | `phx.gen.html/json/live`: full vertical slice (context+schema+web+tests) · CORE · CLI-5 |
| Domain-layer-only generators | — | `model`, `migration`, `resource` · CORE · CLI-2 | make:model/migration/seeder standalone · CORE · CLI-3 | — (`inspectdb` reverse-engineers models from legacy DB · CORE · CLI-20) | `phx.gen.context`/`phx.gen.schema`; re-gen *injects* into existing context · CORE · CLI-6 |
| Generator field syntax | — | `field:type[:index]`, `references` · CORE · CLI-2 | via make:model options · CORE · CLI-3 | — | `title:string:unique`, `references`, `--scope` wiring · CORE · CLI-7 |
| Auth scaffolding | — | `authentication` generator · CORE · CLI-2 | via starter kits · CORE/ECO · CLI-15 | — (runtime contrib + `createsuperuser` · OPT · CLI-24) | `phx.gen.auth` (largest generator; LiveView/controller variants) · CORE · CLI-8 |
| Real-time scaffolding | — | `channel` generator · CORE · CLI-2 | — | — | `phx.gen.channel/socket/presence` · CORE · CLI-9 |
| Generator customization | — | `config.generators`, custom Thor generators, `lib/templates` overrides · CORE · CLI-3, CLI-4 | `stub:publish` overrides templates · CORE · CLI-12 | `--template` mechanism on start* · CORE · CLI-2, CLI-3 | — (not in inventory) |
| App templates / recipe DSL | — | `rails new -m template.rb` DSL (`gem`, `route`, `ask`, `after_bundle`) · CORE · CLI-5 | community starter kits (Composer/repo) · ECO · CLI-15 | — | — |
| Symmetric un-generate | — | `bin/rails destroy` · CORE · CLI-2 | — | — | — |
| Generated-code ownership stance | — | conventional, config-swappable (e.g. rspec fallback) · CLI-3 | framework-owned stubs (customizable) · CLI-12 | codegen avoided by design | explicit: generators are learning tools; rename/refactor freely · CORE · CLI-15, CLI-6 |
| REPL / console | — (no REPL for a compiled language · CLI-18) | `rails console`: `app`+`helper`, `--sandbox` rollback · CORE · CLI-6 | Tinker (psysh): Eloquent/jobs/events, allow-list · CORE · CLI-2 | `shell`: auto-imports models (**6.0**), ipython/bpython, customizable imports · CORE/DIY · CLI-6, CLI-7 | `iex -S mix phx.server`, `recompile()`, `.iex.exs` config · CORE · CLI-3, CLI-16 |
| Production remote console | — | — | — | — | `bin/my_app remote` live IEx; `eval` one-offs · CORE · CLI-17 |
| DB console | — | `dbconsole` multi-DB aware · CORE · CLI-7 | — (not in inventory) | `dbshell` (psql/mysql/sqlite/sqlplus passthrough) · CORE · CLI-8 | — |
| One-off script runner | `go run` compile-and-execute · TOOL · CLI-2 | `bin/rails runner` in app context · CORE · CLI-8 | `Artisan::call` from code · CORE · CLI-11 | `shell -c` / stdin exec · CORE · CLI-6 | release `eval` · CORE · CLI-17 |
| Dev server & process runner | `go run`; fast compiles as the dev loop · TOOL · CLI-2 | `server` (Puma), `bin/dev`, `bin/setup` · CORE · CLI-9 | Herd local env · CORE/ECO · CLI-15 | `runserver` w/ autoreload (Watchman opt.); ASGI dev via ECO `daphne-runserver` · CORE/ECO · CLI-4, CLI-5 | `mix phx.server` · CORE · CLI-3 |
| Hot reload / live reload | — (air/wgo are ecosystem · CLI-19) | — (bundler watch via `bin/dev`) · CLI-9 | — (Vite HMR lives in P3) | Python autoreload per request · CORE · CLI-4 | hot code reload + browser live-reload, CSS w/o refresh · CORE · CLI-4 |
| Custom commands | FlagSet subcommands + `switch os.Args[1]` · STD · CLI-12, CLI-13 | rake tasks in `lib/tasks` w/ app env · CORE · CLI-12 | `make:command`, `$signature` DSL, DI `handle()`, closure cmds · CORE · CLI-4, CLI-5 | `management/commands/` `BaseCommand` (+App/LabelCommand) · DIY · CLI-32, CLI-33 | Mix aliases as workflow glue (user-editable) · CORE · CLI-13 |
| Interactive prompts / TUI toolkit | — (x/term raw mode only · CLI-20) | `ask`/`yes?` in template DSL only · CORE · CLI-5 | Prompts: text/select/search/form + spinners/progress, testable, fallbacks · CORE · CLI-8, CLI-9, CLI-10 | colored output + `DJANGO_COLORS` only · CORE · CLI-28 | — |
| Auto-prompt for missing args | — | — | `PromptsForMissingInput` · CORE · CLI-7 | — | — |
| Programmatic command invocation | `os/exec` process-level only · STD · CLI-14 | — | `Artisan::call/queue`, `$this->call` between cmds · CORE · CLI-11 | `call_command()` w/ stream redirect · CORE · CLI-31 | — |
| Command locking / isolation | — | — | `--isolated` cache-lock single execution · CORE · CLI-6 | — | — |
| Signal handling & console events | — | — | `trap`; CommandStarting/Finished events · CORE · CLI-12, CLI-13 | — | — |
| Introspection commands | `go vet` static checks (no app introspection) · TOOL · CLI-4 | `routes/about/stats/initializers/middleware/notes` (TODO scanner) · CORE · CLI-10 | `about` env overview · CORE · CLI-1 | `check` (system checks), `diffsettings`, `showmigrations` · CORE · CLI-9, CLI-10, CLI-15 | `mix phx.routes` · CORE · CLI-11 |
| Migration/DB task CLI | — | (db:* tasks live in P5) | `make:migration` (rest in P5) · CORE · CLI-3 | full suite: `makemigrations/migrate/sqlmigrate/squash/optimize/flush` · CORE · CLI-13..19 | `ecto.create/migrate/rollback/gen.migration` w/ friendly diagnostics · CORE · CLI-12 |
| Data dump/load CLI | — | — | — | `dumpdata`/`loaddata` (compression, natural keys) · CORE · CLI-11, CLI-12 | — |
| Secrets/credentials CLI | — | `credentials:edit/fetch`, `db:encryption:init` · CORE · CLI-11 | — (key:generate not in inventory) | — | `phx.gen.secret` · CORE · CLI-10 |
| Deployment/release codegen | `go build`: the binary *is* the release artifact · TOOL · CLI-1 | — | — | — | `phx.gen.release [--docker]`; `phx.gen.cert` dev TLS · CORE · CLI-10 |
| Code style / formatting tooling | `gofmt`/`go fmt` canonical, argument-free · TOOL · CLI-5 | RuboCop `rails-omakase` in default Gemfile · ECO · CLI-14 | Pint: zero-config fixer, presets, CI modes · OPT · CLI-14 | auto-`black` of generated files when on PATH · OPT · CLI-30 | — (mix format not in inventory) |
| i18n / cache infra commands | — | — | — | `makemessages/compilemessages`, `createcachetable` · OPT · CLI-23 | — |
| Uniform global CLI flags & UX | auto `-h`/usage + `PrintDefaults` via `flag` · STD · CLI-9 | — | verbosity levels, `--env` · CORE · CLI-1 | `--settings/--pythonpath/--verbosity/--no-color/--skip-checks` on every command; bash completion · CORE/OPT · CLI-27, CLI-29 | — |
| Dev containers / IDE support | — | generated `.devcontainer`, Codespaces flow · OPT · CLI-13 | Boost editor/agent setup docs · CORE · CLI-16 | — | — |
| Dev error pages w/ actions | — | — | — | — (debug page lives outside P17) | actionable buttons (run migrations/seeds) via `Plug.Exception`; `debug_errors` · CORE · CLI-18 |
| Directory-layout convention as DX | — | test/ mirrors app/ (P16 TEST-1) | — | — | `lib/app` vs `lib/app_web` split documented as architectural statement · CORE · CLI-14 |

**Notable divergences.** Codegen philosophy is the sharpest split: Django essentially refuses CRUD scaffolding (skeletons + `inspectdb` only), Rails and Laravel generate heavily but treat output as convention-shaped framework code, and Phoenix generates the most per invocation (full vertical slices, the giant `phx.gen.auth`) while explicitly declaring the output *yours* — a learning tool to rename and refactor. Where Django invests instead is operational breadth: ~40 built-in commands (migrations, dumpdata/loaddata, i18n, checks) with uniform global flags, versus Laravel investing in CLI *ergonomics* (Prompts TUI, isolatable commands, auto-prompting, console testing) that no other framework matches. REPL centrality varies: Rails and Django load the whole app into a console, Laravel sandboxes Tinker with an allow-list, and Phoenix goes furthest — the REPL *is* the runtime (`iex -S mix`) and extends into production (`bin/app remote`), something only the BEAM can offer. Custom-command extension is CORE and DSL-driven in Laravel/Rails, but DIY-tier plumbing in Django.

**Volt notes.**
- Go has no REPL: Tinker/console/`iex`/`shell` — CORE in all four — is simply unavailable. The realistic substitutes are a `runner`-style command executing snippets in app context, `dbshell` passthrough, and rich introspection commands (`routes`, `about`, `check`); Volt should over-invest there to compensate.
- Codegen is *more* natural in Go than in these languages, since Go lacks the runtime metaprogramming Rails/Laravel use to avoid writing code; Phoenix's model (generate explicit code into the user's app, declare it user-owned, inject into existing files on re-run) fits Go's explicitness culture best, and `gofmt` gives the Django-style auto-format for free.
- Single static binary changes the CLI shape: the app binary itself can carry ops commands (migrate, routes, eval-ish tasks) à la Phoenix releases, separating dev-time codegen (a `volt` tool) from run-time ops (the compiled app) — none of the four has to make that split.
- Hot reload is table stakes in all four (autoreload/live-reload CORE in Phoenix/Django, watch flows in Rails); Go needs rebuild-and-restart tooling (air-style watcher) plus browser live-reload to match, which the framework must ship or bless since the compiler makes DIY setups fiddly.

## P18 — Configuration, environments & deployment
(Django titles this "Configuration & deployment")

- **Go std**: config is spartan — `os.Getenv` + `flag` are the whole story (no env files, no layering, no secrets) — but deployment is the crown jewel: one `go build` yields a self-contained static binary with `go:embed`-ed assets, cross-compiled via `GOOS`/`GOARCH`, serving production TLS itself and shutting down gracefully via `os/signal` + `Server.Shutdown`.
- **Rails**: convention-first layered Ruby config with versioned `load_defaults`, Zeitwerk autoloading, and a fully owned deploy path: generated production Dockerfile + Kamal 2 + Thruster + Puma out of the box.
- **Laravel**: dotenv + plain PHP config files with aggressive production caching, `bootstrap/app.php` as single wiring point, maintenance mode, and a fleet of first-party env tools (Sail/Valet/Homestead/Envoy) plus paid hosting (Forge/Cloud).
- **Django**: settings are a plain Python module (`DJANGO_SETTINGS_MODULE`) with dict-of-alias config families; deployment is a WSGI/ASGI callable handed to ecosystem servers, gated by `check --deploy`.
- **Phoenix**: compile-time config layering plus `runtime.exs` for boot-time env vars; `mix release` produces a self-contained artifact (VM included) with generated Docker and clustering support.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Config format & source | `os.Getenv`/`LookupEnv` + `flag` only · STD · CONF-1, CONF-3 | Ruby `application.rb` + per-env files · CORE · CONF-1, CONF-3 | dotenv + PHP config files · CORE · CONF-1, CONF-4 | Python settings module · CORE · CONF-1, CONF-2 | `config.exs` → per-env `.exs` layering · CORE · CONF-1 |
| Environment selection/detection | — (no environment notion · CONF-23) | env files + custom envs · CORE · CONF-1 | `APP_ENV` + `App::environment()` · CORE · CONF-2 | `DJANGO_SETTINGS_MODULE` / `--settings` · CORE · CONF-1 | `MIX_ENV` · CORE · CONF-4 |
| Runtime vs build-time config split | `go:embed` + `-ldflags -X` vs env at boot · TOOL/STD · CONF-7, CONF-11 | boot-time initializers (no split) · CORE · CONF-4 | `env()` only in config files; cache cuts env off · CORE · CONF-1, CONF-6 | all boot-time; no mutation after load · CORE · CONF-2 | `runtime.exs` vs compile-time; `force_ssl` compile-time called out · CORE · CONF-1, CONF-15 |
| Secrets handling | — (env vars or external stores · CONF-6) | credentials + `.kamal/secrets` fetch · CORE · CONF-12 | encrypted committable env files · CORE · CONF-3 | `SECRET_KEY` + `SECRET_KEY_FALLBACKS`, checklist · CORE · CONF-6 | `SECRET_KEY_BASE` + env vars in runtime.exs · CORE · CONF-2, CONF-8 |
| Typed/structured config access | `flag` typed values incl. `TextVar` · STD · CONF-3 | `config.x.*` + `config_for` env-keyed YAML · CORE · CONF-6 | `config()` dot-notation typed getters · CORE · CONF-4 | `settings` object; roll-your-own uppercase keys · CORE/DIY · CONF-2, CONF-5 | OTP app config (keyword lists) · CORE · CONF-1 |
| Backing-service config families | — (no config framework · CONF-5) | `database.yml` + Solid trifecta DB wiring · CORE · CONF-7, CONF-15 | per-service config files, slim defaults · CORE · CONF-5 | dict-of-alias `DATABASES/CACHES/STORAGES/TASKS/MAILERS/TEMPLATES` · CORE · CONF-13, CONF-14 | Repo config surface (pool, socket opts, types) · CORE · CONF-13 |
| Config caching/boot optimization | — (compiled binary; N/A) | eager load + Bootsnap · CORE · CONF-8, CONF-10 | `config:cache`, `optimize` umbrella · CORE · CONF-6 | — | compile-time config is the cache · CORE · CONF-1 |
| Versioned defaults / upgrade path | — | `load_defaults` + `new_framework_defaults_*` · CORE · CONF-2 | upgrade docs + Shift/AI-assisted · DIY · CONF-18 | release notes + `-Wa` deprecation testing · CORE · CONF-17 | — |
| Debug-mode toggle | — (no prod-mode switch · CONF-23) | per-env config · CORE · CONF-1 | `APP_DEBUG` with prod warning · CORE · CONF-7 | `DEBUG` critical setting · CORE · CONF-6 | `debug_errors` endpoint flag · CORE · CONF-3 |
| App wiring / boot process | `main()` wired by hand · TOOL | documented boot pipeline + initializer chain · CORE · CONF-4, CONF-10 | `bootstrap/app.php` chained builder · CORE · CONF-11 | `django.setup()` + app registry · OPT · CONF-4 | supervision tree as boot config · CORE · CONF-14 |
| Autoload / dev code reload | — | Zeitwerk + file-watcher reload · CORE · CONF-8, CONF-9 | — (Composer, outside section) | — (runserver autoreload, outside section) | `code_reloader` + watchers · CORE · CONF-3 |
| Production HTTP server story | in-process `http.Server`: timeouts, TLS + auto-HTTP/2, graceful `Shutdown` on signal · STD · CONF-12–15, CONF-17 (ACME: x/crypto autocert · CONF-16) | Puma + Thruster in-box · CORE · CONF-13, CONF-14 | nginx + FrankenPHP guidance · DIY · CONF-9 | hand off WSGI/ASGI callable to Gunicorn/Uvicorn/etc. · CORE+ECO · CONF-8–11 | built-in endpoint, `mix phx.server` · CORE · CONF-3, CONF-5 |
| Deployable build artifact | static binary w/ `go:embed`-ed assets · TOOL · CONF-7, CONF-9 | multi-stage Dockerfile generated per app · CORE · CONF-11 | — (guidance only) | — (checklist only) | `mix release` self-contained dir + `bin/server`/`bin/migrate` · CORE · CONF-6 |
| Docker support | — (FROM-scratch trivial · CONF-9) | generated production Dockerfile · CORE · CONF-11 | Sail (dev only) · OPT · CONF-13 | — | `phx.gen.release --docker` prod Dockerfile · CORE · CONF-7 |
| First-party deploy tool | — (scp the binary · CONF-9) | Kamal 2 zero-downtime deploys · CORE · CONF-12 | Envoy SSH runner · OPT · CONF-16 | — (checklist · CORE · CONF-6) | — (platform guides: Fly/Gigalixir/Heroku · CORE docs · CONF-11) |
| First-party hosting/platform | — | — | Forge + Laravel Cloud (paid) · ECO · CONF-17 | — | — (Fly.io auto-detect documented · CONF-11) |
| Local dev environments | — | — | Sail / Valet / Homestead · OPT · CONF-13, CONF-14, CONF-15 | — | watchers in dev endpoint · CORE · CONF-3 |
| Deploy config linting | — (CONF-23) | — | optimization checklist · DIY · CONF-9 | `check --deploy` + per-env guidance · CORE · CONF-6, CONF-7 | — |
| Health endpoint | — (P20 OBS-26) | `/up` · CORE · (P21 ADMIN-5) | `/up` + `DiagnosingHealth` event · CORE · CONF-10 | — | — |
| Maintenance mode | — | — | `down`/`up`, bypass secret, multi-server · CORE · CONF-8 | — | — |
| Clustering / multi-node | — | — | — | — | `dns_cluster` default + libcluster; multi-node decision matrix · CORE/ECO · CONF-9, CONF-10, CONF-12 |
| Concurrency/runtime tuning config | cgroup-aware `GOMAXPROCS` + `GOMEMLIMIT` · STD · CONF-18 | executor/load-interlock semantics, `WEB_CONCURRENCY` · CORE · CONF-14, CONF-18 | — (Octane outside section) | ASGI async-safety caveats · CORE · CONF-9 | supervision tree · CORE · CONF-14 |
| Drop unused framework parts | — | à-la-carte railties vs `rails/all` · CORE · CONF-17 | slim unpublished configs · CORE · CONF-5 | `INSTALLED_APPS` (see P19 EXT-10) | — (deps-based) |
| Multi-site/domain awareness | — | — | — | contrib.sites + CurrentSiteMiddleware · OPT · CONF-20, CONF-21 | — |
| Subdirectory deploy / static toggles | — | `relative_url_root`, `RAILS_SERVE_STATIC_FILES` · CORE · CONF-16 | — | `STATIC_ROOT` + `collectstatic` pointer · OPT · CONF-15 | `cache_static_manifest`, url host config · CORE · CONF-3 |

**Notable divergences.** Rails and Phoenix own the path to production (generated Dockerfile + Kamal; `mix release` + generated Docker/clustering), while Laravel sells it (Forge/Cloud are the blessed story, OSS docs are DIY guidance) and Django explicitly stops at the WSGI/ASGI callable boundary, delegating servers to the ecosystem. Phoenix is alone in formalizing the compile-time vs boot-time config distinction (`runtime.exs`) and in treating clustering as a default-on concern; the other three are single-node-first. Laravel invests uniquely in local-dev environments (Sail/Valet/Homestead) and maintenance mode; Django invests uniquely in config linting (`check --deploy`) and multi-site config. Rails is the only one with versioned behavior defaults (`load_defaults`) as an upgrade mechanism. Set against the Go baseline, the deployment half of this table largely evaporates — std already delivers what Rails and Phoenix generate (self-contained artifact, in-process production server with TLS, graceful shutdown) — while the configuration half sits on a bare `os.Getenv`, which is exactly where all four frameworks did their work.

**Volt notes.**
- Go compiles to a static binary, so Volt gets Phoenix's "self-contained release artifact" for free — the open questions are the Rails-style parts around it: generated Dockerfile, deploy tool, health endpoint, zero-downtime story.
- The compile-time/runtime config split Phoenix formalizes maps naturally onto Go (build-time vs env-at-boot); the Laravel/Rails "cache your config" machinery is a scripting-language workaround Volt doesn't need.
- Nobody except Django ships config linting (`check --deploy`); a typed-config language could do this at compile time — a potential differentiator.
- All four converge on: layered per-env config, env-var secrets at boot, a debug flag with prod warnings, and typed access to config — that's the table-stakes core.

## P19 — Extensibility: DI, events, hooks & packages

- **Go std**: no DI container, no signals, no app registry — the universal extension mechanism is the small implicitly-satisfied interface (`http.Handler`, `fs.FS`, `sql/driver`, `slog.Handler`); composition is import + constructor arguments by hand, distribution is go modules, registration is the `init()`+blank-import idiom.
- **Rails**: not DI — Railties. Every component is a Railtie; gems hook boot via initializers and `on_load`; engines are mountable mini-apps; `ActiveSupport::Notifications` is the pub/sub spine.
- **Laravel**: the service container is the universal seam — everything resolvable/replaceable; providers are the composition root, events decouple domains, macros/facades extend surfaces, and a documented package protocol powers the ecosystem.
- **Django**: no DI container — the composition unit is the *app* (`AppConfig.ready()` in a process-wide registry), wired by signals, checked by the system-check framework, with dotted-path settings as the override seam.
- **Phoenix**: no DI container, no global event bus. Extension is contracts: the Plug behaviour, Elixir protocols, Ecto callbacks, LiveView hooks, and telemetry — all wired explicitly.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Service container / DI | — (`context.WithValue` request-scoped only · EXT-14, EXT-18) | none, deliberate · DIY · EXT-14 | reflection autowiring, full binding vocabulary, contextual bindings/attributes, tagging, PSR-11 · CORE · EXT-1–6 | none; app registry instead · — · (P19 intro) | none; function args + explicit wiring · — · EXT-12 |
| Composition root / boot hook | `main()` by hand; `init()` + blank-import registration · TOOL · EXT-7 | Railtie initializers · CORE · EXT-1 | service providers `register`/`boot`, deferred providers · CORE · EXT-7, EXT-8 | `AppConfig.ready()` · CORE · EXT-10 | `MyAppWeb` macro module as app-wide seam · CORE · EXT-5 |
| Reusable package/app unit | go modules: semver, `go get`, vendoring · TOOL · EXT-16 | engines (mountable mini-apps) + plugin scaffold · CORE · EXT-2, EXT-5 | package protocol: auto-discovery + publishable groups · CORE · EXT-17 | apps framework + reusable-app conventions · CORE/OPT · EXT-10, EXT-12 | Hex/`mix deps` (incl. sparse git deps) · CORE · EXT-10 |
| Package ↔ host integration points | exported constructors only; no hooks/auto-discovery · TOOL · EXT-16 | migration copying, host-class config, `main_app`/engine URL proxies · CORE · EXT-3 | `mergeConfigFrom`, `loadRoutesFrom/Migrations/Views/Translations`, override cascade · CORE · EXT-18 | `INSTALLED_APPS` + app registry APIs · CORE · EXT-11 | pipelines + `forward` mount whole plug apps · CORE · EXT-11 |
| Host overrides package behavior | substitute implementations behind interfaces · TOOL · EXT-1 | view path shadowing, `to_prepare` + class_eval decorators · CORE · EXT-4 | publish + override cascade · CORE · EXT-17, EXT-18 | settings dotted-paths + subclassing · CORE · (P19 intro) | — |
| Domain event system | — (channels are the primitive · EXT-19) | `ActiveSupport::Notifications` as general pub/sub · CORE · EXT-6 | events/listeners, discovery, wildcards, subscribers, dispatch control, `Event::fake` · CORE · EXT-12, EXT-14, EXT-15 | `Signal` w/ weak refs, `dispatch_uid`; custom signals discouraged · CORE/DIY · EXT-1, EXT-9 | no bus; `Phoenix.PubSub` + `handle_info` idiom · CORE (idiom) · EXT-13 |
| Framework lifecycle events | — (EXT-19) | `on_load` lazy hooks + Notifications catalog · CORE · EXT-7, EXT-6 | framework events via same bus · CORE · EXT-12 | model/request/migrate/db/test/task signals · CORE/OPT · EXT-3–8 | telemetry events across the stack · CORE · EXT-6 |
| Async/queued event handling | — (goroutines/channels, DIY) | — (Active Job separately) | queued listeners: `ShouldQueue`, backoff, after-commit · CORE · EXT-13 | `asend`, async receivers via TaskGroup · CORE · EXT-2 | processes/PubSub natively async · CORE · EXT-13 |
| Middleware contract as extension seam | `http.Handler` wrapping + client-side `RoundTripper` · STD · EXT-2, EXT-3 | — (Rack, see P2) | — (see P2) | — (see P2) | Plug behaviour: `init/1`+`call/2` slots anywhere · CORE · EXT-1 |
| Interface/protocol extension points | implicit interfaces everywhere: `fs.FS`, `sql/driver` + `sql.Register`, `slog.Handler`/`LogValuer`, marshaler seams, `error` · STD · EXT-1, EXT-4–6, EXT-8–12 | ActiveModel lint tests · CORE · EXT-13 | contracts package for every component · CORE · EXT-11 | subclass-based protocols · CORE · (P19 intro) | protocols (`Phoenix.Param`, `FormData`, `Plug.Exception`) + Ecto adapter/repo callbacks · CORE · EXT-2, EXT-3 |
| Extend core API surfaces in place | — (no open classes; wrap instead) | `Concern` mixins + core extensions to Ruby itself · CORE · EXT-8, EXT-9 | macroable Response/HTTP/collections/strings; container `extend()` · CORE · EXT-16, EXT-5 | — | — (closed modules; use behaviours) |
| Static-ergonomics layer | — | — | facades + real-time facades · CORE · EXT-9, EXT-10 | — | — |
| Generator/scaffold extensibility | — | `hook_for :orm/:template_engine/:test_framework` · CORE · EXT-12 | — (stubs, see P17) | — | scope config reshapes future generators · CORE · EXT-9 |
| UI-framework lifecycle hooks | — | — | — | — | `on_mount` / `attach_hook` LiveView hooks · CORE · EXT-4 |
| Config linting / system checks | — (EXT-21) | — | — | check framework: tags, custom checks, deploy checks, silencing · CORE/DIY/OPT · EXT-13–19 | — |
| Generic model references | — | — | — (morphs, see P4) | contenttypes: GFK, GenericRelation, generic inlines, GenericPrefetch · OPT · EXT-20–25 | — |
| Stdlib-augmentation toolkit | — (it *is* the stdlib) | core extensions, time/date, `HashWithIndifferentAccess`, inquirers, callbacks · CORE · EXT-9–11 | — (see P25) | `django.utils.*`: functional/html/text/timezone/module_loading/encoding · OPT · EXT-26–31 | — (Elixir stdlib suffices) |
| Sanctioned internal-boundary pattern | — (`internal/` packages, outside corpus) | — | — | apps as boundaries · CORE · EXT-10 | contexts: public module, private schemas · CORE (pattern) · EXT-8 |
| Load-order-safe lazy extension | package `init()` dependency order · TOOL · EXT-7 | `ActiveSupport.on_load` · CORE · EXT-7 | deferred providers · CORE · EXT-8 | three-stage `django.setup()` loading · CORE · EXT-12 | — (OTP app deps order) |

**Notable divergences.** The DI split is the sharpest disagreement in the whole comparison: Laravel makes a service container the universal extension seam; Rails, Phoenix, and Django all explicitly reject one, substituting Railties/conventions, explicit functional wiring, and the app registry respectively — three different "no"s to the same question. Event systems diverge just as much: Laravel has a rich domain-event bus with queued listeners, Django has signals (whose own docs discourage them vs. explicit calls), Rails repurposes its instrumentation bus, and Phoenix deliberately has no bus at all (PubSub + message passing is the idiom). Rails and Laravel embrace open-class/macro modification of core surfaces; Phoenix and Django cannot or do not, extending via protocols/subclassing instead. Only Rails (engines) has a package unit that carries its own routes/MVC/migrations as a mountable sub-application; Django's apps are close but flatter; Laravel packages integrate via a publish protocol; Phoenix packages are plain libraries plus `forward`.

**Volt notes.**
- Go can't do Laravel-style reflection autowiring ergonomically or Rails-style monkey-patching at all; the realistic seams are Phoenix's — interfaces (behaviours/protocols), explicit middleware contracts, and compile-time codegen — which is also the answer 3 of 4 frameworks chose anyway.
- Every framework has *some* blessed "package registers itself at boot" hook (provider / Railtie / AppConfig.ready / OTP app). Go's `init()` is too weak and implicit; Volt needs an explicit registration contract.
- The event-bus question is genuinely open: strong bus (Laravel) vs instrumentation-bus-doubling-as-events (Rails) vs none (Phoenix). Go channels ≠ an app-level event API; Volt must decide tier, not just mechanism.
- Django's system-check framework (config linting with tags, extensible by packages) has no equivalent in the other three and fits a static language well.
- The Go baseline strengthens the case for the Phoenix path: the stdlib is itself a catalog of extension seams (`http.Handler`, `fs.FS`, `sql/driver`, `slog.Handler`), so Volt can adopt existing std interfaces as its extension points instead of inventing framework-specific contracts.

## P20 — Observability: logging, metrics, errors

- **Go std**: unusually strong — `log/slog` structured logging with pluggable handlers, `expvar` + `runtime/metrics` for metrics, always-on production profiling over HTTP (pprof), and `runtime/trace` with a flight recorder; but nothing exports any of it (no Prometheus/OTLP, no tracing, no error reporter, no request-log middleware).
- **Rails**: tagged logger + a ~70-event instrumentation bus, a first-class error-reporter API spanning requests/jobs, and (8.1) structured event reporting; APMs subscribe rather than monkey-patch.
- **Laravel**: Monolog channel logging + a centralized exception pipeline in core; Telescope (local debug), Pulse (prod perf dashboard), Pail (log tailing) as first-party add-ons; no metrics/tracing exporters.
- **Django**: stdlib `logging` via `LOGGING` dictConfig with framework-named loggers, error reports emailed to `ADMINS` with sensitive-data scrubbing, rich debug page; no metrics or tracing story.
- **Phoenix**: `:telemetry` events emitted by every layer, `Telemetry.Metrics` declarations, pluggable reporters, LiveDashboard as built-in visualizer — metrics-first, error-tracking absent.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Logging core | `log/slog` levels + Text/JSON handlers · STD · OBS-1, OBS-2 | `BroadcastLogger` over AS::Logger, per-env levels · CORE · OBS-1 | Monolog channels (daily/slack/syslog/custom) · CORE · OBS-1 | stdlib `LOGGING` dictConfig + defaults · CORE · OBS-1, OBS-2 | Plug/Phoenix logger · CORE · OBS-9 |
| Multi-destination fan-out | `slog.NewMultiHandler` · STD · OBS-3 | BroadcastLogger · CORE · OBS-1 | log stacks, per-channel levels · CORE · OBS-2 | handlers/filters per logger · CORE · OBS-1 | — (Logger backends, outside corpus) |
| Structured/contextual log metadata | `Logger.With`/`WithGroup` + ctx-aware calls · STD · OBS-5, OBS-6 | tagged logging (`log_tags`, `tagged` blocks) · CORE · OBS-2 | `Context` request/job-scoped, queue-propagating; `withContext` · CORE · OBS-3, OBS-10 | — (filters only · OBS-5) | request-id metadata · CORE · OBS-10 |
| Framework-named logger taxonomy | — | — | — | `django.request/db/security.*/server…` loggers · CORE · OBS-3 | `Phoenix.Logger` event catalog · CORE · OBS-5 |
| Instrumentation bus / event spans | — (`runtime/trace` tasks/regions are tracer-only · OBS-18) | Notifications: ~70 documented timed events · CORE · OBS-3 | — (gap; domain events only) | — (signals are not timed instrumentation) | `:telemetry` events everywhere + span pattern · CORE · OBS-1, OBS-5–8 |
| Structured app-event reporting | — | `Rails.event.notify` (8.1), tags, pluggable emit · CORE · OBS-6 | — | — | `:telemetry.execute` custom events · CORE · OBS-8 |
| Metrics declaration/aggregation | `expvar` + `runtime/metrics` registry · STD · OBS-12, OBS-13 | — (events are raw material) · DIY · OBS-14 | — · OBS-15 (named gap) | — (no story, per P20 intro) | `Telemetry.Metrics` 5 types, tags, poller · CORE · OBS-2, OBS-3 |
| Metrics exporters (StatsD/Prometheus/OTel) | — (named gap · OBS-23) | — · DIY · OBS-14 | — · OBS-15 | — | pluggable reporters; Console bundled, rest hex · CORE/ECO · OBS-4 |
| Error/exception reporting API | — (OBS-25) | `Rails.error.handle/record/report` + subscribers, auto-wrapped executions · CORE · OBS-4 | `report()` hooks, per-type levels, ignore lists, dedup · CORE · OBS-6 | ADMINS email on 500, MANAGERS on 404 · OPT · OBS-4, OBS-8 | — (telemetry is the surface) · — · OBS-13 |
| Error-service integration model | — (OBS-25) | Sentry et al. register reporter subscribers · ECO · OBS-13 | report hooks · CORE · OBS-6 | logging handler swap · OPT · OBS-4, OBS-6 | telemetry handlers · — · OBS-13 |
| Soft assertions | — | `Rails.error.unexpected`: raise in dev, report in prod · CORE · OBS-5 | — | — | — |
| Error report throttling/sampling | — | — | `Lottery` sampling, `Limit` caps · CORE · OBS-8 | `IGNORABLE_404_URLS` only · OPT · OBS-8 | — |
| Sensitive-data scrubbing in reports | `ReplaceAttr` + `LogValuer` redaction · STD · OBS-2, OBS-7 | param filtering (see P8) | — (hidden context · OBS-10) | `sensitive_variables`/`sensitive_post_parameters`, reporter filters · OPT/DIY · OBS-9, OBS-10 | password/token param filtering in logs · CORE · OBS-9 |
| Custom error rendering (prod) | — | — (public/*.html, outside section) | per-type `render()`, JSON-vs-HTML, per-status views · CORE · OBS-7, OBS-9 | overridable 400/403/404/500 templates + views · CORE · OBS-11 | ErrorHTML/ErrorJSON modules · CORE · OBS-12 |
| Dev error pages / debuggers | — | web-console REPL, actionable errors, `debug` gem · CORE · OBS-8, OBS-9 | — (Telescope dumps · OBS-11) | debug 500 page w/ locals · CORE · OBS-11 | Plug.Debugger-style pages with actions · CORE · OBS-12 |
| Request correlation IDs | — (no request-log middleware · OBS-11) | `log_tags :request_id` · CORE · OBS-2 | via `Context` · CORE · OBS-10 | — | `Plug.RequestId` → `x-request-id` · CORE · OBS-10 |
| SQL observability | — | verbose query logs (caller line), SQL comment tags · CORE · OBS-7 | Telescope query watcher w/ slow flag · OPT · OBS-11 | `django.db.backends` logger (DEBUG only), `test --debug-sql` · OPT · OBS-7 | Ecto telemetry timings + dev debug logs · CORE · OBS-6, OBS-9 |
| Live observability dashboard | `/debug/pprof/` index + `/debug/vars` (raw) · STD · OBS-17, OBS-12 | — | Telescope (19 watchers, local) + Pulse (prod, custom cards) · OPT · OBS-11, OBS-12, OBS-13 | — | LiveDashboard: real-time metrics charts + request logger · CORE · OBS-11 |
| Log tailing tooling | — | — | Pail CLI (`--filter/--level/--user`) · OPT · OBS-5 | — | LiveDashboard RequestLogger · CORE · OBS-11 |
| Subsystem health monitors | `runtime/metrics` + `ReadMemStats` VM numbers · STD · OBS-13, OBS-14 | — | `db:monitor`, `queue:monitor` + alert events · CORE · OBS-14 | — | telemetry_poller VM measurements · CORE · OBS-2 |
| In-browser perf surfacing | — | `ServerTiming` middleware → DevTools · CORE · OBS-10 | — | — | — |
| Logger internals customization | `slog.Handler` custom sinks; `LevelVar` runtime level · STD · OBS-3, OBS-4 | custom loggers per component · CORE · OBS-1 | Monolog tap/formatters/processors · CORE · OBS-4 | `LOGGING_CONFIG` swap/disable · OPT · OBS-6 | — |

**Notable divergences.** No framework ships a metrics *exporter*, but they differ on how close they get: Phoenix has a full metrics pipeline (declare + aggregate + pluggable reporters, dashboard) stopping only at the wire format; Rails ships the instrumentation events and says "subscribe"; Laravel and Django name it a flat gap. Error reporting inverts that ranking: Laravel and Rails have rich first-class error-reporter pipelines (hooks, dedup, sampling, subscriber APIs), Django's is a 1.0-era email-the-admins design with excellent scrubbing, and Phoenix has nothing — telemetry is presumed sufficient. Dashboards split three ways: Phoenix bundles LiveDashboard in every app (CORE), Laravel offers Telescope/Pulse as opt-in packages, Rails and Django offer none. Rails 8.1's structured event reporter and `Rails.error.unexpected` soft assertions are novel and have no counterpart elsewhere.

**Volt notes.**
- Go's ecosystem standard is OpenTelemetry + `log/slog`; every one of these four predates or ignores OTel — shipping OTel-native instrumentation (Phoenix's telemetry model with an exporter, closing the gap all four share) is the clearest open lane.
- The Phoenix/Rails pattern — framework emits named timed events, everything else (logs, metrics, APM) subscribes — decouples core from vendors and is directly portable to Go.
- An error-reporter API à la Rails (`handle/record/report` + subscribers, spanning HTTP and background work) is cheap in core and prevents every APM from reinventing panic-recovery middleware.
- Sensitive-data scrubbing appears in three of four inventories (params, locals, headers); it needs to be designed into logging/error paths from day one, not bolted on.
- The Go baseline also hands Volt observability primitives none of the four frameworks possess — always-on production profiling (`net/http/pprof`), profiler labels for per-route attribution, and the 1.25 trace flight recorder — so surfacing them behind an authenticated mount is near-free differentiation.

## P21 — Admin & operational UIs

- **Go std**: nothing — no admin site, no CRUD scaffolding, no ops dashboard; the only self-served pages are the `/debug/pprof/` and `/debug/vars` diagnostic indexes, which are unauthenticated developer surfaces, not administration.
- **Rails**: thin, targeted surfaces (mailer previews, inbound-mail conductor, Mission Control jobs UI); data admin is app code you write by hand.
- **Laravel**: operational dashboards for its own subsystems (Horizon, Telescope, Pulse) behind gates; no domain CRUD admin in OSS (Nova is commercial, Filament community).
- **Django**: `django.contrib.admin`, the flagship — register a model and get list/search/filter/edit/history/inlines/actions/permissions/theming; 39 inventory rows on its own.
- **Phoenix**: LiveDashboard for runtime introspection in every default app; no data-admin framework — generators scaffold your own `/admin` CRUD.

| Capability | Go std | Rails | Laravel | Django | Phoenix |
|---|---|---|---|---|---|
| Domain CRUD admin framework | — (no ORM or generator to build on · ADMIN-1, ADMIN-6) | — (guides teach hand-built admin namespaces) · DIY · ADMIN-1 | — (commercial Nova / community Filament, outside corpus) · ADMIN-5 | ModelAdmin registration + autodiscovery · OPT · ADMIN-1, ADMIN-2 | — (scaffold your own `/admin` resources; gap by design) · ADMIN-4 |
| Changelist: columns/filters/search/facets | — | — | — | `list_display`, `list_filter` + custom filters, `search_fields`, facet counts, date hierarchy · OPT/DIY · ADMIN-3–11 | — |
| Changelist perf/pagination controls | — | — | — | `list_select_related`, pagination, ordering, `list_editable` · OPT · ADMIN-9, ADMIN-12–15 | — |
| Edit-form customization & widgets | — | — | — | fieldsets, readonly, autocomplete/raw-id/filter_horizontal, form overrides, formfield hooks · OPT/DIY · ADMIN-16–23 | — |
| Inline/nested editing | — | — | — | Tabular/Stacked inlines, M2M-through, generic inlines · OPT · ADMIN-24, ADMIN-25 | — |
| Bulk actions | — | — | — | `@admin.action`, site-wide actions, intermediate pages · OPT · ADMIN-29, ADMIN-30 | — |
| Admin permissions/row scoping | — (no auth system to integrate · ADMIN-2) | hand-rolled admin flag · DIY · ADMIN-1 | gate-protected dashboards · OPT · ADMIN-4 | per-action permission hooks, per-user `get_queryset()`, `staff_member_required` · OPT · ADMIN-28, ADMIN-39 | scoped routes/live_session auth (idiom) · — · ADMIN-4 |
| Admin extensibility (custom views/URLs/templates/theming) | — | — | — | `get_urls`, AdminSite subclassing, template overrides, CSS-var theming + dark mode, JS events · OPT/DIY · ADMIN-31–36 | — |
| Audit trail of admin edits | — (ADMIN-3) | — | — | `LogEntry` history model + history view · OPT · ADMIN-37 | — |
| Auto-generated internal docs | — | — | — | admindocs (docstrings, reST roles, bookmarklet) · OPT · ADMIN-38 | — |
| Jobs/queue operations UI | — (nothing to dashboard · ADMIN-4) | Mission Control — Jobs: inspect/retry/discard · OPT · ADMIN-2 | Horizon: workload, metrics, failed-job retry · OPT · ADMIN-1 | — | — (Oban Web outside corpus) |
| Runtime/perf dashboard | `/debug/pprof/` + `/debug/vars` diagnostics · STD · ADMIN-5 | — | Telescope `/telescope` + Pulse `/pulse` · OPT · ADMIN-2, ADMIN-3 | — | LiveDashboard in every default app · CORE · ADMIN-1 |
| Live process/VM inspection | goroutine/heap dumps via `/debug/pprof/` · STD · ADMIN-5 | — | — | — | Erlang `:observer` · CORE (runtime) · ADMIN-3 |
| Mail dev/ops UIs | — | mailer previews `/rails/mailers` + inbound Conductor · CORE · ADMIN-3, ADMIN-4 | — (Mailpit via Sail, P18 CONF-13) | — | `/dev/mailbox` Swoosh preview · CORE · ADMIN-2 |
| Health endpoint | — (P20 OBS-26) | `/up` for load balancers · CORE · ADMIN-5 | `/up` (P18 CONF-10) | — | — |
| Dashboard access-control pattern | — (debug pages self-register unauthenticated · ADMIN-5) | — (DIY) | `viewHorizon/viewTelescope/viewPulse` gates · OPT · ADMIN-4 | admin login + staff flag · OPT · ADMIN-39 | dev-only mount by default · CORE · ADMIN-1 |

**Notable divergences.** This is the most lopsided section: Django's model admin is a 39-row flagship with no counterpart — the other three all explicitly declare domain CRUD admin a non-goal (Laravel routes it to a commercial product, Rails and Phoenix to hand-written app code). Conversely, Django has *zero* operational/runtime UI, while Laravel (Horizon/Telescope/Pulse) and Phoenix (LiveDashboard, `:observer`) are strongest exactly there; Rails sits in between with narrow single-purpose surfaces. Tier philosophy differs too: Phoenix mounts its dashboard in every generated app (CORE), Laravel's are opt-in packages, and Rails keeps its ops UIs minimal and mostly dev-oriented.

**Volt notes.**
- "Ops dashboards for the framework's own subsystems: yes; domain CRUD admin: no" is the 3-of-4 consensus — and Django's admin is widely cited as its killer feature. A Go framework must consciously pick a side; Go's static typing + struct tags could make a reflection/codegen-driven admin cheaper than in Rails/Phoenix.
- Every dashboard here needed an auth answer (gates, staff flags, dev-only mounts); design the protected-mount primitive once and reuse it for all operational UIs.
- Phoenix shows a real-time dashboard is viable as a default-on component when the framework already owns a live-update transport; that couples P21 to Volt's websocket/SSE story.
- Small dev surfaces (mailer preview, health endpoint, jobs UI) appear repeatedly across frameworks and are cheap wins independent of the big-admin decision.
- Go std sharpens the auth point: its only self-served pages (`/debug/pprof/`, `/debug/vars`) register themselves on `DefaultServeMux` unauthenticated, so Volt's protected-mount primitive must gate std's own debug surfaces, not just Volt's dashboards.

---

**First-party breadth outliers.** Laravel's extra sections show it expanding first-party into *product* domains: P23 ships a provider-agnostic AI SDK, agent framework, MCP server/client, and agent-onboarding tooling (Boost) as a new pillar, and P24 ships Cashier's full Stripe/Paddle subscription-billing wrappers — territory no other framework's docs touch. Django's P22 (GeoDjango) is the opposite kind of breadth: deep specialized *infrastructure* — a complete GIS stack (spatial fields, lookups, DB functions, GEOS/GDAL bindings) maintained in contrib for decades. The signal: Laravel grows toward monetizable application features, Django toward encyclopedic built-in capability, while Rails and Phoenix keep their breadth inside the web-runtime envelope (Rails' extra depth goes to deployment, Phoenix's to the BEAM runtime itself).

---

## What Go already covers

Adding the Go std baseline column makes one question answerable at a glance for every row of P1–P21: does Volt have to build this, or does it come with the toolchain? Grouped, the answers form two short lists.

**Where the std baseline is already strong.** A meaningful slice of the table-stakes list below arrives before Volt writes a line: verb-aware parameterized routing with shadowing-proof precedence (`ServeMux` 1.22+); a production-grade HTTP/1.1+HTTP/2 server with timeouts, graceful shutdown, and per-request panic isolation; in-process TLS 1.3 termination; templates whose contextual auto-escaping (HTML/attr/CSS/JS/URL) is *stronger* than the blunt HTML escaping all four frameworks apply; JSON/XML/CSV codecs with struct-tag field mapping; multipart parsing and streaming responses (`http.Flusher` covers SSE today); origin-checked CSRF (1.25 `CrossOriginProtection`); structured logging with swappable backends (`slog`); a complete zero-config test story — subtests, fuzzing, benchmarks, coverage, the race detector, `httptest` — that no framework needed to scaffold; best-in-class profiling (pprof, `runtime/metrics`, flight recorder, PGO); and the single-binary deploy path (`go build`, `//go:embed`, cross-compilation) — the one problem area (P18) where std delivers the finished product rather than primitives.

**And what it doesn't.** The load-bearing NOs cluster into the framework Volt has to be: everything above the raw SQL string — ORM, relations, query building, migrations, seeding (P4/P5); a validation framework with declarative rules and field-keyed errors (P6); sessions, the entire auth stack, and the hidden-field-token CSRF fallback (P2/P7/P8); WebSockets and everything real-time above SSE (P10); durable jobs, retry/backoff, and cron (P9); mail composition, attachments, and pluggable/dev-mode backends (P11); a cache framework at any granularity (P12); i18n message catalogs and a per-request locale pipeline (P15); the asset pipeline (P3); scaffolding and a codegen CLI (P17); config loading and layering (P18); DI/events (P19); metrics export and trace propagation (P20); and admin (P21). [gostd.md](./gostd.md)'s "The gap Volt fills" reads this list top to bottom and calls it a fair first draft of Volt's table of contents.

## Table stakes

*With the Go std column in place, this section doubles as Volt's build-vs-free map: each capability below is either already in the baseline (STD/TOOL/X) or a NO Volt must supply.*

Capabilities all four frameworks provide (tier and mechanism vary; qualifications noted). This is the must-have baseline any full-stack framework — Volt included — is measured against.

**Routing & HTTP.** Parameterized route declaration with named routes and URL generation; grouping/prefixing/namespacing; composable route files or sub-routers; a route-resolution performance answer (compile or cache); route introspection (a CLI in three, programmatic in Django).

**Request layer.** A middleware pipeline with explicit ordering control and short-circuiting; a curated default middleware battery (request ID, parsers, session, CSRF, proxy-header rewriting) with a documented order; a rich request-introspection object; body parsing and multipart uploads; content negotiation; sessions over pluggable stores; signed cookies (Phoenix: session cookie only); flash messages; streaming responses/SSE; reverse-proxy header trust; exception-to-response mapping with dev error pages and overridable production error rendering.

**Views & assets.** Auto-escaping templates with an explicit raw opt-out; layouts/inheritance plus partials or components; template compilation or caching; inline/string rendering; named-fragment/partial responses (the htmx/Turbo pattern — now in all four); fingerprinted assets with a build manifest.

**Data layer.** Parameterized-by-construction queries; a composable/lazy query-building model; associations including many-to-many with join-table data; explicit eager loading plus an N+1 guardrail; transactions with after-commit hooks; single-record finders with raise-on-missing variants; bulk writes; race-free atomic updates; enum attributes; auto timestamps; batch/streaming reads for large tables; a raw-SQL escape hatch. Migrations: generated files, deterministic ordering, an applied-versions ledger, rollback, and a seeding story.

**Validation.** One error-value contract (message bag / errors object / changeset / errors dict) consumed by both HTML forms and JSON responses; typed coercion of inbound params somewhere in the stack; custom and cross-field validators; a documented uniqueness answer; error-message customization and i18n.

**Auth & security.** bcrypt/argon2-class password hashing; session login/logout with server-side revocation; route/controller guards; purpose-scoped expiring tokens; CSRF protection for browser routes; XSS-safe templating; secret-keyed signing with (in three of four) fallback-key rotation; host/proxy trust configuration.

**Mail.** A message-composition abstraction over pluggable transports; dev preview/capture backends; test capture with assertions.

**Testing.** A test harness scaffolded with the app; an in-process HTTP client with framework-aware response assertions; a DB isolation strategy; auth helpers; view/template render tests; exception-path assertions.

**CLI & DX.** A project generator; a dev server with code reload and live reload; custom app-defined commands; introspection commands; a REPL/console — CORE in all four, and the one table-stake Go cannot replicate (see P17 notes).

**Config & ops.** Layered per-environment config with typed access; env-var secrets at boot; a debug flag with production warnings; a package/app unit with a blessed boot-registration hook; multi-destination logging.

**i18n.** Translation catalogs with an in-template retrieval API and variable interpolation (pluralization documented in three; negotiation middleware in only one).

Just as telling is what is *not* table stakes: background jobs, websockets, caching, rate limiting, CORS, and file-storage abstraction each have at least one refusenik among the four. The "obvious" batteries are contested territory, not baseline.

## Differentiators

Capabilities that exist in only one or two of the four — where each framework's identity actually lives.

**Phoenix-only.**
- **LiveView** — server-rendered reactive UI over the channel transport: 30+ inventory rows (streams, uploads, live navigation, form recovery, crash remount) with no counterpart anywhere (P10).
- **Compile-time verification suite** — verified `~p` route literals, HEEx HTML/attribute validation, compile-checked Ecto queries, SQL-interpolation-as-compile-error (P1, P3, P4, P8).
- **BEAM primitives** — zero-broker PubSub and clustering, masterless CRDT Presence, supervision trees, remote production IEx, `:observer` (P10, P17, P18); the only framework with a documented delivery-semantics story (at-most-once + client outbox).
- **SQL Sandbox** — the only DB-test isolation compatible with concurrent tests (P16); **Scopes** — generated row-level authorization threaded through every context/query (P7); Mix-free release binaries with `bin/migrate` (P5, P18); the formalized compile-time/runtime config split (P18).

**Django-only.**
- **The model admin** — 39 rows of list/filter/search/inlines/actions/permissions/audit derived from model metadata; the other three explicitly declare domain CRUD admin a non-goal (P21).
- **Derived migrations** — autodetection by diffing model state, with the squash/fake/`RunPython`/historical-models apparatus around it (P5); the system-check framework (`check --deploy`, extensible tags) (P18, P19).
- **Full gettext pipeline** — extraction/compilation commands, `LocaleMiddleware` negotiation, JS catalogs, plus an entire timezone framework filed under i18n (P15).
- Generic CBVs + formsets as request-layer CRUD machinery (P2, P6); the pluggable chunked upload-handler pipeline (P13); multi-engine template abstraction (P3); a published API-stability promise; GeoDjango.

**Laravel-only.**
- **The first-party package ring** — Fortify (2FA, passkeys), Passport (OAuth2 server), Sanctum (SPA/API tokens), Socialite, Reverb, Horizon, Cashier, Scout, Octane, Pennant, plus the new AI/MCP pillar: no other framework's docs go past email login or touch these product domains (P7, P10, breadth outliers).
- **Multi-channel notifications** — one class fanned out to mail/database/broadcast/SMS/Slack; Rails and Phoenix both name this a gap (P11).
- **Job orchestration depth** — chains, batches with completion callbacks, unique/debounced jobs, encrypted payloads, driver failover, plus a code-defined scheduler with multi-server locks (P9).
- Cache tags, atomic locks/funnels, and a general rate limiter built on the cache (P12); implicit route-model binding (P1); Precognition live validation (P6); maintenance mode (P18); pgvector in the query builder (P4); the Prompts TUI + console testing (P17).

**Rails-only.**
- **The Solid trifecta** — Queue, Cache, and Cable all running on the app's own RDBMS, making the database the default infrastructure (P9, P10, P12); **Kamal + Thruster + generated Dockerfile** owning the deploy path (P18).
- **Action Mailbox** — inbound email as a framework concern, uncopied by anyone (P11); Action Text rich-text (P3); job Continuations (resumable long jobs) (P9).
- Encrypted credentials in-repo (P8); strong parameters at the controller boundary (P2); russian-doll fragment caching (P12); versioned `load_defaults` upgrades (P18); `bin/ci` local CI DSL + Brakeman in generated CI (P16, P8); global ReDoS timeout (P8); engines as mountable full-stack mini-apps (P19); core optimistic locking and attribute encryption, and full composite-PK support (P4).

**Two-of-four capabilities worth noting.** Generated, owned auth code (Rails 8 + Phoenix — the newest designs converge here, P7); presence tracking (Laravel + Phoenix, P10); predictive/live validation (Laravel Precognition + Phoenix LiveView, P6); ops dashboards for the framework's own subsystems (Laravel's Horizon/Telescope/Pulse + Phoenix's LiveDashboard, P21); HTML-over-the-wire as default interactivity (Rails Hotwire + Phoenix LiveView, P3); no-Node asset pipelines (Rails importmaps + Phoenix-wrapped esbuild, P3); direct-to-cloud presigned uploads (all but Django, P13).

## Universal gaps

What none of the four ships in core — the open opportunities for a new framework.

1. **OpenAPI/schema generation, API versioning, GraphQL.** The single richest collectively-marked gap (P14): all four route versioning through plain prefixes/scopes, none generates a schema, and none ships a serializer with a typed contract. A statically-typed framework could derive OpenAPI from handler/struct types — something none of these dynamic-language cores could offer cheaply.
2. **Metrics and tracing exporters.** No Prometheus/StatsD/OpenTelemetry wire-up anywhere (P20). Phoenix gets closest (declare + aggregate + pluggable reporters) but stops at the wire format; Rails emits events and says "subscribe"; Laravel and Django name it a flat gap. All four predate or ignore OTel — OTel-native instrumentation is the clearest open lane.
3. **Replica read load-balancing.** Role/replica *switching* exists in all four data layers, but distributing reads is nobody's business — Rails marks it explicitly out of scope (P4).
4. **Real-time delivery guarantees.** At-most-once is the best documented semantics (Phoenix); message persistence and reconnect catch-up are application code everywhere; the other three don't state guarantees at all (P10).
5. **Application search.** No first-party search engine or typo-tolerant/faceted search: database FTS at best, with Laravel's Scout syncing to third-party services and Rails naming FTS a non-goal (P12).
6. **Migration safety tooling.** No drift detection, no migration linting, no zero-downtime automation — only documented recipes (Django's concurrent-index/NOT VALID patterns, Laravel's `--isolated` lock) (P5).
7. **Multi-tenancy as a product.** Fragments exist (Phoenix Scopes and query prefixes, Rails horizontal sharding, Laravel/Django nothing), but no framework ships an end-to-end tenancy story (P4, P7).
8. **Secrets beyond the environment.** Rails' encrypted-credentials file is the only in-repo answer; no framework integrates external secret managers (P8, P18).
9. **Config validation.** Only Django lints configuration at all (`check --deploy`, runtime); typed compile-time config checking is unclaimed territory (P18).

**Near-universal gaps (one framework has it).** CORS in core (Laravel only — Rails/Phoenix/Django all point at ecosystem middleware, P1/P8); server-side rate limiting (Laravel and Rails only; Phoenix marks it a gap, Django an explicit non-goal, P12); object-level authorization as a shipped framework (Laravel policies only — Django deliberately stubs its object hooks, Rails ships nothing, Phoenix's Scopes are generated per-app code, P7); locale-negotiation middleware (Django only, P15); WebSockets in core (absent from Django, externalized by Laravel, P10); a durable job queue in the default install (Rails' Solid Queue only, P9); inbound email (Rails only, P11); a domain admin (Django only, P21). Each of these is a proven-demand capability where three of four frameworks leave users to the ecosystem.

## Design tensions for Volt

Ten decisions where the four frameworks split — with the trade-offs as evidence, not verdicts.

1. **Runtime model vs goroutines.** The four answer concurrency at the runtime layer: PHP process-per-request (escaped via Octane), Ruby threads behind the GVL (escaped via tuning playbooks), BEAM processes (no escape needed), Python's dual sync/async stack (~20 inventory rows of adaptation rules — the cautionary tale of bolting async on). Go gets Octane/Puma-class concerns for free, but goroutines are not BEAM processes: no supervision, no isolation, no crash containment (P9 notes). Volt must decide how much explicit lifecycle/panic-recovery machinery replaces what Elixir gets from OTP and what the others get from process boundaries.

2. **Reflection magic vs codegen.** Rails and Laravel buy their convention density with runtime metaprogramming Go does not have; Phoenix shows the alternative — compile-time macros, verified routes/queries/templates, and generators that write owned code. Phoenix's compile-time verification is the only static-safety story among the four and the most natural fit for Go; Volt's sibling project **not-an-orm** has already staked the codegen position for the data layer. The cost side is real: generated code freezes at generation time (Phoenix documents that security fixes will not auto-apply), so a codegen-first Volt needs a regeneration/upgrade story from day one.

3. **Batteries vs ecosystem.** Laravel ships everything first-party; Rails ships a complete-but-swappable default meal; Django ships interfaces and lets the ecosystem compete (its tasks framework deliberately has no worker); Phoenix ships almost nothing outside the runtime. Go's ecosystem norm is à-la-carte — which is precisely why a curated default (P2's middleware battery, P9's durable queue) is itself a headline feature. The tension: every battery Volt ships is a maintenance commitment and an ecosystem it partially forecloses; every battery it skips re-creates the fragmentation users come to a framework to escape.

4. **Active record vs data mapper vs schema-as-truth.** Three frameworks put persistence and behavior on the model object; Ecto refuses and eliminates whole bug classes (lazy loading, callbacks, global scopes) structurally. Orthogonally, the single source of truth differs: Rails reflects from the live schema, Django derives everything (forms, admin, migrations) from the model declaration, Ecto declares schemas that queries are checked against. For Volt the question is what Go structs are: hand-authored truth, codegen output from SQL (the not-an-orm stance), or reflection targets. Each choice fixes where the Django-style "derive everything from one declaration" leverage can and cannot come from.

5. **Where validation lives.** Four genuinely different answers: the HTTP boundary (Laravel), the model on save (Rails), an explicit pipeline value between boundary and repo (Phoenix changesets), a form class that also owns rendering (Django). The convergent artifact is a single error-value contract serving both HTML and JSON — designing that struct first is the load-bearing decision (P6 notes). Phoenix's constraint-errors-as-data (DB violations translated into the same error shape, race-proof uniqueness) maps cleanly onto Go's `(T, error)` idiom; Laravel's ~100-rule string DSL is the exact trade Go would not make.

6. **Real-time as core vs bolt-on.** Four architectures: socket-in-process with channel classes (Rails), externalized broadcast driver (Laravel), socket-as-programming-model (Phoenix), refusal (Django). Goroutine-per-connection makes an in-process CORE websocket layer cheap for Volt, and topic routing + per-topic join auth is the convergent abstraction across all three implementers. But single-node is the easy 90%: Phoenix's clustering/presence has no Go equivalent, so multi-node Volt needs an explicit backplane (Redis/NATS/Postgres) — and the LiveView-scale bet (real-time as the *UI paradigm*) is a separable product decision, not a transport decision.

7. **Derived vs authored migrations.** Django derives migrations by diffing model state — and pays for it with dependency graphs, historical models, and a value-serialization apparatus the other three simply don't need; Laravel/Rails/Phoenix have developers author migrations with smart generators, and Rails adds a canonical schema snapshot so fresh DBs skip the replay. Autodetection in Go would require serializing model state without Python's introspection — heavy. Authored-with-generators plus a canonical snapshot, plus Phoenix's Mix-free `bin/migrate` release pattern (the closest analogue to Go's static-binary deploys), is the low-complexity path; derived migrations are the higher-magic, higher-DX bet.

8. **Convention density.** Rails demonstrates the ceiling (naming is the wiring, zero registration) and Django the counter-position (explicit registration, "no magic" as doctrine). Go's culture sides with Django, but the chunks show conventions are where Rails/Laravel productivity actually comes from. Codegen offers Volt a third way — *generated* convention, where the wiring is explicit in code but written by the tool — at the price of more generated surface to own and regenerate.

9. **DI container vs explicit wiring.** The sharpest disagreement in the whole comparison (P19): Laravel makes a reflection-autowired container the universal extension seam; Rails, Phoenix, and Django all reject one, with three different substitutes (Railties, plain function arguments, the app registry). Go cannot do reflection autowiring ergonomically, and three of four frameworks chose against it anyway — but every framework has *some* blessed "package registers itself at boot" contract, and Go's `init()` is too weak and implicit to be it. Volt needs an explicit registration/composition contract even if it never needs a container.

10. **Generated-owned auth — and how far batteries extend past login.** The 2024–25 convergence (Rails 8, Phoenix 1.8) is generated, editable auth code over installed libraries, which suits Go's explicitness — but it re-raises tension #2's patch problem for the most security-critical code in the app. And the table-stakes union beyond login is large (email verification, reset or magic links, sudo mode, per-device revocation, throttling, API tokens); only Laravel keeps going into 2FA, passkeys, OAuth2-server, and social login as first-party packages. Volt must pick both the delivery mechanism (generated vs packaged) and where on the Laravel–Phoenix breadth spectrum its auth story stops.
